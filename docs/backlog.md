# Бэклог владельца

Сюда пишет только владелец. Агент читает записи на старте сессии, предлагает
взять их в работу и удаляет выполненное после себя. Агент сам сюда не пишет,
если владелец прямо не попросил.

<!-- Записи ниже. Одна запись — один пункт списка. -->

- **Ловить ошибку брошенной футуры.** Задача бежит в своей error-зоне, и
  ошибка future, которую тело запустило и не дождалось, достаётся
  наблюдателю задачи, а не окружающей зоне. Сегодня такая ошибка уходит в
  корневую зону и роняет процесс — проверено стендом 2026-09-07. Ядро уже
  делает соседнее: шлёт неотнаблюдённый провал исхода в зону создания
  (`_reportUnobserved`, `job_base.dart`). Цена: форк зоны на каждую задачу
  и то, что ошибка может прийти сильно позже конца задачи, — решить, кому
  она тогда достаётся. Разбор, почему зона не годится для ожидания
  брошенной работы (I/O она не видит вовсе), — в обсуждении спеки уборки
  2026-09-07.
