> **Состояние на 2026-09-10:** выполнено; входит в этот коммит в `main`.
> **Что это:** план реализации и проверки `Job.then`.
> **Связанные записи:** `2026-09-10[3]-job-then-design.md`,
> `2026-09-10[2]-job-then-report.md`.

# План

Цель: последовательность `Job` с двусторонним каскадом отмены.
Дизайн: `2026-09-10[3]-job-then-design.md`.
Стек: Dart, `FakeAsync`, `Completer`, проверки пакетов и документов.
Работа в `main`; без push и публикации. Выполнение в текущей сессии.

## Реализация ядра

Файлы: `packages/async_job/lib/src/job_base.dart`, `job_then.dart`,
`outcome.dart`; тесты `packages/async_job/test/then_test.dart`.

- [x] Добавить тест последовательности: исходный `Job<int>` возвращает 2,
  первое продолжение добавляет 3, второе возвращает строку `value=5`.
  Журнал должен показать уборку каждого звена перед следующим колбэком.
- [x] Проверить отсутствие API командой `dart test test/then_test.dart`
  из пакета ядра. Ожидается отказ компиляции на отсутствующем `then`.
- [x] Добавить метод интерфейса и реализацию в `JobBase`:

  ```dart
  Job<R> then<R>(
    FutureOr<R> Function(JobContext ctx, T value) onValue, {
    JobObserver? observer,
  }) =>
      _ThenJob<T, R>(this, onValue, observer: observer);
  ```

- [x] Создать `_ThenJob<T, R>` с собственным `_CoreContext`, ожиданием
  исхода источника, регистрацией отмены в обе стороны и отложенным
  завершением отмены до старта. В `execute` вызывать обработчик
  с сохранённым значением. Отвергать усыновление через `adoptedBy`.
- [x] Добавить `ChainCancelReason` с `Cancelled cause` и меткой `chain`.
  У каждого звена собственный `Cancelled`, причины хранят связь.
- [x] Проверить каскад от каждого из трёх звеньев, отмену до старта,
  отложенный источник, позднюю подписку, отказ и задержку отмены,
  ожидание тела, детей и уборки, исходы и наблюдение ошибок. Использовать
  `Completer` для каждого рубежа и `FakeAsync.flushMicrotasks` без сна.
- [x] Выполнить тесты и анализатор ядра; устранить подтверждённые ошибки.

## Интеграция и документация

Файлы: `packages/solo/test/then_test.dart`, README обоих пакетов и
их переводы, CHANGELOG ядра и `solo`, архитектура, handoff и эта запись.

- [x] Проверить `then` на задаче очереди `solo`: отмена хвоста снимает
  ожидающий источник; `close` передаёт отмену через работающий источник;
  продолжение не удерживает слот очереди и получает контекст ядра.
- [x] Добавить пример цепочки с `ctx.join`, описать каскад и ожидание,
  отложенный источник, ошибки и границы очереди на обоих языках.
- [x] Обновить CHANGELOG, архитектуру и handoff. Выполнить проверки
  переводов, ширины и dartdoc ядра и `solo`.
- [x] Выполнить анализ и тесты ядра, `solo`, примера `solo`, Flutter-пакета;
  анализ примера Flutter. Проверить форматирование и `git diff --check`.
- [x] Выполнить независимое ревью; находки воспроизвести, исправить,
  повторить затронутые проверки. Записать результат в отчёт работы.
- [x] Проверить `git status`, закоммитить только изменения этой работы
  вместе с документами Conventional Commit на английском языке.
