> **Состояние на 2026-09-11:** связка выпуска `solo` 0.2.0 выполняется;
> итог дописывается отдельным docs-коммитом после публикации.
> **Что это:** выполнение разрешённой владельцем связки выпуска `solo`.
> **Связанные записи:**
> `2026-09-11[1]-solo-release-readiness-report.md`,
> `2026-09-10[21]-async-job-release-report.md`.

# Выпуск solo 0.2.0

Владелец разрешил публикацию отдельным запросом «публикуй», состоящим
только из просьбы опубликовать. Публикуется `solo` 0.2.0 из `main`
на основе `9038b8c`. Это первый выпуск пакета: имя на pub.dev свободно,
API отвечал 404.

## Подготовка

Подготовка описана в `2026-09-11[1]-solo-release-readiness-report.md`.
Кратко, всё против опубликованного `async_job` 0.2.0 с pub.dev:

- оверрайды ядра сняты у пакета и его примера;
- анализ чистый, 459 тестов, формат без изменений, dartdoc без замечаний;
- пример: анализ чистый, 9 тестов, запуск отработал;
- `dart pub publish --dry-run`: 0 предупреждений, архив 114 KB;
- ширина строк и пять пар переводов проверены;
- ядро после тега `async_job-v0.2.0` не менялось.

Перед связкой: `git fetch` выполнен, `main` не отстаёт от `origin/main`
и содержит 13 локальных коммитов поверх него. Тега `solo-v0.2.0` нет ни
локально, ни в origin. Пакет в дереве без незакоммиченных правок.
Чужая правка `.vscode/launch.json` в выпуск не включается.

Известное и принятое владельцем: README зовёт ставить `flutter_solo`
и ссылается на его страницу pub.dev, а пакет ещё не опубликован.
Порядок выпусков — `solo`, затем `flutter_solo`.

## Связка

Итог дописывается отдельным docs-коммитом после публикации.
