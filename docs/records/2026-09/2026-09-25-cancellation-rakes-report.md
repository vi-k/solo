> **Состояние на 2026-09-25:** вычитка сделана на ветке `doc-cancellation`,
> владелец читает страницу; вопросы и правки по ним — в разделе «По чтению
> владельца».
> **Что это:** отчёт о вычитке `packages/async_job/doc/cancellation.md` —
> четыре раздела с первыми попытками, 33 сторожа, четырнадцать мутаций.
> **Связанные записи:** `2026-09-24-outcomes-rakes-report.md`,
> `2026-09-19-cleanup-rakes-report.md`,
> `2026-09-11[16]-docs-structure-design.md`.

# Вычитка cancellation.md

## Что было

135 строк: блок кода с четырьмя контрольными точками, таблица к нему и три
раздела — перехват `Cancelled`, остановка самой операции, работа, которую
задача не ждёт. Пять блоков кода, первых попыток нет, и ни одной строки о том,
что печатает код. Во вступлении токен `stop` уходил в `migrate` и `markReady`,
но нигде не объявлялся. Раздел о перехвате советовал ветку
`on Cancelled { rethrow; }` и говорил, что обе формы работают.

## Что стало

353 строки, четырнадцать заголовков и двадцать блоков кода, из них девять —
строки, которые код печатает. Русская версия — 354 строки той же формы. Четыре
раздела, и каждый открывается версией, к которой ведут имена:

- **Остановка операции.** Первая попытка — `ctx.wait` вокруг миграции: отмена
  выходит из `wait` сразу, `discard` закрывает базу, и миграция пишет ещё два
  шага в закрытую базу. Вторая — `ctx.join` без токена: база закрывается после
  миграции, но миграция дописывает шаг, который уже никому не нужен. Ответ —
  токен через `onCancel` и `join`.
- **Шаг, который должен закончиться.** Первая попытка — `join` вокруг
  `markReady`: `join` принимает отмену сразу, `onCancel` отменяет токен,
  и `markReady` встаёт между версией схемы и флагом готовности. Ответ —
  `uncancellable`.
- **Перехват ошибок операции.** Первая попытка — прежний совет страницы, ветка
  `on Cancelled { rethrow; }`: с токеном из `join` выходит собственная ошибка
  миграции, а не `Cancelled`, и лог называет отмену неудачной миграцией.
  Ответ — `ctx.check()` первой строкой `catch`.
- **Работа, которую задача не ждёт.** Первая попытка — `unawaited`: ошибка
  уходит в зону мимо наблюдателя. Ответ — `ctx.unattended`.

Вступление объявляет токен и подключает его через `onCancel`; строка таблицы
о `join` называет и собственную ошибку действия. Обещание вступления — что
строки под кодом печатает код, что значат их пометки (`log:`, `onError:`,
`cancel`, `outcome:`, `zone:`) и чем являются первые попытки. Путеводитель
README в обеих версиях обещал на этой странице причины отмены, а они живут
в `outcomes.md`: в строке стало `unattended`.

## Зонды

Зонды лежали в `packages/async_job/.probe/`, каталог снят перед коммитом.

| Сценарий | Что вышло |
| --- | --- |
| `wait` вокруг миграции, отмена на 15 мс | база закрыта на 15 мс, шаги 2 и 3 пишутся в закрытую |
| `join` без токена | база закрыта на 30 мс, после последнего шага |
| токен через `onCancel` и `join` | миграция встаёт на 20 мс, `onError: DatabaseStopped`, база закрыта после |
| то же без наблюдателя | ошибка миграции не доходит ни до кого, зона пуста |
| `join` вокруг `markReady` с токеном | версия записана, флаг нет |
| `uncancellable` вокруг `markReady` | обе записи; `onCancel` и `whenCancelled` — на 20 мс, после секции |
| `uncancellable` без `await`, отмена внутри | `cancel()` возвращается с `Done(null)` |
| `cancellable: false` | после старта отказ, до старта `Cancelled(manual)` с `started: false` |
| ветка `on Cancelled`, токен | `log: migration failed: DatabaseStopped` |
| ветка `on Cancelled`, без токена | ветка работает, лог пуст |
| только `on Exception`, без токена | `migration failed: Cancelled(manual)` |
| `ctx.check()` в `catch`, токен | лог пуст, `onError` пуст, исход `Cancelled(manual)` |
| `ctx.check()` в `catch`, собственная ошибка без отмены | ошибка в логе, `markReady` прошёл, `Done(null)` |
| необязательный ребёнок отменён напрямую | `rethrow` — `Cancelled(handler: child null: Cancelled(manual))`, `check()` — `Done` |
| `unawaited` с наблюдателем и без | в зону в обоих случаях |
| `unattended` с наблюдателем и без | `onError`; без наблюдателя в зону |
| `async`-колбэк `onCancel` | в зону мимо наблюдателя |
| синхронный бросок в `onCancel` | `onError`, отмена идёт своим ходом |
| future снаружи, ждут внутри `unattended` | не возвращается, ошибка в зону |
| future внутри `unattended`, ждут снаружи | задача не кончается, ошибка в `onError` |
| члены контекста после принятия отмены | `check`, `wait`, `join`, `uncancellable`, `run`, `each`, `onCancel` бросают; `onDispose`, `onDiscard`, `disown`, `unattended` работают |

## Что нашлось

**Совет страницы не работал с токеном.** Страница сама подключает токен через
`onCancel` и тут же советует ветку `on Cancelled { rethrow; }`. Остановленная
токеном операция обычно бросает свою ошибку, а `join` бросает ошибку действия
как есть, отменена задача или нет: до `catch` доходит `DatabaseStopped`, ветка
для `Cancelled` её не видит, и `on Exception` пишет отмену в лог как сбой
миграции. Вопрос «отменена ли задача» задаётся задаче: `ctx.check()` первой
строкой `catch`. Без токена прежний совет работает, и страница так и говорит.

**Куда уходит ошибка действия после отмены.** Страница говорила, что `join`
бросает исходную ошибку и принятая отмена остаётся в силе, но не говорила, куда
девается сама ошибка. Зонд: исход — `Cancelled`, ошибка — наблюдателю, а без
наблюдателя никуда. С `ctx.check()` в `catch` строки `onError` нет вовсе: тело
сдаётся с отменой задачи, а не с ошибкой миграции.

**`ctx.check()` пропускает и чужую отмену.** Необязательный ребёнок, отменённый
напрямую, бросает свой `Cancelled` из `run`. Ветка, пробрасывающая любой
`Cancelled`, отменяет родителя с причиной `handler`; с `check()` родитель
доходит до `Done`. Страница об этом говорила отдельным абзацем, теперь это
следствие ответа.

**`unawaited` и `unattended` различимы только при наблюдателе.** Без
наблюдателя обе ошибки попадают в одну зону. Первая попытка четвёртого раздела
поэтому стоит при наблюдателе, и требование раздела его называет.

**Граница `unattended` держит в обе стороны.** Страница говорила «держите
future внутри: там отдельная зона ошибок» и не говорила, что будет иначе. Зонд:
future, созданный снаружи и ожидаемый внутри, при ошибке не возвращается,
и ошибка уходит в зону, где он создан; созданный внутри и ожидаемый снаружи
вешает того, кто ждёт, — тело, а с ним и задачу.

**Слова без предмета.** `stop` во вступлении не был объявлен. «A library built
on the core can enforce cancellation through its own rules» не называла
библиотеку: стало `solo`, и оба утверждения держат его сторожа —
`packages/solo/test/cancel_test.dart` («rules cancel a cancellable: false job
anyway») и `packages/solo/test/cancellable_test.dart` («rules cancel a job
inside an uncancellable step anyway»). Абзац «several steps inside
`ctx.uncancellable` can complete together» переписан правилом для любого
действия: контекст не ставит контрольных точек между шагами действия.

## Сторож

Новый файл `packages/async_job/test/cancellation_rakes_test.dart`, 33 теста.
Код страницы повторён в нём, как стоит там; три места, где линтер просит каскад
`ctx..check()..log(...)`, помечены `// ignore:` с объяснением. Последний тест
читает `doc/cancellation.md` и сверяет строки блоков `text` с константой
`quoted`, с которой сверяют себя остальные.

Мутаций четырнадцать. После каждой файлы восстановлены копией, хэши двух файлов
`lib/src/` и страницы совпали с исходными.

| Мутация | Кто краснеет |
| --- | --- |
| `wait` не кончает ожидание | 6 |
| `join` отдаёт значение после отмены | 4 |
| `join` бросает отмену вместо ошибки действия | 3 |
| `join` бросает, не дождавшись освобождения значения | 1 |
| позднее значение `wait` освобождается на месте | 1 |
| секция не удерживает отмену | 3 |
| удержанная отмена теряется при закрытии секции | 3 |
| `cancellable: false` не отказывает | 1 |
| `onCancel` регистрируется после принятия отмены | 1 |
| синхронный бросок `onCancel` проглатывается | 1 |
| ошибка после отметки не доходит до наблюдателя | 2 |
| `check` пропускает помеченную задачу | 10 |
| работа `unattended` отдаёт ошибку в зону | 3 |
| страница цитирует другую строку | 1 |

## Проверки

Ветка `doc-cancellation`, worktree `.claude/worktrees/doc-errors`.
`packages/async_job`: `dart format` без изменений, `dart analyze` чисто,
`dart test` — 526 (493 и 33 новых). `lib/` не менялся, `packages/solo` тоже.
Из корня зелены `reflow.py --check`, `check_line_width.py`,
`check_translations.py` (четырнадцать заголовков и двадцать блоков с обеих
сторон), `check_links.py` и `check_doc_shape.py`; сайт собран, 42 страницы.
Скрипты скилла `doc-reader`: `jargon.py` — ни одного слова кухни,
`bare_names.py --allow=future,Dart` — ни одного голого имени; `Dart` здесь имя
языка, как на других русских страницах. В `CHANGELOG` `async_job` добавлена
запись о странице.

## По чтению владельца

Чтение идёт с 2026-09-25.

1. «Нужно объяснить, что значит „помечается“» — о комментарии вступления над
   `ctx.uncancellable`: «Пока это идёт, задача не помечается и токен остаётся
   как был; бросит следующая точка». Страница называла одно событие двумя
   словами: абзац под таблицей — «принимает запрос», комментарий вступления,
   раздел о токене и обе части раздела о шаге, который должен закончиться, —
   «помечает задачу», и ни одно из двух не объясняла. Событие одно — момент,
   когда задача становится отменённой: в движке это `_pendingCancel`, снаружи —
   `isCancelled`. Осталось одно слово, «принимает», и абзац под таблицей теперь
   его объясняет: работающая задача принимает запрос прямо внутри `cancel()`,
   если его не придерживает секция `uncancellable` и задача не создана
   с `cancellable: false`; приняв его, она становится отменённой — вызываются
   её колбэки `onCancel`, отмена передаётся запущенным ею детям, и задача
   кончится `Cancelled`, что бы тело ни делало дальше. Комментарий вступления
   говорит, что происходит, без термина: отмена ждёт, пока секция не кончится,
   `onCancel` не вызывается, поэтому токен остаётся как был, а потом отмену
   бросает следующая точка. Раньше комментарий не связывал токен с `onCancel`,
   и «токен остаётся как был» читалось отдельным фактом. Остальные места
   переписаны на «принимает». Фраза раздела о токене «отмена помечает задачу»
   стала «отмена задачи до операции за ней не доходит». Фраза «`join` принимает
   отмену, как только она пришла» была неверна по существу: принимает задача,
   внутри `cancel()`, а `join` её только не придерживает, — так теперь
   и сказано. «Помечать» осталось на странице в двух других смыслах —
   `markReady` помечает базу готовой, `unawaited` помечает future — и к задаче
   больше не относится.

   Что колбэки `onCancel` вызываются внутри `cancel()`, уже держал тест
   `onCancel runs inside cancel(), before the body goes on`. Остальное держит
   новый `accepting reaches the children at once and decides the outcome`:
   когда `cancel()` вернулся, колбэк `onCancel` ребёнка уже вызван
   и `isCancelled` отвечает `true`, а тело, которое после отмены без единой
   контрольной точки возвращает `42`, кончается `Cancelled`. Две мутации
   `lib/src/job_base.dart`, обе пойманы: без передачи отмены детям при
   принятии — два красных, новый
   и `a held cancellation reaches no child until the section ends`; исход
   по тому, что отдало тело, а не по принятой отмене, — шесть красных, новый
   среди них. Во второй мутации обе строки `_pendingCancel ?? outcome`,
   в `finish` и на пути тела: каждая дублирует другую, и по одной файл сторожа
   их не ловит. Хэш `job_base.dart` после отката совпал.

   Проверки: `packages/async_job` — `dart format` без изменений, `dart analyze`
   чисто, `dart test` 527, сторож — 34 теста. Из корня зелены
   `reflow.py --check`, `check_line_width.py`, `check_translations.py`,
   `check_links.py` и `check_doc_shape.py`, сайт собран, 42 страницы;
   `jargon.py` и `bare_names.py --allow=future,Dart` — ноль.

2. «Посмотри и в других документах». То же слово для того же события нашлось
   ещё на четырёх страницах, в обеих версиях каждой, и везде стало «принимает».

   - `packages/async_job/doc/observing.md`: провал, брошенный до «пометки»
     и после неё, теперь брошен до того, как задача приняла отмену, и после.
     Своего определения на странице нет, поэтому «приняла отмену» — ссылка
     на `cancellation.md`, где определение есть.
   - `packages/async_job/doc/children.md`: каскад, у которого кончился стек,
     говорит «остановись» каждой задаче, которую отменил, а не каждой
     помеченной; `ctx.onDispose` регистрируется и на задаче, которая уже
     приняла отмену. В русской версии там же «задании» стало «задаче»; ещё пять
     «заданий» на этой странице остались — это другой разговор, о двух словах
     для `Job`, и в эту правку он не входил.
   - `packages/solo/doc/cancellation.md`: то же двоесловие, что и на странице
     `async_job`. Таблица и абзацы о `join` говорили «принимает», вступление,
     абзац об `onCancel` и оба абзаца о шаге платежа — «помечает». Под таблицей
     появился абзац с определением, сказанным для контроллера: работающая
     задача принимает отмену, как только её просят остановиться, если запрос
     не придерживает секция `uncancellable` и задача не отклоняет его через
     `cancellable: false`, — оба случая страница разбирает ниже. Комментарий
     вступления «Nothing marks the job at all while this runs» был ещё
     и неверен: правила состояния отменяют задачу и внутри секции — это держит
     тест `rules cancel a job inside an uncancellable step anyway`
     в `packages/solo/test/cancellable_test.dart`, и страница сама говорит
     об этом ниже. Теперь там «An ordinary cancellation waits for this to
     end» — тем же словом «обычная», что и в строке таблицы.
   - `packages/solo/doc/errors.md`: поле `cancellation` у `SoloPending` —
     отмена, которую задача приняла, а `cancellationPending` говорит о принятой
     или придержанной. В русской версии правка заодно развела два предмета:
     «помеченная отмена» стояла на одной странице с разделом «Отметка отмены»,
     где наблюдатель отмечает время отмены.

   Остальные «mark» и «помечает» в документах — о другом: исход, помеченный
   наблюдённым или обработанным (`job.ignore()`, `done`, `value`), аннотации
   `@protected` и `@visibleForTesting`, отметка о прочтении в примере чата,
   ключ, которым помечены задачи накопителя, `markReady` и `unawaited` на самой
   `cancellation.md`. Их правка не трогала. Dartdoc публичного API по-прежнему
   говорит «marked»: `isCancelled`, `pendingCancel`, `ctx.onCancel`,
   `SoloPending.cancellation` и другие, восемнадцать мест в `async_job`
   и `solo` вне приватных членов. Это код пакетов, а не страницы, и в правку он
   не входил.

   Сторож — группа `accepting a cancellation`
   в `packages/solo/test/cancellation_rakes_test.dart`, один тест на новый
   абзац `solo`. У задачи есть свой ребёнок и нет контрольной точки после шага.
   Когда `cancel()` вернулся, колбэки `onCancel` ребёнка и самой задачи уже
   вызваны, а тело, вернувшее `42`, кончается `Cancelled`. Три мутации
   `packages/async_job/lib/src/job_base.dart` через оверрайд `solo`, все
   пойманы: без передачи отмены детям — один красный, новый; исход по тому, что
   отдало тело, — четыре, новый среди них; колбэки `onCancel` в микрозадаче
   после возврата `cancel()` — один, новый. Хэш после отката совпал.
   На остальных страницах сменилось слово, а не утверждение, и сторожа у этих
   правок нет.

   Проверки: `packages/solo` — `dart format` без изменений, `dart analyze`
   чисто, `dart test` 791; `packages/async_job` — 527. Из корня зелены
   `reflow.py --check`, `check_line_width.py`, `check_translations.py`,
   `check_links.py` и `check_doc_shape.py`, сайт собран, 42 страницы.
   `bare_names.py` — ноль; `jargon.py` — два подозреваемых, оба в строках,
   которых правка не трогала: `measured` в `children.md` и «прогонами» в её
   русской версии.

3. «Колбэк освобождения для значения дожидается первым. Не понял фразу» —
   строка `join` в таблице. У фразы две беды. «Колбэк освобождения» нигде
   на странице не назван: это `dispose` или `discard`, переданный самому
   вызову, а тот же безымянный колбэк стоял ещё в комментарии вступления,
   в строке `wait` и в абзаце о позднем результате `wait`. «Дожидается первым»
   читалось так, будто ждёт колбэк, и не говорило, первым раньше чего. Теперь
   строка `join` говорит: если вызову передан `dispose` или `discard`, `join`
   сперва отдаёт туда значение, дожидается конца освобождения и только потом
   бросает. Строка `wait` и абзац о позднем результате тоже называют `dispose`
   и `discard` по имени. Комментарий вступления говорит просто, что значение
   отбрасывается: у `ctx.wait(database.readAll)` во вступлении ни того,
   ни другого нет. Больше «cleanup callback» и «колбэк освобождения» нет
   ни на одной странице.

   Сторож: страница теперь называет оба параметра, а тесты держали по одному —
   `join` только с `dispose`, поздний результат `wait` только с `discard`. Все
   три теста — `join awaits the … of its value before it throws` и два
   о позднем значении `wait` — идут теперь с каждым из двух параметров,
   сторож — 37 тестов. Четыре мутации `lib/src/job_context.dart`, все пойманы:
   `join` бросает, не дождавшись освобождения, — два красных, по одному
   на параметр; `join` забывает `discard`, когда бросает, — один; поздний
   результат `wait` забывает `discard` — два; забывает `dispose` — два. Хэш
   после отката совпал.

   Проверки: `packages/async_job` — `dart format` без изменений, `dart analyze`
   чисто, `dart test` 530. Из корня все пять проверок зелёные, сайт собран, 42
   страницы; `jargon.py` и `bare_names.py` — ноль.

4. «„Встать“ обычно говорят про зависание. Если речь об остановке,
   то „остановиться“» — о фразе «Когда пользователь отменяет, миграция должна
   встать». Слово стояло в значении «остановиться» в шести местах русской
   страницы: в комментарии вступления, в требовании раздела об остановке,
   в двух абзацах о токене, в первой попытке шага, который должен закончиться,
   и в требовании раздела о перехвате. Везде стало «остановиться». В других
   русских документах то же значение нашлось дважды,
   в `packages/flutter_solo/README.ru.md`: комментарий «возвращается, когда
   задача действительно встала» и «тело встанет на ближайшем обращении
   к контексту» — там тоже «остановиться». Остальные «встаёт» в документах —
   о месте, а не об остановке: `Job` встаёт в очередь, на место отброшенной,
   объектив встаёт на 2. Их правка не трогала.

   Английский текст не менялся: там везде «stop». Сторожа у правки нет —
   сменилось слово, а не утверждение. Из корня все пять проверок зелёные, сайт
   собран, 42 страницы; `jargon.py` — ноль; `bare_names.py` на странице — ноль,
   в `README.ru.md` `flutter_solo` — восемь, все прежние (Flutter, bloc,
   listenable), а README ещё ждёт вычитки по карте.

5. «„Отмена задачи до операции за ней не доходит“ — сложно сказано». Фраза
   открывала раздел о токене и появилась по первому вопросу чтения, когда
   из неё убирали «помечает». Она говорила об операции вообще: «операция
   за ней», «её собственный механизм», — а в примере операция одна, миграция,
   и механизм один, токен. Теперь там подлежащее из примера: «Миграция не знает
   об отмене задачи: она слушает только свой токен. Отменить токен вместе
   с задачей можно через `onCancel`». То же в английской версии. Сторожа
   у правки нет — утверждение прежнее, а что миграция читает только токен,
   видно в коде раздела; сторож страницы зелёный, 37 тестов. Из корня все пять
   проверок зелёные, сайт собран, 42 страницы; `jargon.py` и `bare_names.py` —
   ноль.

6. «А почему в примере „onError: DatabaseStopped“? Откуда ошибка?» — о выводе
   раздела «Токен через `onCancel`». Постановка раздела говорила, что миграция
   читает токен перед каждым шагом, но не говорила, как она останавливается,
   и первый абзац под выводом снова говорил просто «останавливается». Что
   остановка — это бросок `DatabaseStopped`, читатель узнавал только во втором
   абзаце, уже после того, как строка `onError:` его озадачила. В разделе
   о шаге, который должен закончиться, та же строка стояла в выводе первой
   попытки, и не объяснял её ни один абзац. Теперь постановка раздела
   об остановке говорит, что миграция, найдя токен отменённым, останавливается,
   бросая `DatabaseStopped`. Первый абзац под выводом — что она бросает его
   перед третьим шагом. Второй открывается ответом: «Строка `onError:` — это
   тот самый `DatabaseStopped`»: `join` бросает ошибку своего действия как
   есть, тело её не ловит, задача всё равно кончается `Cancelled`, и ошибка
   доходит только до наблюдателя. Постановка раздела о шаге говорит, что
   `markReady` останавливается так же, а абзац под первой попыткой — что
   `DatabaseStopped` доходит до `onError`, как в разделе выше. Заодно
   в английской постановке «15 ms in» больше не рвётся между строками: фраза
   теперь кончается на «during the second step, 15 ms in».

   Сторожа у правки нет: всё, что абзацы теперь называют, — бросок
   `DatabaseStopped`, строка `onError:` в обоих выводах, исход `Cancelled` —
   уже держат тесты страницы и сверка её цитат; сторож зелёный, 37 тестов.
   Из корня все пять проверок зелёные, сайт собран, 42 страницы; `jargon.py`
   и `bare_names.py` — ноль.

7. «Переформулируй, сложновато» — абзац под первой попыткой раздела о шаге,
   который должен закончиться. Он складывался из правок по трём прежним
   вопросам: «принимает» вместо «помечается», «останавливается» вместо
   «встаёт», строка про `onError`. В итоге одна фраза с двоеточием несла четыре
   события подряд, а их порядок не совпадал с порядком строк вывода. Теперь
   абзац идёт короткими фразами в порядке вывода: `join` ждёт `markReady`
   до конца, и всё равно `markReady` останавливается на полпути; отмена
   приходит, пока пишется версия; `join` её не придерживает, поэтому задача
   принимает её сразу, и `onCancel` отменяет токен; `markReady` дописывает
   версию, видит отменённый токен и бросает `DatabaseStopped`, так и не записав
   флаг; база остаётся с версией, но без флага, а ошибка уходит в `onError`.
   То же в английской версии.

   Сторожа у правки нет: порядок, который абзац теперь пересказывает, — ровно
   вывод первой попытки, и его держит тест
   `join lets the token stop the step halfway` со сверкой цитаты; сторож
   зелёный, 37 тестов. Из корня все пять проверок зелёные, сайт собран, 42
   страницы; `jargon.py` и `bare_names.py` — ноль.

8. «Не понятно, зачем это: „и всё равно `markReady` останавливается
   на полпути“». Оборот отвечал на ожидание, которое вслух нигде не сказано:
   будто `join` раз ждёт действие до конца, то и доведёт шаг до конца. Читатель
   этого ожидания не слышал, и «всё равно» повисало. Теперь абзац открывается
   тем, что вышло, по требованию раздела: «Шаг не доходит до конца: база
   остаётся с версией, но без флага готовности». Дальше — почему, в порядке
   вывода: отмена приходит, пока пишется версия, и задача принимает её сразу,
   потому что `join` ждёт действие, но отмену не придерживает; `onCancel`
   отменяет токен, `markReady` дописывает версию, видит отменённый токен
   и бросает `DatabaseStopped`, так и не записав флаг; ошибка уходит
   в `onError`. То же в английской версии.

   Сторожа у правки нет, утверждения прежние; сторож страницы зелёный, 37
   тестов. Из корня все пять проверок зелёные, сайт собран, 42 страницы;
   `jargon.py` и `bare_names.py` — ноль.

9. «В целом непонятно, зачем такая сложность: мы передаём `CancelToken`, чтобы
   отменить, но с помощью `uncancellable` не отменяем. Что нам мешает просто
   не использовать `onCancel`?» Ничего. Пробный тест показал, что `join` без
   токена доводит `markReady` до конца: обе записи сделаны, исход `Cancelled`.
   Первая попытка раздела держалась на том, что страница сама отдавала шагу
   токен миграции, а шаг, который нельзя останавливать, токена не берёт.
   Натянутая ловушка — то, от чего предостерегает скилл `doc-reader`. Та же
   проба показала, где `uncancellable` действительно нужен: шаг с дочерней
   задачей внутри `join` теряет флаг, а внутри `uncancellable` делает обе
   записи.

   Владелец выбрал перестройку из трёх предложенных вариантов: два `join` →
   один `join` → `uncancellable`. Раздел теперь такой. База пишет готовность
   двумя вызовами, `writeVersion` и `writeReadyFlag`. Первая попытка — два
   `join`, по одному на запись: каждый `join` — своя контрольная точка, первый
   дожидается версии и бросает отмену, и флаг не пишется. Ответ, под заголовком
   «Один `join` на шаг», — один `join` вокруг обеих записей: внутри действия
   контрольных точек нет, обе записи сделаны. Этого хватает, пока шаг — обычный
   код: не берёт токен, который отменяет `onCancel`, не запускает дочерних
   задач и не проходит контрольных точек контекста. Под «Придержать отмену» —
   шаг, который сам пользуется задачей: флаг пишет дочерняя задача
   `database.readyFlag()`. С `join` флаг снова теряется: задача принимает
   отмену сразу, и `ctx.run` бросает её вместо запуска ребёнка.
   С `uncancellable` пишутся обе записи. Код вступления показывает
   `uncancellable` на том же шаге с ребёнком, а раздел о перехвате кончается
   `join` вокруг обеих записей: «`join` следующего шага бросает, не начав своё
   действие». `markReady`, `marking stopped` и токен в шаге ушли со страницы.
   Страница — 406 строк в оригинале и 404 в переводе, пятнадцать заголовков
   и двадцать четыре блока.

   Той же конструкцией — `ctx.uncancellable(() => database.markReady(stop))`
   и объяснением, что шаг должен закончиться, не получив сигнала токена, —
   открываются `packages/async_job/README.md`,
   `packages/async_job/README.ru.md`
   и `packages/async_job/example/example.dart`. Их правка не трогала: это
   отдельный вопрос владельцу.

   Сторож переписан под раздел: у `Database` вместо `markReady(stop)` три
   члена — `writeVersion`, `writeReadyFlag` и `readyFlag()`, которая отдаёт
   запись флага отдельной задачей. Шесть тестов раздела: два `join` теряют
   флаг, один `join` делает обе записи, один `join` не спасает ни от токена,
   ни от контрольной точки внутри, `join` вокруг шага с ребёнком теряет флаг
   и ребёнок не стартует (дважды — с ребёнком в тесте и с `readyFlag()`
   страницы), `uncancellable` делает обе записи вместе с ребёнком. Цитат стало
   одиннадцать, сторож — 41 тест, набор `async_job` — 534. Прежние четырнадцать
   мутаций прогнаны заново и пойманы все, вместе с повтором «`join` забывает
   `discard`, когда бросает» — один красный. Новых три: `ctx.run` запускает
   ребёнка после принятия отмены — три красных; `ctx.check` пропускает принятую
   отмену и колбэки `onCancel` не вызываются — новый тест краснеет на обеих.
   У `ctx.run` проверка принятой отмены стоит дважды — до вопроса правилам
   домена и после него, — и мутация одной из двух не краснит ничего: вторая её
   перекрывает, поэтому снималась пара. Хэши после откатов совпали.

   Проверки: `packages/async_job` — `dart format` без изменений, `dart analyze`
   чисто, `dart test` 534. Из корня все пять проверок зелёные, сайт собран, 42
   страницы; `jargon.py` и `bare_names.py` — ноль.

10. «В этой фразе есть ощущение, что у пакета `solo` есть какие-то свои
    правила, по которым он может отменить» — о «`solo`, построенный на этом
    ядре, отменяет задачу и по своим правилам» в абзаце о `cancellable: false`.
    Правила не пакета, а самой задачи: в `solo` задача объявляет, в каких
    состояниях контроллера она работает, — типом состояния в `run<W, T>` или
    условием `keepWhile`. Когда состояние перестаёт подходить, контроллер
    отменяет её отменой, которую нельзя отклонить:
    в `packages/solo/lib/src/solo.dart` это `rejectable: false` с комментарием
    «A rule of the job's own: it may not refuse this one», в контексте задачи —
    `cancelOwnJob`. Других неотклоняемых отмен в `solo` нет. Фраза теперь
    говорит это прямо: `solo` отменяет задачу ещё и тогда, когда состояние
    контроллера перестаёт ей подходить, — задача там сама объявляет, в каких
    состояниях она работает, — и такую отмену не удерживают ни секция,
    ни `cancellable: false`. Имена `W` и `keepWhile` на страницу `async_job`
    не вынесены: они из API `solo`, и читателю ядра хватает того, что условие
    объявляет задача.

    Сторож — прежние тесты `solo`:
    `rules cancel a cancellable: false job anyway`
    в `packages/solo/test/cancel_test.dart`
    и `rules cancel a job inside an uncancellable step anyway`
    в `packages/solo/test/cancellable_test.dart`, оба файла зелёные, 24 теста.
    На страницах `solo` «its rules» тоже встречается, но там это правила задачи
    в пакете, где они объявлены, и правка их не трогала. Из корня все пять
    проверок зелёные, сайт собран, 42 страницы; сторож страницы — 41 тест;
    `jargon.py` и `bare_names.py` — ноль.

11. «Но код между `catch` и этой точкой выполняется на отменённой задаче — код
    внутри `catch`?» Да: в примере между `catch` и `join` следующего шага нет
    ничего, кроме самой ветки `on Exception`, и выполняется именно она — пишет
    отмену в лог как сбой. «Между `catch` и этой точкой» можно было прочесть
    и как код после `catch`. Теперь абзац называет ветку: ветка для `Cancelled`
    пропускает `DatabaseStopped`, его ловит `on Exception`, и код этой ветки
    выполняется на уже отменённой задаче; задача всё равно кончается
    `Cancelled`, потому что `join` следующего шага бросает, не начав своё
    действие. В абзаце теперь две ветки, поэтому и дальше они названы: «без
    токена ветка для `Cancelled` работает», «без этой ветки `on Exception`
    ловит и этот `Cancelled`». То же в английской версии.

    Сторожа у правки нет: строка лога и исход — цитата, которую держит тест
    `a clause for Cancelled logs the stopped migration as a failure`; сторож
    страницы зелёный, 41 тест. Из корня все пять проверок зелёные, сайт собран,
    42 страницы; `jargon.py` и `bare_names.py` — ноль.

12. Два вопроса к абзацу под «Спросить задачу».

    Первый — о фразе «строка `onError: DatabaseStopped` остановленной миграции
    выше тоже пропала: тело сдаётся с отменой задачи, а не с ошибкой миграции»:
    «ошибка пропадает, потому что мы её ловим, а здесь как будто из-за того,
    что мы отменили». Причин две, и фраза называла только вторую:
    `DatabaseStopped` ловит `on Exception`, поэтому из тела он не выходит,
    а тело кончается отменой задачи из `ctx.check()`, и отмена в `onError`
    не попадает. Теперь сказаны обе, в этом порядке, и названо, с чем
    сравнение — с разделом о токене, где ветки нет.

    Второй — «Тот же `ctx.check()` работает и в ветке `on Object`»: «у нас
    ветка `on Exception`, а не `on Object`; либо это ошибка, либо непонятно».
    Не ошибка: это совет для ветки, которая ловит всё, включая `Error`, —
    `on Exception` его не ловит, и кто хочет ловить всё, пишет
    `on Object catch (error)`. Фраза этого не говорила. Теперь: «Ветка, которая
    ловит всё, включая `Error`, — `on Object catch (error)` — начинается
    с того же `ctx.check()`». То же в английской версии.

    Сторожа у правки нет: отсутствие строки `onError:` держит цитата вывода
    и тест `check in the catch lets the cancellation through`, ветку
    `on Object` — тест `check in an on Object catch does the same`; сторож
    страницы зелёный, 41 тест. Из корня все пять проверок зелёные, сайт собран,
    42 страницы; `jargon.py` и `bare_names.py` — ноль.

13. «„Ветка, которая ловит всё, включая `Error`, — `on Object catch (error)` —
    начинается с того же `ctx.check()`“ — должна начинаться? У нас нет такой
    ветки, и ничего не начинается». Фраза, написанная по двенадцатому вопросу,
    говорила о ветке изъявительным наклонением, как о факте примера,
    а в примере ветки `on Object` нет — это совет на случай, когда её напишут.
    Теперь совет сказан как совет: «Если ветка должна ловить всё, включая
    `Error`, как `on Object catch (error)`, первой строкой в ней нужен тот же
    `ctx.check()`». То же в английской версии.

    Сторож прежний — тест `check in an on Object catch does the same`; сторож
    страницы зелёный, 41 тест. Из корня все пять проверок зелёные, сайт собран,
    42 страницы; `jargon.py` и `bare_names.py` — ноль.
