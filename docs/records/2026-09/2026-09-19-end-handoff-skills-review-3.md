> **Состояние на 2026-09-19:** круг пройден, все 52 пункта внесены
> в `~/.claude/skills` коммитом `92ae5de`.
> **Что это:** пятый круг независимого ревью личных скиллов `end`
> и `handoff`: 57 находок трёх ревьюеров, сведённых в 52 пункта, с вердиктом
> по каждому.
> **Связанные записи:** `2026-09-19-end-handoff-skills-review-2.md` — четвёртый
> круг, чьи формулировки этот проверял; `2026-09-19-clear-and-live-work-report.md` —
> разбор про `/clear`, две цитаты которого этот круг опроверг.

# Пятый круг ревью скиллов `end` и `handoff`

Четвёртый круг внёс 37 находок за один заход, и его собственные формулировки —
включая новый рецепт отделения своего от чужого — прочитал только тот, кто их
писал. Владелец согласился на пятый круг 2026-09-19.

Три ревьюера на Opus, каждому свой клон репозитория скиллов и своя песочница:

- **A — исполнитель.** Собрал подставной проект с чужой незакоммиченной правкой
  в файле состояния, красным pre-commit хуком и недоделанной работой и прошёл
  `end` от шага 1 до шага 10 буквально. 19 находок.
- **B — проверяющий утверждений.** Каждое утверждение об окружении и каждая
  команда обоих скиллов: верно, неверно или проверить нечем. 11 находок, около
  шестидесяти утверждений признаны верными.
- **C — ревизор правки.** Коммит `818b3c5` как рецензируемая правка плюс
  `check_move.py` и его сторожа как код. 27 находок.

Всего 57 находок, пять пар совпали, различных пунктов 52. Приняты все 52. Ниже
они сгруппированы по тому, что чинят.

## Рецепт отделения своего от чужого

Четырнадцать находок из пятидесяти двух — об одном абзаце и одном блоке команд,
заведённых прошлым кругом. Вместе они не чинились: рецепт заменён целиком.

**1 (A1, B6). Редирект обнуляет файл раньше, чем отработает `git show`.**
Строка `git show HEAD:<файл> > <файл>` создаёт и обрезает цель до запуска
команды. Если файла в `HEAD` нет — а это ровно тот случай, когда сессия сама
его завела или он переехал, как `worklog.md` в kokjai, — `git show` падает
с кодом 128, а в дереве остаётся ноль байт, и `git status` показывает только
`?? <файл>`.

**Вердикт (2026-09-19):** принята и проверена своим прогоном: 131 байт стали
нулём, код 128. Рецепт, написанный ради спасения чужой работы, стирал и её,
и свою. Строка убрана вместе со всем откатом.

**2 (A2). Коммит путями не работает на файле, которого нет в индексе.**
`git commit -- <путь>` на неотслеживаемом файле падает
с `pathspec … did not match any file(s) known to git`. Шаг 9 про это знает
и держит строку `git add`, рецепт шага 1 — нет, хотя шаг 9 отсылает к нему
«за любым файлом».

**Вердикт (2026-09-19):** принята и проверена: `commit rc=1`. В новом рецепте
стоит `git add '<файл состояния>'` с оговоркой «только если его нет в `HEAD`».

**3 (A3). Оборвавшийся рецепт оставляет чужую правку только внутри `.git`.**
Красный хук роняет коммит посередине; дерево выглядит обычным (` M <файл>`),
чужих строк в нём нет, и сказать о них некому. Для патчей шага 3 правило «раз
его не видно, блок называет патч сам» записано, для копии шага 1 — нет.

**Вердикт (2026-09-19):** принята. В новом рецепте есть строка на обрыв: коммит
не прошёл — верни чужие строки прежде, чем заканчивать ход; не смог — назови
путь копии в блоке и в сообщении владельцу, как называют снятый патч.

**4 (A4, C1). Имя копии фиксированное, а `.git/wip` общий для всех копий
дерева.** `state-tree.md` — одно имя на все сессии. Вторая сессия накрывает
копию первой, и первая возвращает в своё дерево чужой файл целиком; итоговая
проверка `git diff HEAD` этого не видит. Соседние места скилла этой грабли
избегают: патчи названы `<дата>-<тема>.patch`, а выгрузка —
`left-<имя копии>.txt`.

**Вердикт (2026-09-19):** принята. Копия названа `<дата>-<тема>-state.md`
и удаляется в конце.

**5 (A5). Опасное окно открывается не коммитом, а откатом.** Проза
предупреждала про окно «между коммитом и возвратом копии», а файл лежал
откатанным к `HEAD` начиная с `git show` — через все ходы модели на повторение
Edit'ов. Соседняя сессия, выполняющая в это окно свой шаг 6, видит пустой
`git diff HEAD`, заключает «чужого нет» и коммитит поверх.

**Вердикт (2026-09-19):** принята, и это самая дорогая из четырнадцати: сессия,
сделавшая всё правильно, теряла свой блок и не могла этого заметить. В новом
рецепте отката нет вовсе, и окно сжато до двух Edit'ов с коммитом между ними;
названо прямо, вместе с запретом делать внутри него что-либо ещё.

**6 (A6, B5). У рецепта нет `cd` в корень.** `git show HEAD:<путь>` считает
путь от корня репозитория, а `cp`, `cmp` и редирект — от текущего каталога.
Из подкаталога, где такой путь тоже есть, содержимое подменяется молча
с нулевым кодом. Соседний блок шага 3 эту строку имеет и объясняет, зачем она.

**Вердикт (2026-09-19):** принята. `cd "$(git rev-parse --show-toplevel)"`
стоит первой строкой после `set -e`.

**7 (A7). «Не сошлось — стоп» ничего не останавливает.** Единственная защита
была комментарием, а блок гонится одним вызовом, который на ненулевом коде
не встаёт. Построчно его прогнать нельзя: `$W` живёт только внутри вызова.

**Вердикт (2026-09-19):** принята. В блоке `set -e` первой строкой, а `cp`
и `cmp` связаны через `&&`.

**8 (A8). Внутри блока `sh` стояла строка, которая шеллом не исполняется.**
«Перечитай файл и повтори свои Edit'ы» — действие модели, неотличимое
от комментария. Прогон одним вызовом доходил до коммита с файлом, равным
`HEAD`, и падал на `no changes added to commit`.

**Вердикт (2026-09-19):** принята. Рецепт разбит на три блока с прозой между
ними, и действия модели стоят в прозе, а не в блоке.

**9 (A9). Коммит в рецепте назван одним файлом состояния.** Шаг 9 велит
коммитить файл состояния вместе с работой, к которой он относится, а рецепт
клал его отдельным коммитом.

**Вердикт (2026-09-19):** принята. В строке коммита теперь два плейсхолдера,
и шаг 9 добавляет «тем же коммитом и теми же путями, а не отдельным».

**10 (A10). Коммит путями молча снимает `git add` соседа.** После
`git commit -- <путь>` запись индекса для этого пути совпадает с новым `HEAD`:
заиндексированная правка соседа из индекса пропадает. Текст возвращается
в дерево, а заиндексированность — нет, и обычный коммит соседа перестаёт
проходить.

**Вердикт (2026-09-19):** принята и проверена своим прогоном: `diff --cached`
после коммита пуст. В рецепте появилась строка
`git diff --cached --stat -- '<файл состояния>'` до коммита и правило назвать
это в блоке и в сообщении владельцу.

**11 (A17). Плейсхолдеры в рецепте стояли без кавычек.** В шаге 9 такой же
плейсхолдер в кавычках; на пути с пробелом рецепт рвётся и оставляет в дереве
посторонний файл.

**Вердикт (2026-09-19):** принята, мелочь. Все вхождения в одиночных кавычках.

**12 (A19). Рецепт с коммитом лежал в шаге «Факты — командами».** Блок пишется
на шаге 6, коммит идёт на шаге 9, а рецепт стоял на шаге 1, который сам же
говорит «уборку и коммит своего не начинай».

**Вердикт (2026-09-19):** принята. Рецепт остался там, где стоит объясняющее
его правило, но открывается строкой «Сам порядок — на шаге 9, когда блок уже
написан».

**13 (C2, B7). «В дереве после коммита их не остаётся» — неверно.** Оба скилла
утверждали, что при коммите путями чужая правка исчезает из дерева. Файл
на диске не меняется вовсе: пропадает не текст, а его незакоммиченность.

**Вердикт (2026-09-19):** принята и проверена своим прогоном: и в коммите,
и в дереве лежат обе строки, `git status` чист. Это поправка к собственному
выводу четвёртого круга: вывод «так писать нельзя» был верен, объяснение — нет.
Формулировка заменена в обоих скиллах и в строке таблицы.

**14 (C3). Возврат копии откатывает свой же закоммиченный текст.** В копии
лежала и своя правка — та, что была до коммита. Если повторённые по `HEAD`
Edit'ы отличались хоть чем-то, `cp` возвращал в дерево откат собственного
коммита, а следующая строка объявляла это «чужой правкой».

**Вердикт (2026-09-19):** принята. В новом рецепте копия — только сеть
безопасности, а дерево чинится Edit'ами; итоговая сверка стала строже:
`diff "$C" '<файл состояния>'` должен быть пуст, то есть рецепт не меняет
дерево вовсе.

## Команды и их вывод

**15 (B2). Список команд, переписываемых хуком rtk, неполон.** Кроме шести
названных хук переписывает `cat`, `find`, `tail`, `wc`, `du`, `diff`, `rg`
и `tree`. Опасны `find` — 5 строк из 243 по `docs` solo — и `diff`, который
печатает оба файла целиком, не говоря, что изменилось. `find` — как раз та
команда, которой ищут свои каталоги перед удалением.

**Вердикт (2026-09-19):** принята. Списки в обоих скиллах дополнены, добавлено
проверенное ревьюером «конвейер и несколько команд в строке хук не трогает».
Заодно исправлена собственная новая строка рецепта: сверка была написана голым
`diff`.

**16 (B4). `origin/main..<ветка>` не отвечает на вопрос «чего нет
на `origin`».** У отправленной ветки копии эта команда печатает её целиком,
и в блок уедет «не отправлено» про отправленное.

**Вердикт (2026-09-19):** принята. Заменено
на `log --oneline <ветка> --not --remotes=origin`, проза объясняет разницу.

**17 (A13). Чужие коммиты предлагалось искать по `%an`.** Параллельные сессии —
это сессии того же владельца с тем же `user.name`: в solo все тридцать
последних коммитов принадлежат `nashol`. Сам скилл двумя экранами выше говорит,
что авторства git не знает.

**Вердикт (2026-09-19):** принята. `%an` убран из формата, признак назван
работающий: свои коммиты знает та сессия, которая их положила.

**18 (A14). Ветка без upstream роняет и счёт, и push.** `@{u}..HEAD` падает
с «no upstream configured», `git push` — с «has no upstream branch». Владелец
уже ответил «Да, отправить», а шаг 9 отправить не может.

**Вердикт (2026-09-19):** принята. Названы оба случая, отправка такой ветки —
`git push -u origin '<ветка>'`, и отдельно сказано, что это не повод писать
«push не удался».

**19 (A15). Репозиторий без remote.** `git fetch` проходит с нулём, а падают
обе формулы счёта; ветка «упал — счёт по локальной ссылке» не срабатывает.

**Вердикт (2026-09-19):** принята, мелочь. Добавлено: нет remote — вопроса
об отправке не задают, а блок пишет, что `origin` у репозитория нет.

**20 (A16). Отсутствующий файл состояния неотличим от файла без правок.**
`git diff HEAD -- <файл>` в обоих случаях молчит с нулём. Случай не выдуманный:
раздел «Старшинство» сам рассказывает про переезд файла в kokjai.

**Вердикт (2026-09-19):** принята, мелочь. Различает
`git log --oneline -1 -- '<файл состояния>'`, и пустой ответ — вопрос
владельцу, а не повод завести свой файл.

**21 (A18). `git add '<свои новые файлы>'` — множественное число внутри одной
пары кавычек.** Буквальная подстановка двух путей даёт один путь с пробелом
и падение.

**Вердикт (2026-09-19):** принята, мелочь. Плейсхолдеры разведены по своим
кавычкам, добавлена фраза о том, почему.

**22 (B8). `rtk gh run list` описан неточно.** Он оставляет имя workflow, слово
`ok` и id, а убирает ветку, сообщение и время. Имя workflow — как раз то,
по чему отличают `gate` от `site`.

**Вердикт (2026-09-19):** принята, мелочь. Описание исправлено.

**23 (C12). Обоснование `git diff HEAD` ссылалось на шаг 9.** Оба скилла
объясняли требование тем, что «сосед по шагу 9 делает `git add`», а шаг 9 велит
индексировать только новые файлы. Правило верное, причина — нет.

**Вердикт (2026-09-19):** принята. Причина названа настоящая: заиндексировать
файл мог владелец руками, сессия не по этим скиллам или оборвавшийся чужой
коммит, и запись в индексе переживает `/clear`.

## Живая работа, `/clear` и расписания

**24 (B1). Утверждение о расписаниях неверно, и цитата под ним не находится
нигде.** Скилл говорил, что `CronCreate`, `ScheduleWakeup` и `/loop` привязаны
к беседе и что останавливать их не нужно, подпирая это фразой «Starting a fresh
conversation clears all session-scoped tasks». В бандле 2.1.278 этой фразы нет,
как и её частей. Что бандл говорит на самом деле: `durable: true` пишет задачу
в `.claude/scheduled_tasks.json` и «survive restarts», а без `durable` она
«dies when this Claude session ends»; `/loop` — «Runs until you close this
session». Граница — сессия, то есть процесс CLI, а не беседа.

**Вердикт (2026-09-19):** принята, и проверена мной самим: `grep` по выгрузке
строк бандла даёт ноль совпадений на фразу и подтверждает описание `durable`.
Это худшая находка круга, потому что неверным оказался не скилл, а мой
собственный вывод, уже записанный и закоммиченный. Откуда взялась цитата, видно
в транскрипте: она пришла ходом `queue-operation` — отчётом справочного
субагента — и была процитирована как текст документации. Абзац скилла переписан
по тому, что в бандле есть; совет теперь — смотреть `CronList` и снимать
ненужное `CronDelete`. Запись `2026-09-19-clear-and-live-work-report.md`
получила раздел поправок.

**25 (B11). Отметка «Проверено» стояла на абзаце, проверенном наполовину.** Что
уведомление глохнет — подтверждено транскриптом дословно. Что задача продолжает
работать — держится на имени символа `hasAgentTaskSurvivingClear` и на том, что
предупреждение принадлежит выходу; единственный найденный переход границы
принадлежит задаче со статусом `killed`.

**Вердикт (2026-09-19):** принята. Абзац разделён на два: проверенное с датой
и версией, выведенное — с прямым «прогоном не подтверждено». Правило
от разделения не меняется: отчёта нет в обоих случаях.

**26 (C6). «Фоновому прогону хватает перенаправления, заданного при запуске».**
Если его не задали — а нигде в скилле не сказано задавать, — на шаге 2 сделать
уже нечего, и правило «всё живое пишет результат в файл» для такого прогона
не выполняется никак.

**Вердикт (2026-09-19):** принята. Названы оба случая: с перенаправлением —
путь файла в блок; без него — либо дождись сам, либо напиши в блоке, что вывод
остался в старой сессии и работу придётся повторить.

**27 (C7). «Ждут подтверждения» от субагента — ожидание без границы.**
Сообщение доберётся до него на его же следующем круге инструментов, а посреди
долгой работы это когда угодно; двумя экранами выше скилл сам велит ждать
только прогона проверок.

**Вердикт (2026-09-19):** принята. «Ждут подтверждения» заменено на «ответа
не жди» с объяснением, когда сообщение дойдёт.

**28 (C13). Строка таблицы про фоновую задачу не знает про расписания.** Правка
развела фоновые задачи и расписания, а таблица — то место, куда смотрят
по памяти, — отвечала за обе одинаково.

**Вердикт (2026-09-19):** принята, мелочь. В таблице заведена своя строка про
расписания.

**29 (C17). «Перед этим» в шаге 2 не к чему отнести.** Ближайший антецедент —
остановка задач, а речь ровно о том живом, которое не останавливают.

**Вердикт (2026-09-19):** принята, мелочь. «До конца хода».

## Проверки и заливка

**30 (A11). Пишущая заливка по имени перезаливает чужой абзац.** Правило
«пишущий режим — только на своих файлах, по имени» защищает на уровне файла,
а конфликт в этом скилле — на уровне абзаца внутри файла состояния. Ревьюер
прогнал настоящий `tool/reflow.py` solo и перезалил строку соседа; шаг 4
запрещает это прямым текстом.

**Вердикт (2026-09-19):** принята. Сказано, что файл состояния с чужой правкой
к «своим файлам» здесь не относится: по нему только `--check`, ширина своих
абзацев держится руками, красное на чужом абзаце идёт в блок. Эта же грабля
сработала у меня в этой сессии: `reflow.py docs/handoff.md` я гонял, и повезло
только тем, что чужой правки в файле не было.

**31 (A12, C5). Патч «чистой выгрузки» тащит в неё чужую правку.** Шаг 5
обещает выгрузку, где лежит ровно своя работа, и на этом основании запрещает
проверять в дереве. Для файла состояния патч собирается тем же
`git diff --binary HEAD`, а в нём лежат и чужие строки: проверка ширины
краснеет на чужом абзаце, которого в коммите не будет.

**Вердикт (2026-09-19):** принята. Названо исключение: патч файла состояния
несёт и чужие строки, и чужое красное различают по правилу ниже, а не чистотой
выгрузки.

**32 (C10). Закрывающая строка в `handoff` строже, чем в `end`.** Четвёртый
круг починил тупик «красное из-за соседа закрывает сессию навсегда» только
в `end`, а условие закрывающей строки переписал в `handoff` без этой оговорки.
По связке `end` → `handoff` строку говорит `handoff`, то есть тупик остался
на пути, ради которого правка и делалась.

**Вердикт (2026-09-19):** принята. Различение повторено в шаге 8 `handoff`
со ссылкой на шаг 10 `end`.

## Числа и цитаты

**33 (B3). Цитата «как портится верх» в истории solo не существует.** Абзац
подан цитатой прогона 2026-09-18, но ни одна версия `docs/handoff.md` такого
текста не содержала: `git log --all -S` по двум его фразам даёт ноль. Сама
болезнь настоящая — верх `5512b5a` именно таков, — а число 1742 принадлежит той
версии, тогда как `handoff` называет ту же историю числом 1848.

**Вердикт (2026-09-19):** принята и проверена: ноль версий по обеим фразам,
1742 у `5512b5a`, 1848 у `8d617cf`. Цитата заменена настоящим верхом `5512b5a`,
оба числа названы и разведены. Это вторая находка круга, где неверным был
не скилл, а то, что я сам в него записал: пересказ стоял в кавычках.

**34 (B10). «Пять и ни одной» проверить нечем.** Уборка не названа, правило
счёта не задано, наивный счёт по самому большому архиву даёт 19.

**Вердикт (2026-09-19):** принята. Числа убраны, осталось наблюдение без них.

**35 (C16). «Два-три коммита» не считает коммит-поправку про push.** Тот же
круг добавил в шаг 8 поправку строки об отправке отдельным коммитом.

**Вердикт (2026-09-19):** принята, мелочь. «А если выбрана отправка —
и четвёртый».

**36 (C18). «Первых… вторых…» считает в обратном порядке.** Порядковые
отсчитываются от порядка слов внутри фразы, а не от порядка пунктов списка.

**Вердикт (2026-09-19):** принята, мелочь. Вещи названы вместо номеров; вместе
с находкой 34 фраза переписана целиком.

## `handoff` и шов между скиллами

**37 (C4). «Обе кладут коммиты» — про уборку неверно.** Уборка шага 2 — это
`TaskStop`, `kill`, `git worktree remove`, `git branch -d` и `rm`; коммитов
среди них нет. Настоящая причина не начинать уборку сильнее: она необратима,
а владелец ещё не ответил.

**Вердикт (2026-09-19):** принята. Причина названа настоящая.

**38 (C8). Шаг 1 `handoff` даёт на чужую правку два разных ответа.** Первый
абзац велит останавливаться на любой, третий разрешает чужую правку вне вырезки
и шлёт к рецепту. Обоснование, приписанное первому правилу, — это обоснование
второго.

**Вердикт (2026-09-19):** принята. Сведено к одному правилу в первом абзаце:
внутри вырезки — стоп, вне — рецепт шага 1 `end`.

**39 (C9). «Шаг 3 своего коммита не делает» спорит с самим шагом 3.** Приходя
из `end`, шаг 3 обычно как раз и кладёт коммит — тот, что становится базой
переноса: поднять живое из того, что уедет, `end` не мог, потому что не знает,
какие разделы вычистит `handoff`.

**Вердикт (2026-09-19):** принята. Скобка переписана: шагу 3 остаётся поднять
живое, а коммита у него нет только тогда, когда поднимать нечего.

**40 (C11). Таблица `handoff` не знает про счёт вхождений.** Шаг 5 считает
число вхождений и прямо говорит, что блок, стоявший дважды и оставшийся раз,
уехал честно; строка таблицы называла проблемой любой оставшийся блок.

**Вердикт (2026-09-19):** принята, мелочь. Строка приведена к правилу.

**41 (C14). «Слотов всего четыре» читается двояко.** Предыдущая фраза называет
два потолка по четыре — вариантов на вопрос и вопросов за вызов.

**Вердикт (2026-09-19):** принята, мелочь. «Вопросов всего четыре», и заодно
сказано, что делать с тем, что не поместилось.

**42 (C15). «Неверна база или путь» не обновлена под четвёртый аргумент.**
Скрипт печатает `wrong <base> or <head>?`, а путь в этой ошибке не называет
вовсе: неверный путь падает раньше, сообщением git.

**Вердикт (2026-09-19):** принята, мелочь. Вместе с находкой 45 сообщение
и текст скилла переписаны под две причины.

**43 (B9). Описание обрыва цикла в проверке ширины шапки не совпадает
с разметкой, которую скилл же и предписывает.** Цикл встаёт на самом
`# Заголовке` записи, потому что шапка-цитата стоит выше него; довод про `###`
к этой разметке не относится. Вреда нет — проверка делает то, что нужно, —
неверно объяснение.

**Вердикт (2026-09-19):** принята, мелочь. Объяснение приведено к тому, что
происходит.

## `check_move.py` и его сторожа

**44 (C19). Скрипт молчит, когда архив несёт блок столько же раз, сколько тот
стоял, а из файла ушла только часть.** Проверка «скопировали, а не перенесли»
сравнивала число вхождений в файле до и после и ни разу не смотрела, сколько
копий забрал архив.

**Вердикт (2026-09-19):** принята и проверена своим стендом: два законченных
раздела с одной строкой уехали в архив оба, из файла вырезан один —
`0 problems`, зелено. Это дефект счёта, заведённого четвёртым кругом. Проверка
переписана на баланс: архив не может нести больше копий блока, чем отдал файл.
Сторож — вход этой пробы.

**45 (C23). «Wrong `<base>` or `<head>`?» не называет самой вероятной
причины.** Так же выглядит уборка, где текст скопировали в архив и забыли
вырезать; сторож при этом строил именно копию и закреплял неверный диагноз.

**Вердикт (2026-09-19):** принята. Названы обе причины, сторож переписан под
вторую.

**46 (C20). `# ` внутри забора считался заголовком.** Комментарий внутри
`sh`-блока резал блок на три, и одна часть получала освобождение, задуманное
для заголовков разделов; заодно ломался `label()`, добавленный тем же кругом.

**Вердикт (2026-09-19):** принята. `blocks()` ведёт состояние забора так же,
как `body_start`: строки внутри забора — обычные строки прогона.

**47 (C21). `body_start` на незакрытом заборе выбирает между двумя ошибками.**
Либо ложное «no `##` heading» при двух `##` в записи, либо молча урезанное
тело, из которого выпадает первый раздел.

**Вердикт (2026-09-19):** принята. Незакрытый забор теперь называется отдельным
сообщением.

**48 (C22). Скрипт краснел на дословном переносе, где разошлись только пустые
строки.** Печаталось это как `left state.md, not in the archive (x2): ''` —
строка, по которой искать нечего.

**Вердикт (2026-09-19):** принята. Пустые строки считаются отдельно
и печатаются словами; провалом это больше не считается, и докстринг про это
говорит.

**49 (C24). Четвёртый аргумент не покрыт ни одним сторожем.** `<head>` —
единственное, что правка добавила в интерфейс, и на нём стоит целый абзац
скилла; помощник тестов получил параметр `head`, и ни один из 11 тестов его
не задавал.

**Вердикт (2026-09-19):** принята и проверена: 11 вызовов без него,
а единственное совпадение с ним — сама сигнатура помощника. Заведён сторож
на коммит поверх переноса.

**50 (C25). Тесты оставляют по временному репозиторию на тест.** Одиннадцать
каталогов за прогон, притом что обслуживаемый ими скилл велит убирать за собой
каталоги в `/tmp`.

**Вердикт (2026-09-19):** принята. Каталоги чистятся в `tearDownModule`; после
прогона в `/tmp` не остаётся ничего.

**51 (C26). Докстринг обещает «both promises» при трёх обещаниях.**

**Вердикт (2026-09-19):** принята, мелочь. «All three promises», заодно убран
двойной пробел.

**52 (C27). Итоговая строка печатает «1 problems».**

**Вердикт (2026-09-19):** принята, мелочь. Заведён `plural()`, и та же строка
перестала печатать «1 non-blank lines».

## Что ревьюеры проверили и признали верным

- **B**, около шестидесяти утверждений и команд: весь набор git-рецептов обоих
  скиллов (patch/archive/apply, stash, worktree, `branch -d`, коммит путями,
  `add -N`, detached HEAD, кавычки zsh), поведение rtk для шести названных
  команд, все числа уборок (1848 → 247, 154, 59, 776, 460, 2368), схема
  `AskUserQuestion`, существование `/tasks`, `TaskStop`, `SendMessage`,
  `Monitor`, `CronCreate`, `ScheduleWakeup`, файлы состояния и заголовки
  в solo, saqlume и kokjai, коммит `30c64d8`.
- **A**: на happy path прежний рецепт обещание держал — в коммит уходила только
  своя правка, чужой абзац возвращался в дерево байт в байт; верны `$W` через
  `--path-format=absolute --git-common-dir`, выгрузка и `git apply` шага 3,
  разница `worktree list` с `-v` и без, число 2368, и то, что `rtk git commit`
  путями ведёт себя как `/usr/bin/git`.
- **C**: числа шапки `handoff` сходятся с выводом скрипта и с `git show`,
  прогон `check_move.py` на всех трёх настоящих уборках solo зелёный, списки
  команд rtk в двух скиллах совпадали дословно, все 11 прежних сторожей
  зелёные.

После правок: 17 сторожей `check_move.py` зелёные, шесть из них краснеют
на прежней версии скрипта; на трёх настоящих уборках solo скрипт по-прежнему
даёт ноль проблем.

## Что осталось непроверенным

- Продолжает ли фоновая задача работать после `/clear`. Проверяется только
  живым опытом с настоящим `/clear`, который набирает владелец.
- Уносит ли `/clear` задачу `CronCreate` без `durable` и цикл `/loop`. Бандл
  говорит про конец сессии, а не беседы; считается ли очистка концом сессии,
  из него не следует, и хук на ней называется `SessionStart:clear`.
- Новый рецепт отделения своего от чужого прогнан мной на стенде
  с заиндексированной чужой правкой и держит оба обещания, но читал его, как
  и в прошлый раз, только тот, кто его писал.
