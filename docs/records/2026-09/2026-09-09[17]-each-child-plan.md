> **Состояние на 2026-09-10:** выполнено в `main`, коммит `0b3e19e`;
> проверки и независимое ревью пройдены.
> **Что это:** план реализации дочернего `each` и проверки совместимости.
> **Связанные записи:** `2026-09-09[16]-each-child-design.md`,
> `2026-09-09[18]-each-child-report.md`.

# План реализации

Цель: отдельно отменяемая обработка потока с обычным жизненным циклом
дочернего `Job`. Дизайн: `2026-09-09[16]-each-child-design.md`.
Стек: Dart, `fake_async`, тесты пакетов, существующие проверки документов.
Работать в `main`; выпуск и push не входят в задачу.

- [x] Добавить в `packages/async_job/test/each_child_test.dart` сценарий:
  тело вызывает `ctx.each(source.stream, (_, event) {})` без ожидания;
  после микрозадач родитель ещё работает и подписка открыта; закрытие
  потока завершает оба `Job`. До реализации проверить отказ старого API.
- [x] Добавить сценарии отдельной отмены, каскада родителя, ожидания
  занятого обработчика и отмены через дочерний контекст. Проверять журнал
  событий и момент освобождения ресурса через `Completer` и `FakeAsync`.
- [x] В `packages/async_job/lib/src/job_context.dart` объявить
  `Job<void> each<T>(Stream<T> stream,
  FutureOr<void> Function(JobContext ctx, T event) onData)` и защищённую
  фабрику `Job<void> createEachJob(Future<void> Function(JobContext) body)`.
  Реализация запускает `run(createEachJob(...))`.
- [x] Перевести `job_stream.dart` в `part`, сохранить буфер и подписку,
  добавить ожидание выполняющегося обработчика в `finally`. Обновить
  `job_base.dart` и экспорт `async_job.dart`.
- [x] В `packages/solo/lib/src/job_context.dart` уточнить тип обработчика
  и переопределить фабрику: `_solo.job<W, void>(body,
  keepWhile: _job._keepWhile, describe: () => 'each')`.
- [x] Адаптировать существующие тесты `each_test.dart` обоих пакетов:
  `.value`, второй аргумент обработчика, дочерние события наблюдателя.
  Сценарии ухода тела заменить проверками ожидания ребёнка. В
  `unattended_test.dart` проверить отказ запуска дочернего `each`.
- [x] Проверить в `solo` отдельную отмену, тип дочернего контекста,
  сохранение правил после конца тела, ожидание уборки и очереди.
- [x] Обновить README ядра и `solo` с переводами, CHANGELOG, архитектуру
  и handoff. Поискать остальные обращения к `each` и старому extension.
- [x] Выполнить `dart analyze` и `dart test` ядра, `solo` и его примера;
  анализ и `flutter test` Flutter-пакета, анализ его примера. Проверить
  переводы, ширину, форматирование и dartdoc затронутого публичного API.
- [x] Провести независимое ревью, исправить подтверждённые дефекты,
  обновить записи и закоммитить всю работу одним Conventional Commit.
