> **Состояние на 2026-09-24:** вычитка сделана, ждёт чтения владельца; ветка
> `doc-camera`, слияние за владельцем.
> **Что это:** отчёт о вычитке `packages/solo/doc/camera.md` и её русской
> версии `docs/ru/solo/camera.md`: зонды по утверждениям, две правки самого
> примера по решению владельца, перестройка страницы первыми попытками
> и сторож `camera_rakes_test.dart` в пакете примера; в конце — попутная
> правка раздела 3 `packages/solo/doc/vs-bloc.md` о закрытии `bloc`.
> **Связанные записи:** `2026-09-23-testing-rakes-report.md`,
> `2026-09-16[2]-state-rakes-report.md`,
> `2026-09-18[8]-cancellation-rakes-report.md`,
> `2026-09-11[16]-docs-structure-design.md`.

# Вычитка camera.md

## Что было

158 строк, один заголовок и ни одного раздела: четыре блока кода подряд —
состояния, контроллер целиком, пример вызова, команды запуска — и по абзацу
прозы между ними. Первых попыток не было, сторожа тоже.

Контроллер на странице был упрощённой копией
`example/lib/src/camera_controller.dart` и с ней разошёлся. У `init`
на странице не было посадки на провал, а в примере она была; у `dispose()`
не было строки `hw.onError = null`, без которой сбой устройства после
освобождения перетирает `Disposed`; у `Ready` не было `focusPoint`, который
печатают журналы. Страница при этом говорила, что `FakeCameraHardware`
и `Photo` берутся из примера, — читатель, открывший файл, находил там другой
код.

## Зонды

Тридцать один зонд в семи файлах `example/test/camera_probe*_test.dart` и ещё
один, о линте, в `packages/solo`; все удалены после работы. Что они показали:

| Утверждение | Прогон |
| --- | --- |
| `run<Initial, void>` с `emit(Preparing)` | `Cancelled(rules: is not Initial)`, `hw.open` не вызван, контроллер навсегда в `Preparing` |
| `init` без `canStart`, дважды | устройство открыто два раза |
| `init` страницы, дважды | вторая `dropped Cancelled(rules: canStart)` |
| `setZoom` без политики, три вызова в такте | состояние то же `Ready(zoom: 4.0)`, до объектива доходят все три |
| `Policy.replace`, три вызова | две `dropped Cancelled(manual)`, до устройства доходит один |
| `replace`, пока масштаб 2 выставляется | 2 доходит, 3 отброшен, потом 4 |
| `takePhoto` без `queue.clear()` | масштаб, запрошенный во время снимка, выставляется после него |
| `dispose()` без очистки и отмены | встаёт в конец очереди, камера доснимает и меняет масштаб после приказа выключаться |
| `dispose()` с очисткой и отменой | ждущее отброшено, идущий масштаб отменён, но `join` дождался устройства |
| `dispose(); close();` в одном такте | `dropped Cancelled(closed)` несмотря на `cancellable: false`, устройство не закрыто, состояние `Ready` |
| `close()` при стартовавшем освобождении | ждёт его, устройство закрыто, `Done` |
| слушатель устройства, оставленный на месте | сбой после освобождения публикует `Broken` поверх `Disposed`; после `close()` бросает `StateError` в колбэк устройства |
| `try`/`catch`-посадка при отмене открытия | `emit` в `catch` бросает `Cancelled`, состояние остаётся `Preparing`; так вела себя и `init` примера |
| два `dispose()` подряд с `force: true` | первый вызывающий получает `Cancelled(manual)`, хотя камера освобождена |
| два `dispose()` подряд без `force` | обе `Job` одна и та же, обе `Done` |
| линт на `camera.setZoom(2);` | `unawaited_futures` молчит о `Job` и срабатывает на `Future` рядом |

## Что нашлось в самом примере

Два расхождения, оба в коде примера, а не в странице. Решения владельца
2026-09-24, оба по рекомендованному варианту.

1. `init` и `reopen` сажали камеру в `Broken` через `try`/`catch` в теле —
   ровно то, что `doc/state.md` показывает первой попыткой. Отмена проходила
   через тот же `catch`, `emit` на отменённой `Job` бросал `Cancelled`,
   и открытие, отменённое на полпути, оставляло камеру в `Preparing`: `init`
   хочет `Initial`, `reopen` — `Ready` или `Broken`, и стартовать не могло
   ничего. Обе посадки переписаны на `onError` и `onCancel` у `run`: провал
   и отмена ведут в `Broken`. Порядок строк журнала сменился — `state: Broken`
   теперь идёт после строки `error`, потому что состояние вычисляет обработчик,
   и тест примера `a failure while reopening leaves the camera broken`
   поправлен на этот порядок.
2. `dispose()` чистил очередь через `queue.clear(force: true)`. Во всём
   контроллере неотменяемая `Job` верхнего уровня одна — само освобождение, так
   что `force` менял одно: второй `dispose()` вычищал первый, и его вызывающий
   получал `Cancelled(manual)` за освобождённую камеру. Без `force` первая
   `Job` переживает очистку, и `Policy.droppable` отдаёт её второму вызову.

Замечено и не тронуто: тест примера про `pause` и `resume` проверяет состояние,
а не журнал, — поэтому последняя фраза страницы говорит «тесты проходят каждую
из них», а не «проверяют журнал каждой».

## Что стало

539 строк, восемь разделов, шесть открываются первой попыткой, 27 блоков кода
в обеих версиях. Во вступлении — состояния примера, устройство и формат
журнала; обещание одно: последний фрагмент каждого метода — его код в примере,
без комментариев.

| Раздел | Первая попытка | Цена |
| --- | --- | --- |
| Opening the camera | рабочий тип `Initial` | `Job` отменена собственной публикацией, камеру больше не открыть |
| An opening that fails | посадка через `catch` | отмена оставляет `Preparing` при открытом устройстве |
| Only the last zoom | без политики | состояние то же, устройство получает каждый запрос |
| Commands that arrive during a shot | — | раздел открывается ответом |
| Disposing of the camera | освобождение как обычная `Job` | ждёт всю очередь, камера снимает после приказа выключаться |
| Closing the controller | `close()` в том же такте | освобождение отброшено, устройство открыто навсегда |
| A failure after the disposal | слушатель оставлен | `Broken` поверх `Disposed`, после `close()` — бросок в колбэк |
| Running the example | — | раздел открывается ответом |

Раздел «Commands that arrive during a shot» объясняет `queue.clear()` в снимке
и не показывает версии без неё первой попыткой: считать команды частью снимка —
решение этого примера, а не ловушка.

## Сторож

`packages/solo/example/test/camera_rakes_test.dart` — 30 тестов, в примере
стало 39. Первые попытки живут в классе `FirstAttempts`, ответы — сам
`CameraController`. Каждый журнал, процитированный страницей, сверяется
построчно. Последний тест читает `../doc/camera.md`, берёт для каждого метода
последний фрагмент на странице и требует, чтобы он без комментариев и отступов
нашёлся в `lib/src/camera_controller.dart`: так держится обещание вступления.

Двенадцать мутаций кода примера, все пойманы, и каждая, кроме мутации `reopen`,
краснит заодно сверку фрагментов: `init` без `canStart`, без `onError`, без
`onCancel` (по одному сторожу), `reopen` без `onCancel` (1), `setZoom` без
`replace` (2) и без `canStart` (1), `takePhoto` без очистки (1), `dispose()`
без очистки (1), без отмены (2), с `force` (1), без отключения слушателя (1)
и отменяемый (3). Мутации гонялись копией файла с восстановлением в `finally`.

Одно утверждение страницы держится не сторожем, а зондом: что
`unawaited_futures` молчит о брошенной `Job`. Пример подключает
`package:lints/recommended.yaml`, где этого линта нет; зонд запускался
в `packages/solo`, где он включён.

## Проверки

`packages/solo/example`: `dart format` без изменений, `dart analyze` чисто,
`dart test` 39, `dart run bin/main.dart` доходит до `closed`. `packages/solo`:
`dart format lib test example` без изменений, `dart analyze` чисто, `dart test`
790. Из корня: `reflow.py --check`, `check_line_width.py`,
     `check_translations.py` (21 заголовок и 27 блоков в обеих версиях),
     `check_links.py`, `check_doc_shape.py` — зелёные. Скрипты скилла
     `doc-reader`: `jargon.py` чист на обеих версиях,
     `bare_names.py --allow=future` — ноль голых имён.

## Попутно: очередь bloc при закрытии

Владелец спросил 2026-09-24, что `bloc` делает при закрытии с событиями,
которые ещё не разобраны. Раздел 3 `packages/solo/doc/vs-bloc.md` отвечал про
один работающий обработчик: «With `sequential()`, `close()` waits for the
running handler». Зонд на версиях стенда — `bloc` 9.2.1 и `bloc_concurrency`
0.3.0, три события по 20 мс, потом `close()`:

```text
sequential()            все три обрабатываются по очереди, emit принимаются,
                        close() возвращается через 60-70 мс; состояние
                        [1, 2, 3]
concurrent, restartable очереди нет: все три уже в обработчиках; close()
                        возвращается сразу, тела доходят до конца, emit молча
                        игнорируются, состояние []
droppable               2 и 3 отброшены ещё при add; close() возвращается
                        сразу, тело первого доходит до конца, emit игнорируется
```

У `sequential()` очередь проходится внутри `await _eventController.close()`:
событие «конец потока» стоит в буфере за ждущими, и эмиттеры отменяются, когда
отменять уже нечего. `Bloc.isClosed` при этом уже истинно, так что охранник
`if (isClosed) return;` из ответа раздела отклоняет ответ каждого слитого
события — но сам вызов API в нём уже сделан.

Правка в обеих версиях: фраза о трансформерах говорит, что `sequential()`
проходит очередь до конца, а остальные три ничего не держат; ответ `bloc`
говорит, что каждое ждущее сообщение всё равно уходит в API; сторона `solo`
говорит, что ждущее сообщение кончается `Cancelled(closed)`, не стартовав,
а слить очередь, как `sequential()`, — это `close(mode: SoloCloseMode.drain)`.
В фейковом API стенда появились списки `sent` и `answered`, в драйверах
раздела — сторожа на каждое из этих утверждений: `sequential()` с охранником
и без, `concurrent`, `droppable`, `restartable`, обычное закрытие `solo`
и `drain`. Две перевёрнутые проверки — `droppable` с двумя доставленными
и `drain`, заменённый обычным закрытием, — краснят драйвер. Стенд целиком
зелёный: анализ трёх пакетов чист, все драйверы доходят до конца,
`check_traces.py` — 2 и 11 трасс, ни одной не напечатанной.
