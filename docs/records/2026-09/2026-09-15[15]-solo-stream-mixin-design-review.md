> **Состояние на 2026-09-15:** ревью пройдено, вердикт стоит у каждой находки; принятое внесено в спеку.
> **Что это:** первое независимое ревью спеки `2026-09-15[14]-solo-stream-mixin-design.md`.
> **Связанные записи:** `2026-09-15[14]-solo-stream-mixin-design.md`, `2026-09-15[16]-solo-stream-mixin-design-review-2.md`.

# Ревью спеки про миксин стрима, первое

Ревьюер — Fable 5.1, копия дерева `/Users/user/development/my/solo-review-a`
на `6861f0d`. Зонды: миксин из спеки дословно против нынешнего `SoloBase` (10
тестов), анализатор, комбинация в `flutter_solo` (5 тестов). Наборы прогнаны
целиком.

## 1. `doc/api/**` не под гитом — спека и критерий утверждают обратное

Спека: «Сгенерированный `doc/api/**` лежит под гитом — его перегенерировать
дартдоком». Критерий: «`doc/api/**` перегенерирован».

`git ls-files packages/solo/doc/api | wc -l` → `0`;
`ls -d packages/solo/doc/api` → `No such file or directory`;
`.gitignore:9: **/doc/api/`.

Критерий непроверяем, а исполнитель потратит время на несуществующий шаг.
Убрать абзац и критерий. Severity: важная.

**Вердикт: принято.** Проверено в рабочем дереве: `git ls-files` даёт ноль,
строка `**/doc/api/` в `.gitignore` на месте. Файлы у меня на диске лежат
несохранёнными, поэтому мой grep их и нашёл — счёт вёлся по диску,
а не по индексу. Абзац и критерий сняты.

## 2. Стримовых чтений не восемь; пропущены `children.md` и драйверы стенда

Спека: «код, читающий `.stream` у контроллера — восемь мест… `close_test`,
`closed_state_test`, `hooks_test`, `sequential_test`, один пример
в `doc/state.md` и абзац README».

Тесты — 7 вызовов; `doc/state.md:94`, `README.md:119` и их русские копии;
**`packages/solo/doc/children.md:119`** —
`await ctx.each(session.stream, take)` при `final Solo<Session> session;`
(строка 108), страница стендом не собирается, гейт не поймает;
**`tool/doc_snippets.py:749`** — `p2.stream` у `PlayerController`
из `vs-bloc.md:798`, **`:1571`** — `controller.stream` у `ReportController`.
Итого 12 вызовов, а объявлений, которым нужен миксин, девять.

Пропуск в `children.md` — сломанный пример на pub.dev, никем не пойманный;
стенд — красный CI после первого же коммита правки. Переписать счёт
по объявлениям, а не по вызовам; включить `children.md`, где появляется
четвёртая форма — миксин как тип зависимости,
`final SoloStream<Session> session;` — и оба фрагмента `vs-bloc.md`. Severity:
важная.

**Вердикт: принято целиком.** Проверено: `children.md:107` объявляет
`ScreenController extends Solo<Screen>`, `:108` — поле
`final Solo<Session> session;`, `:119` читает его стрим; `tool/doc_snippets.py`
читает `.stream` на строках 690, 749, 1571. Находка ценнее, чем плюс одно
место: четвёртой формы — миксин в позиции типа — в спеке не было вовсе, а она
меняет то, как пишется зависимость между контроллерами. Счёт в спеке переписан
по объявлениям, `children.md` и `tool/doc_snippets.py` внесены в поверхность,
четвёртая форма добавлена в раздел «Что получается».

## 3. «Восемь мест переводятся на `with SoloStream`» — миксин вешается на фикстуру, не на вызов

Три из семи тестовых вызовов идут через `runSolo` → `TestSolo`
(`test/support/test_solo.dart:6`), которой пользуется большая часть набора:
`close_test.dart:16`, `:181`, `sequential_test.dart:107`.

Решение не записано: либо `TestSolo` получает `with SoloStream<TestState>` —
и тогда почти весь набор `solo` гоняется под миксином, а «база без стрима»
покрыта одним `solo_base_test`; либо стримовые тесты получают свою фикстуру.
Это выбор о том, что именно тестирует набор. Severity: важная.

**Вердикт: принято, решение записано.** Общая фикстура стрим не получает: иначе
критерий «про каждое сказано, что стрим ему не нужен» противоречит сам себе,
а набор перестаёт проверять базу в той форме, в какой её увидит пользователь.
Стримовые тесты получают отдельную фикстуру. Внесено в спеку именами классов.

## 4. Критерий «`SoloBase` не встречается ни в одном живом артефакте» недостижим

`packages/solo/CHANGELOG.md` строки 283, 312, 313, 354, 393 — раздел
**`## 0.2.0`**, выпущенная история, её не переписывают. Ещё:
`packages/async_job/lib/src/job_base.dart:315, 888` — дартдок нижнего пакета;
имена файлов `test/solo_base_test.dart`, `test/listeners_base_test.dart`;
`docs/conventions.md:51`.

Либо критерий нарушен, либо переписывают выпущенный CHANGELOG. Сузить: «в коде,
дартдоке, документации и нерелизных разделах CHANGELOG; выпущенные разделы
не трогаются»; про `async_job` и имена тестовых файлов решить явно. Severity:
важная.

**Вердикт: принято.** Проверено `awk` по разделам: пять упоминаний в выпущенном
`## 0.2.0`. Критерий сужен именно так. По `async_job` решение принято отдельно:
дартдок правится, потому что все три пакета идут на `0.3.0` одной волной — это
записано в handoff и проверено в `pubspec.yaml` каждого.

## 5. Нерелизные записи CHANGELOG противоречат правке и поедут в тот же релиз

`packages/flutter_solo/CHANGELOG.md:70–75` (Unreleased): «**Breaking:**
`SoloListenable` is built on `SoloBase`, not on `Solo`… no longer fits where a
`Solo<S>` is expected; `SoloBase<S>` is the type that…». После переименования
`SoloListenable` снова подходит туда, где ждут `Solo<S>`. Ещё 8 упоминаний
в Unreleased `solo` и 6 во `flutter_solo`.

Читатель заметок к 0.3.0 увидит в одном списке «не подходит под `Solo<S>`»
и «`extends Solo`». Нерелизные записи обоих CHANGELOG сверяются
и переписываются под новые имена, противоречащая запись сворачивается в новую.
Severity: важная.

**Вердикт: принято.** Запись прочитана целиком — она не просто называет старое
имя, она утверждает ровно то, что правка выворачивает. Внесено в поверхность
и в критерии.

## 6. Молчаливые ссылки `[Solo]` и проза «`Solo` with its stream»

Спека считает файлы по `SoloBase` — их дартдок пометит как unresolved.
Но ссылки на **`Solo`** продолжат резолвиться, сменив смысл с «класс
со стримом» на «база»: `solo_base.dart:22`, `solo_listenable.dart:8`,
`doc/state.md:131`, `:168`, `doc/flutter.md:97`,
`flutter_solo/README.md:181, 392`, `docs/architecture.md:113`,
`vs-bloc.md:43, 49`.

Это и есть настоящая «тихая потеря»: не код — любое чтение `.stream` даёт
ошибку компиляции, — а проза, которая остаётся валидной и врёт. Severity:
важная.

**Вердикт: принято, и это лучшая находка круга.** Я назвал главным риском тихую
потерю стрима в коде. Ревьюер показал, что в коде она как раз громкая: все
двенадцать чтений — ошибки компиляции. Тихо ломается проза. Раздел «Риски»
в спеке переписан с этой поправкой, проход по слову `Solo` добавлен
в поверхность.

## 7. `docs/architecture.md`: не три строки, а пять мест

Спека: «строки 25–26, 267 и 310». Ещё строка **303** («`solo.dart` — отдельная
библиотека, `Solo<S>`: `stream` поверх `publish`»), строка **113**
(`Solo.stream`) и абзац 25–30 целиком — «Братья, а не цепочка… (решение
владельца 12.09.2026, отчёт — `2026-09-12[4]-listenable-on-base-report.md`)»:
довод отменяется этой спекой. Шапку отчёта пометить «пересмотрено 2026-09-15»,
как требует AGENTS.md. Severity: мелочь.

**Вердикт: принято.** Проверено построчно. Отчёт `2026-09-12[4]` добавлен
в связанные записи спеки, его шапка помечается в коммите правки.

## 8. Цифры 74 и 15 исключают `tool/` и `docs/*.md`

`extends Solo<` без `docs/records` и `doc/api` — **84** (`tool/`
и `docs/handoff.md` добавляют десять). `extends SoloBase<` — **17**. Также
`tool/doc_snippets.py:1806` ставит `SoloBase.observer` — не в списке спеки.

Девять объявлений в стендах компилируются в CI. Назвать множество явно или
пересчитать. Severity: мелочь.

**Вердикт: принято.** Множество названо в спеке явно, `tool/doc_snippets.py`
внесён в поверхность отдельной строкой.

## 9. Пол зависимости `flutter_solo` на `solo` и порядок релизов не названы

`packages/flutter_solo/pubspec.yaml:21: solo: ^0.2.0`; оба пакета 0.2.0, правка
ломающая для обоих. `flutter_solo` нельзя выпустить раньше `solo`; без поднятия
пола опубликованный `flutter_solo` разрешится против `solo` 0.2.0
и не соберётся. Severity: важная.

**Вердикт: принято.** Проверено в обоих `pubspec.yaml`. Абзац про версии
и порядок выпуска внесён; оверрайды живут до обоих релизов, как и требует
раздел «Оверрайды разработки» в AGENTS.md.

## 10. README-лицо и таблица `vs-bloc` — решение не принято

Первый пример README (`ProfileController extends Solo<ProfileState>`, строка
74) читает `profile.stream` (119, 135). Витрина пакета откроется формой
`with SoloStream`. `vs-bloc.md:43` и `:49` — таблица сравнения с Bloc: после
правки стрим opt-in, сравнение меняется. Это выбор лица пакета, не механическая
замена. Severity: важная.

**Вердикт: принято частично.** Про `vs-bloc.md` решаю сам: оба фрагмента
получают `with SoloStream`, строки таблицы переписываются — страница сравнивает
формы, и показать настоящую форму честнее, чем спрятать её; заодно миксин
попадает под стенд, то есть под компиляцию в CI. Лицо README — вопрос вкуса
и позиционирования, он вынесен владельцу вторым открытым вопросом спеки.

## 11. Комбинация `SoloListenable + SoloStream` работает, но у неё есть цена

Зонд в `flutter_solo`: `order: [listener 1, after set, stream 1]`;
`builds after one change: vlb=2 sb=2` — одно изменение, две перестройки;
`FlutterError (listener) / zone (subscriber)` — **два маршрута ошибок в одном
контроллере**; `close journal: [closed, stream done, hasListeners=false]` —
`await close()` теперь ждёт подписчиков стрима, чего у чистого `SoloListenable`
не было. `SoloBuilder`, `SoloSelectBuilder`, `SoloSelector`, `SoloSelection` —
всё зелёное.

В дартдок `SoloStream` и в `flutter.md`: два маршрута ошибок, двойная
перестройка при двух подписках, `close` ждёт стрим. Severity: мелочь.

**Вердикт: принято.** Спека объявляла комбинацию возможностью и молчала о её
цене. Три следствия внесены в критерии как требование к дартдоку
и к `flutter.md`.

## 12. Порядок миксинов: анализатор ловит, но теста и дартдока в критериях нет

Зонд: `muting above: [], muting below: [1]` — миксин над `SoloStream`,
не зовущий `super.publish`, глушит стрим; под ним — нет. `must_call_super`
срабатывает одинаково над классом и над миксином. Спека в «Рисках» говорит «в
дартдок миксина это надо сказать», в критериях этого нет. Severity: мелочь.

**Вердикт: принято.** Критерий добавлен: дартдок называет правило порядка, тест
на глушитель над и под миксином.

## 13. Ловушка в `tool/accumulation_snippets.py`

`:768` и `:1257` — замена по точному заголовку класса,
`.replace('class LogController extends Solo<int> {', …)`. Если фрагменту
добавят `with SoloStream`, замена молча не найдёт строку, и стенд соберёт два
класса с одним именем. Severity: мелочь.

**Вердикт: принято.** В спеке записано: фрагменты `accumulation.md` стрим
не читают и остаются `extends Solo<` — то есть ловушка не срабатывает, но она
названа, чтобы следующий не наступил.

## 14. Deprecation-алиас: решение не записано

`typedef SoloBase<S extends Object> = Solo<S>` с `@Deprecated` оставил бы чужие
`extends SoloBase<S>` компилирующимися один релиз. Спека этого не обсуждает;
критерий «`SoloBase` не встречается» его неявно запрещает. Severity: мелочь.

**Вердикт: принято.** Алиаса нет: пользователей нет, версия `0.x`, и алиас
пережил бы правку только затем, чтобы его выпиливали отдельным релизом. Фраза
внесена в спеку явно, чтобы вопрос не поднимался второй раз.

## 15. Открытый вопрос про `abstract`: поставлен верно, два следствия не названы

Проверено: абстрактных членов нет; 30 прямых вызовов в четырёх файлах, фикстура
в две строки закрывает все 30. Не названо: (а) довод «живая возможность»
слабее, чем звучит — `externalSetState` защищён, поэтому голый `Solo<int>(0)`
вне тестов двигают только `run`/`add` снаружи, ровно та форма, против которой
`vs-bloc.md`; (б) дартдок базы «Subclasses add a delivery channel through
[publish]; see `Solo` for a stream» переписывается при любом ответе; (в)
симметрия с `SoloListenable`. Severity: мелочь.

**Вердикт: принято.** Все три внесены в открытый вопрос. Пункт (а) — довод
против моей же рекомендации, и он сильный: аргумент «это живая возможность»
держался на тридцати тестах.

## 16. Критерии приёмки: чего не хватает и что непроверяемо

Непроверяемо: `doc/api`; «про каждое сказано» — не сказано, где. Неверное
число: восемь мест. Недостижимо: «`SoloBase` не встречается». Не хватает:
`dart analyze` во всех пакетах, пол `solo: ^0.3.0`, сверка нерелизных
CHANGELOG, проход по `Solo`-прозе, `docs/architecture.md`,
`docs/conventions.md`, `docs/handoff.md`, шапка `2026-09-12[4]`, дартдок
`SoloStream`, стенд `doc_snippets.py`, `check_traces.py`. «Наборы не меньше
нынешних» — слабо, лучше назвать новые тесты поимённо. Severity: важная
совокупно.

**Вердикт: принято целиком.** Критерии в спеке переписаны по этому списку,
включая требование назвать место, где отчёт о работе перечисляет просмотренные
объявления.
