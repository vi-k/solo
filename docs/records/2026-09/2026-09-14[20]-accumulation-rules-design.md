> **Состояние на 2026-09-15:** третья редакция, после двух кругов ревью
> по два ревьюера в каждом. **Сделано и смержено в `main`** четырьмя
> коммитами — `07e4181`, `8068b71`, `fadbb2a`, `7ff9083`; отчёт —
> `2026-09-15[3]-accumulation-rules-report.md`. Не опубликовано. Три
> открытых вопроса были закрыты решениями владельца 2026-09-14 и внесены
> в текст. Работа в пакете `solo`. Три пункта `docs/backlog.md`, взятые
> одной волной по решению владельца 2026-09-14; после волны бэклог пуст.
> **Что это:** три правки накопителя — глобальное умолчание
> `AccumulationPolicy.join`, `AccumulationTiming.throttle(duration,
> startAtOnce: false)` и перенос ожидающей группы вместо отмены её задачи
> в `AccumulationPolicy.replace`.
> **Что изменила вторая редакция:** три числа первой редакции оказались
> неверны, и одно из них несло вывод: стенд документации правку `replace`
> **ловит**, а счёт «двух сторожей» был артефактом раннего выхода драйвера.
> Рецепт команд правка A ломает молча — замерено, что команда до устройства
> не доходит вовсе. Довод про нулевую длительность перестал быть главным.
> Добавлены инварианты переноса, семантика правил старта для трейлинга
> и поимённый перечень того, что теряет правду в документах.
> **Что изменила третья редакция:** трейлинг заводил интервал на каждом
> принятом событии, и группа, уже отстоявшая свой интервал, откатывалась
> назад от следующего события — против обещания справочника. Интервал
> заводится при появлении группы и только там; исправление внесено
> в прототип и замерено. Инвариант о содержимом очереди оказался наполовину
> неверен и переписан; цена стенда названа на том блоке, который
> действительно краснеет; критерии переписаны через исход, а не через
> равенство вызовов.
> **Связанные записи:** круги ревью —
> `2026-09-14[22]-accumulation-rules-review.md`,
> `2026-09-14[23]-accumulation-rules-review-2.md`,
> `2026-09-14[26]-accumulation-rules-review-3.md`
> и `2026-09-14[27]-accumulation-rules-review-4.md`; откуда взялись правки —
> `2026-09-14[18]-accumulation-restructure-report.md`, разделы «Какое правило
> хорошо к таймингу», «Цена немедленного первого старта throttle»
> и «Таблица политик».

# Три правки накопителя

## Откуда это

Все три нашлись при вычитке `accumulation.md` и легли в бэклог по отдельным
поручениям владельца. Общего у них больше, чем кажется: каждая снимает особый
случай, который сегодня приходится объяснять в документе. Волной их берём
потому, что после всех трёх таблица правил становится одноосной, а порознь
каждая правка оставляет таблицу двуосной и требует своего абзаца про
исключение.

## Как мерялось

Рабочее дерево делят параллельные сессии, поэтому прототипы жили в отдельных
копиях — `git worktree add --detach` на `5cc1bef`. Каждая правка применялась
к библиотеке отдельно, потом все три вместе; на каждом состоянии прогонялись
`dart test` пакета `solo` (526 тестов) и стенд документации
(`tool/accumulation_snippets.py`, пять драйверов). Зонды и общий диф трёх
правок лежат в `packages/solo/.artifacts/2026-09-14-accumulation-prototype/` —
каталог под `.gitignore`, он живёт до конца работы.

Сценарий, общий для замеров порядка: событие A1, чужая задача B следом, событие
A2 через 50 мс. Очередь занята первые 100 мс, иначе группа стартует сразу
и правило не проявляется.

**Урок первой редакции про стенд.** Драйвер стенда падает на первом же
`require`: `StateError` останавливает прогон, и сторожа ниже по файлу
не выполняются. Поэтому «сколько сторожей краснеет» нельзя прочитать из одного
прогона — читается только «какие файлы краснеют и на чём именно первый из них».
Первая редакция на этом ошиблась и записала, что правку C стенд не ловит;
ловит.

## Правка A: умолчание `AccumulationPolicy.join`

### Что сегодня

`collect` и `accumulate` берут `AccumulationPolicy.adjacent` при любом
`timing`. Это умолчание хорошо ровно для накопителя без тайминга: `adjacent`
кончает группу, когда за ней встала другая задача, и чужая задача работает
границей.

### Замер

Свободная очередь не показывает ничего: все три правила дают
`run[a1]@0, other@0, run[a2]@50`. Группа стартует сразу, закрывается, и A2
в любом случае заводит новую. Разница видна только пока группа ждёт:

```text
очередь занята 100 мс
  без тайминга
    adjacent  run[a1]@100, other@100, run[a2]@100
    join      run[a1+a2]@100, other@100
  debounce 200
    adjacent  other@100, run[a1]@200, run[a2]@250
    join      other@100, run[a1+a2]@250
  throttle 200
    adjacent  run[a1]@100, other@100, run[a2]@300
    join      run[a1+a2]@100, other@100
```

С таймингом `adjacent` ошибается дважды. Во-первых, группа делится или
не делится смотря по тому, крутилась ли в очереди посторонняя задача, — то есть
одни и те же два события дают то одну отправку, то две. Во-вторых, с throttle
вторая группа ждёт ещё целый интервал: 300 мс против 100.

Что при этом **не** меняется: место B относительно A1. Она идёт после A1 без
тайминга и перед ним с окном, и правило на это не влияет. Меняется место A2:
под `adjacent` она после B, под `join` — перед ней, внутри первой группы.

### Что покупает `adjacent`

Ровно эту границу, и она не абстрактная. Рецепт команд в `accumulation.md`
стоит на умолчании и на границе держится: его `merge` выбрасывает накопленную
команду и оставляет входящую, потому что «последняя команда и есть ответ».
Замер рецепта — `resume`, чужая задача `seek`, `pause`, очередь занята:

```text
adjacent (сегодня)  устройство услышало [resume, seek 30, pause]
                    один хэндл на обе команды: false
join                устройство услышало [pause, seek 30]
                    один хэндл на обе команды: true
```

Под `join` команда `resume` до устройства **не доходит вовсе**: `pause` слилась
в группу, стоящую перед `seek`, и `merge` выбросила `resume`. Плюс порядок
с чужой задачей перевернулся. Абзац документа
(`packages/solo/doc/accumulation.md:581`) это прямо и объясняет: «a job of
another kind added between two commands is a boundary and the merging stops
there. That is what keeps the order with the rest of the work;
`AccumulationPolicy.join` gives it up».

Отсюда решение: **рецепт команд получает явный
`policy: AccumulationPolicy.adjacent`** и сторож на порядок в стенде. Сторожа
на порядок у `commands.dart` сегодня нет — поэтому правка прошла бы мимо него
молча.

И отсюда же — фраза для `CHANGELOG`, которой в первой редакции не было:
`adjacent` остаётся нужен там, где `merge` выбрасывает накопленное, а чужая
задача между событиями обязана исполниться между ними.

### Почему глобальное умолчание, а не условное от `timing`

Три довода, в порядке прочности.

**Первый: условное умолчание не записывается умолчанием, не заводя нового
значения.** Значение по умолчанию в сигнатуре — константа; «`join`, если задан
`timing`» ею не выражается. `policy` пришлось бы сделать `AccumulationPolicy?`
с разбором внутри, и тогда читатель сигнатуры не узнаёт правило вообще:
в параметре стоит `null`, а поведение спрятано в теле.

Обойти это можно ровно одним способом — значением-часовым в самом перечислении,
`AccumulationPolicy.auto`. Оно константа, стоит в сигнатуре, и dartdoc значения
называет правило. Цена своя: в перечислении правил появляется не-правило,
и всякий `switch` пользователя по `AccumulationPolicy` обязан его учитывать.
А второй довод бьёт по часовому целиком, поэтому он отвергнут, а не пропущен.

**Второй: `timing:` перестал бы быть добавкой.** Дописывая `timing:` к готовому
накопителю, автор меняет момент готовности группы. При условном умолчании он
вдобавок молча меняет правило — и, как показывает рецепт команд выше, это может
стоить потерянной команды. В его правке об этом не будет ни слова.

**Третий: нулевая длительность.** Dartdoc обоих конструкторов
`AccumulationTiming` обещает: «A zero duration preserves the behavior of an
accumulator without timing». Обещание держится, это замерено: при нулевой
длительности все три правила дают ровно то же, что без тайминга, в обоих видах
тайминга. Условное умолчание с ключом `timing != null` это обещание ломает.

Третий довод — пример, а не основание: условное умолчание с ключом
`duration != Duration.zero` обещания не нарушает, но остаётся негодным
по первым двум. В первой редакции этот раздел был построен на третьем доводе;
круг ревью показал, что он бьёт только по одному из двух возможных условных
умолчаний.

### Цена

Правка ломающая.

В `packages/` 71 место вызова `collect`/`accumulate`: 65 в тестах
`packages/solo` и 6 в документах; плюс два объявления в `solo_base.dart`.
В `flutter_solo` и в обоих примерах вызовов **ноль** — гейт на этих пакетах про
правку A не говорит ничего, и ссылаться на него как на проверку нельзя.

Из 65 тестовых мест на умолчании стоят 42. Прогон с глобальным `join`: краснеют
**3 теста из 526**, и все три написаны про `adjacent`, просто не называя его:

- `negative timing is rejected and zero preserves queue order` — ждёт
  `A[1], B, A[2]`, получает `A[1, 2], B`;
- `multiple pending throttle groups start one interval apart` — ждёт
  `200ms A[1]`, получает `200ms A[1, 2, 3]`;
- `removing a separator does not merge existing adjacent groups` — ждёт две
  задачи в очереди, получает одну.

Остальные 39 остаются зелёными, и это отдельная работа, а не удача. Один из них
зелен по неверной причине: тест
`${kind.name} adjacent reuses the tail after B has run`
в `packages/solo/test/accumulation_timing_lifecycle_test.dart` назван про
`adjacent`, правила не задаёт и зелен потому, что `join` тоже переиспользует
хвост. После правки он проверяет не то, что обещает именем.

Поэтому в работу входит разбор всех 42 мест по трём корзинам: **проверяют само
умолчание** (оставить как есть), **описывают поведение `adjacent`** (дописать
`policy:`), **безразличны к правилу** (не трогать). Правило разбора: место
опасно тогда и только тогда, когда между двумя добавлениями в очереди стоит
задача другого рода.

Стенд документации при одной правке A краснеет двумя файлами: `policies.dart`
(«adjacent: A2 is a job of its own, not A1's») и `settings.dart` («a reload
still queued ends the group under adjacent»).

У `policies.dart` краснеет **не** строка таблицы: у строк собственные
контроллеры уже есть, их строит `with_policy`
(`tool/accumulation_snippets.py:1106-1119`). Краснеет блок `threeCalls` («the
reference's three lines»), который висит на контроллере рецепта настроек.
Подставить туда `AdjacentSettingsController` нельзя: `with_policy` выбрасывает
дебаунс рецепта, и следующий сторож — «B, the second reload, took the slot
next» — краснеет, потому что без окна A1 берёт слот раньше B. Развилка: либо
заводить второй образец `adjacent`, сохраняющий дебаунс рецепта, либо оставить
блок на контроллере рецепта и переписать три его сторожа с печатной строкой под
`join`. **Выбран второй путь** — он проверен прогоном до конца, все пять
драйверов зелёные, и он честнее: блок демонстрирует именно то, что делает
рецепт.

У `settings.dart` краснеет блок «both halves» — два сторожа, `!identical`
становится `identical`, а `saved.length == 2` становится `1`; дальше файл идёт
до конца. Это фраза рецепта настроек про то, что два щелчка дают одну или две
записи смотря по очереди. После правки фраза становится неправдой, а сам рецепт
— детерминированным. То есть здесь правка не добавляет документу оговорку,
а убирает существующую.

### Что остаётся `adjacent`

Единственное правило, в котором чужая задача — граница группы. Нужно там, где
`merge` выбрасывает накопленное (рецепт команд) или где вклинившаяся задача
меняет смысл накопленного. Правило остаётся в перечислении, в справочнике
и в рецепте команд; теряет только место умолчания.

## Правка B: `AccumulationTiming.throttle(duration, startAtOnce: false)`

### Что сегодня

Throttle разрешает первой группе стартовать сразу: интервал заводится
в `_started`, то есть отсчитывается от старта группы, а до первого старта
таймера нет и `_throttleReady` истинно. На простаивающем накопителе это рвёт
всплеск.

### Замер

Четыре записи журнала, пять секунд тишины, ещё четыре; интервал 1 с. Три формы
всплеска:

```text
всплеск в одном синхронном проходе
  startAtOnce: true     0ms [e0, e1, e2, e3]
  startAtOnce: false    1000ms [e0, e1, e2, e3]
одна микрозадача после первой записи
  startAtOnce: true     0ms [e0]
                        1000ms [e1, e2, e3]
  startAtOnce: false    1000ms [e0, e1, e2, e3]
очередь занята переходом экрана
  startAtOnce: true     50ms [e0, e1, e2, e3]
  startAtOnce: false    1000ms [e0, e1, e2, e3]
```

Одной микрозадачи между первой записью и остальными хватает, чтобы пачка
разорвалась: два обращения вместо одного, и хвост опаздывает на целый интервал.
В приложении записи журнала почти никогда не пишутся одним синхронным проходом,
так что это рядовой случай, а не угловой.

### Механизм

Интервал заводится в двух местах: при старте группы, как сегодня, и при
**появлении** группы — и только там.

```dart
AccumulationTiming.throttle(this.duration, {bool startAtOnce = true})
```

Имя — решение владельца 2026-09-14. `leading` знакомо по Rx и lodash,
но в документах этого слова нет ни разу, а `startAtOnce` уже есть: справочник
открывает раздел фразой «Starting at once has a price on an idle accumulator».
Слово из документа переезжает в имя параметра, и объяснять параметр отдельно
не нужно — объяснение уже написано.

В `_SoloAccumulator.add`, на пути, где кандидата не нашлось и заводится новая
группа, после `group._accepted()` накопитель заводит интервал, если активного
нет. Готовность группы считается как раньше — `_owner._throttleReady`, — так
что группа стартует, когда интервал истёк, и `_started` заводит следующий.
Отдельного состояния не появляется: тот же один таймер на накопитель.

**«И только там» — это не оговорка, а исправление дефекта.** Первый прототип
заводил интервал в `_AccumulationGroup._accepted`, который зовётся на каждое
принятое событие: и на новой группе, и на слиянии `join`, и на переносе правки
C. Пока очередь свободна, разницы нет; при занятой видно (замер снят
прототипом, где флаг уже назван `startAtOnce`):

```text
очередь занята 0..1500, интервал 1 с, e0@0, e1@1200
  startAtOnce: true                 run [e0..e1] @1500
  trailing, интервал в _accepted    run [e0..e1] @2200
  trailing, интервал при появлении  run [e0..e1] @1500
события каждые 200 мс с 0 до 1400
  startAtOnce: true                 run [e0..e7] @1500
  trailing, интервал в _accepted    run [e0..e7] @2000
  trailing, интервал при появлении  run [e0..e7] @1500
```

Группа отстояла свой интервал к 1000 мс и ждала только слот — а событие на 1200
мс отодвигало её на 2200. Это прямо против строки таблицы справочника «An
addition … does not extend it» и против фразы рецепта журнала «the interval is
a floor under the rate, not a delay added to every entry». С правкой C дефект
пересекался напрямую: перенос тоже зовёт `_accepted`.

После исправления занятые сценарии совпадают с немедленным стартом, все прежние
замеры режима печатают то же, что и раньше, прогон даёт те же 34 красных,
`dart analyze` чист. Заодно `_armCooldown` перестаёт быть членом интерфейса
`_AccumulationOwner`: его зовёт сам накопитель, а не группа.

**Флаг остаётся приватным**, как `_kind`, и публичным геттером не становится.
`duration` публичен потому, что осмыслен для обоих видов тайминга;
`startAtOnce` осмыслен только для throttle,
и `AccumulationTiming.debounce(d).startAtOnce` читался бы как `true` ни о чём.
Если читать настройку обратно когда-нибудь понадобится, это отдельная правка
и пара с видом тайминга, а не одиночный геттер.

### Правила старта и трейлинг

Справочник обещает отдельной фразой (`packages/solo/doc/accumulation.md:662`):
«A group rejected by start rules consumes no throttle interval». Обещание
держится и при трейлинге — это замерено. Группа отклонена `canStart`, следующее
событие приходит на 300 мс:

```text
startAtOnce: true     первая группа Cancelled, вторая идёт в 300ms
startAtOnce: false    первая группа Cancelled, вторая идёт в 500ms
```

500 — это 300 плюс собственный полный интервал второй группы, то есть ровно то,
что режим обещает, а не наследство от отклонённой. Но фраза справочника
в нынешнем виде для трейлинга читается как «стартует сразу», и это неверно.
Справочник получает вторую половину фразы, а спека — критерий с этим замером.

### Почему не debounce

Debounce с той же длительностью тоже собрал бы пачку целиком, и вопрос «зачем
тогда trailing throttle» законный. Замер на входе, который не стихает —
двадцать событий по 200 мс:

```text
debounce 1s              1 группа: 4800ms, 20 записей
throttle 1s startAtOnce  5 групп: 0ms/1, 1000ms/4, 2000ms/5,
                         3000ms/5, 4000ms/5
throttle 1s trailing     4 группы: 1000ms/5, 2000ms/5,
                         3000ms/5, 4000ms/5
```

Debounce не публикует ничего 4,8 секунды: у него нет потолка. Leading throttle
потолок держит, но первая группа из одной записи. Trailing throttle — четыре
ровные группы по пять. Это и есть та комбинация, которой сегодня нет.

### Цена

Правка не ломающая: умолчание `startAtOnce: true` сохраняет нынешнее поведение,
и прогон это подтверждает — **526 из 526 зелёных**, стенд документации тоже.
Своя цена у самого режима, и её надо написать в справочнике: одиночное событие
на простаивающем накопителе ждёт целый интервал (`1000ms` против `0ms`).
Trailing throttle никогда не отдаёт рано.

## Правка C: `AccumulationPolicy.replace` переносит группу

### Что сегодня

Каждое принятое событие строит новую задачу и роняет прежнюю
`Cancelled(manual, 'replaced by accumulated group')` — даже когда данные
прежних событий доехали и группа отработает целиком.

### Замер

Пять событий в занятую очередь:

```text
сегодня        обработчик 1 раз: e0+e1+e2+e3+e4
               хэндлов 5 из 5
               исходы: cancelled ×4, done
с переносом    обработчик 1 раз: e0+e1+e2+e3+e4
               хэндлов 1 из 5
               исходы: done ×5
```

`cancellable: false` ничего не меняет ни до, ни после: отмена замены сегодня
проходит поверх защиты.

Что при этом видит наблюдатель:

```text
сегодня        8 событий: finish a Cancelled ×4, start busy,
               finish busy Done, start a, finish a Done
с переносом    4 события: start busy, finish busy Done,
               start a, finish a Done
```

Четыре отмены приходят **до** того, как хоть что-то стартовало.

### Механизм

Ветка `replace` в `_SoloAccumulator.add` сжимается: положить слитое значение
в ту же группу, снять задачу с её места в очереди, вставить в хвост, отметить
принятое событие, толкнуть насос. Возвращается та же задача.

Новой задачи не строится, старая не роняется, `_AccumulationGroup._release`
на этом пути не зовётся вовсе — а вместе с ним отпадает комментарий про буфер
`collect`, который нельзя чистить при передаче.

Одна строка добавляется: `SoloBase._debug(() => 'move $previous to the tail')`
сразу после вставки в хвост. Это решение владельца по открытому вопросу о следе
замены — см. «Что видно вместо отмен» ниже.

### Инварианты, на которых стоит перенос

Их три, и все три проверены по коду, а не выведены:

1. **Задача в `_jobs` всегда `created`.** `cancelWith` снимает задачу из списка
   прежде, чем завершить её (`packages/solo/lib/src/job.dart:78`), и все вызовы
   `_drop` идут после снятия. Значит `_candidate()`, который читает только
   `_jobs` (`accumulator.dart:80`), не вернёт завершающуюся задачу.
   **Открытость группы сюда не входит**: окно debounce запечатывает группу
   своим таймером, пока задача стоит в очереди, так что очередь спокойно держит
   `created`-задачу с закрытой группой — замерено, новое событие получает там
   другой хэндл, а в очереди становится две задачи. Открытость — фильтр
   `_candidate()` (`accumulator.dart:86`), а не свойство очереди, и переносится
   только то, что этот фильтр вернул.
2. **Насос снимает задачу и печатает группу синхронно, до правил.**
   `_takeFirst` делает `removeAt` и `_seal()` подряд (`queue.dart:105-112`),
   поэтому окна, в котором задача уже взята на исполнение, но её группа ещё
   открыта для слияния, не существует.
3. **После проверки тождества кандидата пользовательского кода нет.**
   `_accepted` трогает только таймеры, `_schedulePump` ставит микрозадачу.
   Пользовательский код на этом пути один — `merge`, и он вызывается **до**
   переноса, под флагом `_merging`, с перепроверкой закрытия контроллера
   и тождества кандидата после возврата (`accumulator.dart:122-129`).

Третий инвариант — исправление первой редакции. Там было написано, что `add`
на пути `replace` «вообще не зовёт пользовательский код»; это неверно, и,
прочитанное буквально, снесло бы защиту вокруг `merge`.

Четвёртого инварианта, без которого перенос ломался бы, нет: `_added`
у переносимой задачи уже стоит и не трогается, ключ тот же, так что
`Policy.replace`, `restart` и `droppable` видят её как прежде, `isClosed`
перепроверяется после `merge`, а дрейн новых событий не принимает.

Краевые случаи проверены зондом: защищённая группа переживает `clear()` и потом
переезжает; `Policy.restart` с тем же ключом снимает её как сегодня; открытая
группа не обгоняет запечатанную группу того же накопителя; ошибка `merge`
оставляет группу и её таймер на месте. `cancelAll`, `close(force)`,
`close(mode: drain)` и `add(first: true)` отдельного случая не создают —
перенесённая задача идёт по тем же путям, что любая очередная. Отдельно
проверяются правила старта: перенесённая группа, которой отказал `canStart`,
кончается тем же `RulesCancelReason`, что и сегодняшняя, а работающая группа,
потерявшая `keepWhile`, новым событием `replace` не задевается.

### Что отпадает вместе с отменой

- **Реентрантный узел.** Сегодня `add` обязан опубликовать новое состояние
  очереди до `_drop`, потому что колбэки отмены могут реентрантно добавить,
  очистить, отменить или закрыть. После правки этих колбэков на пути `replace`
  нет вовсе: отменять нечего. В прогоне это видно прямо: из 31 краснеющего
  теста **23 висят на `whenCancelled` или `onFinish` прежнего хэндла** —
  шестнадцать из семейства `onFinish/whenCancelled does add/cancel/clear/close`
  и ещё семь в `accumulation_timing_lifecycle_test`
  (`publishes before $hook re-enters add` ×2,
  `lets $hook $action the published group` ×4,
  `throttle replace re-entry keeps the running cooldown`). У всех двадцати трёх
  исчезает спусковой крючок.
- **Особый случай «замена отменяет и защищённую задачу»**, который сегодня
  приходится писать и в dartdoc `AccumulationPolicy.replace`, и в тесте
  `replacement cannot cancel a running protected group`.
- **Двойственность отчёта.** При `join` каллер, чьё событие `merge` выбросила,
  получает `Done`: движок отчитывается за группу, а не за событие. `replace` —
  единственное исключение, и в обратную сторону. После правки исключения нет.
- **Одноосная таблица правил.** Замер после всех трёх правок: при любом правиле
  события одной группы делят один хэндл. Правила различаются только тем, какая
  группа принимает событие и где она стоит: `adjacent` — только хвост очереди,
  группа на месте; `join` — последняя подходящая, группа на месте; `replace` —
  последняя подходящая, группа уезжает в хвост.

Оговорка про двадцать три, исправленная во второй раз. У двадцати из них
предмет уходит **вместе** с носителем, а не переезжает: опубликованное
состояние очереди до колбэков отмены и колбэк, отменяющий или закрывающий
преемника, — это свойства узла, которого не станет. Придумывать им новый
спусковой крючок нельзя: единственный оставшийся колбэк — `onFinish` доехавшей
группы, а `add` из него открывает обычную новую группу и узла не касается.

Переезжают ровно два свойства, и оба про таймеры: перенос не сбрасывает бегущий
интервал throttle (второе утверждение теста
`throttle replace re-entry keeps the running cooldown`) и заводит свежее окно
debounce. Им нужны сторожа неизменности; остальному — нет.

### Цена

Правка ломающая для кода, который читает эту отмену как «моё вытеснили».

**Тесты: 31 из 526**, все про `replace`, и ни один — про то, какие события
попали в группу или когда она отработала. Все падения одной формы: хэндл тот же
вместо другого, не отменён вместо отменённого, колбэк отмены не позвался.
Двадцать три из них уходят вместе с узлом, восемь переписываются.

**Стенд документации правку ловит.** Прогон с одной правкой C:

```text
commands.dart   ok
logs.dart       ok
policies.dart   RED  Bad state: replace: A2 is a new job
search.dart     ok
settings.dart   ok
```

Краснеют четыре сторожа строки `replace` в `tool/accumulation_snippets.py` —
«A2 is a new job», «A1's job is cancelled», «and it says what replaced it»,
«the replaced group had not started» — и меняется печатная строка таблицы
`| replace | … | a new job; A1 … |`. При всех трёх правках `policies.dart`
гибнет раньше, на сторожe `adjacent`, поэтому в прогоне волны сторожей
`replace` не видно вовсе; это артефакт раннего выхода, а не признак того, что
правка их не трогает.

**Наблюдатель** — настоящая цена. Восемь событий против четырёх: сегодня
по трассе видно, что замены были, после правки не видно ничего. Перенос
молчалив и по отношению к ожидающему: тот, кто держит хэндл, не узнает, что
группу подвинули в хвост, — но он и сегодня об этом не узнаёт, он узнаёт
только, что его задачу отменили.

### Что видно вместо отмен

Решение владельца 2026-09-14: след уходит в `SoloBase.debug`, трассировку
движка, куда `add` уже пишет `add $impl`, а `cancelWith` — `remove $this`.
Перенос — событие очереди того же рода, и живёт оно рядом с ними. Публичный API
не меняется вовсе: ни нового хука у `SoloObserver`, ни записи
в пользовательский журнал задачи через `onLog`.

Замер прототипа, пять событий в занятую очередь:

```text
add Job(busy)
add Job(a)
move Job(a) to the tail
move Job(a) to the tail
move Job(a) to the tail
move Job(a) to the tail
queue has no ready jobs
```

Четыре переноса ровно там, где сегодня наблюдатель видит четыре отмены. Кто
отлаживает движок — видит их по-прежнему; кто смотрит за задачами — перестаёт
видеть события задач, которых больше не создаётся.

## Три вместе

По отдельности краснеют 3, 0 и 31 тест, все три вместе — **34 из 526**, ровно
сумма: **ни один из 526 тестов не краснеет только от сочетания**. Это всё, что
доказано; утверждать «правки не пересекаются» шире замеренного, и первая
редакция это делала зря. Общие точки у правок есть: умолчание A меняет правило
у накопителей с throttle без явного `policy`, B правит путь `_accepted`,
которым пользуется C, нулевая длительность относится и к A, и к B, а C обязан
сохранить бегущий интервал throttle при переносе. Ни одно из этих сочетаний
нынешними тестами не покрыто, и они идут в критерии.

Для стенда документации непересечения нет и не доказывалось: `policies.dart`
краснеет и от A, и от C, и при волне виден только первый отказ.

## Цена решения целиком

- **API.** Одна ломающая правка умолчания (A), одна ломающая правка семантики
  (C), одно необязательное расширение (B).
- **Тесты.** 34 падения: 23 удаляются вместе с исчезнувшим узлом, 11 правятся.
  Из предмета удаляемых переезжают два свойства таймеров, остальное уходит
  вместе с узлом. Плюс разбор 42 мест на умолчании и починка теста, зелёного
  по неверной причине.
- **Стенд.** Пять позиций: блок `threeCalls` в `policies.dart` переписывается
  под `join`; четыре сторожа строки `replace` и её печатная строка; два сторожа
  блока «both halves» в `settings.dart`; сторож на порядок в `commands.dart`,
  которого сегодня нет; сторожа к новым утверждениям справочника про трейлинг.
- **Документы.** Поимённо — ниже.

## Что переписывается в документах

По правке **C** в `packages/solo/doc/accumulation.md` теряют правду восемь
утверждений: про новый хэндл даже когда группа уже в хвосте; про `Cancelled`
прежней задачи с `started: false` и описанием; про то, что новая группа
в очереди раньше, чем отработают колбэки прежней, и что возвращённый хэндл
может быть уже отменён; про обязательность замены, включая
`cancellable: false`; строка таблицы `| replace | … |`; про то, что отмена
прежнего хэндла не отменяет преемника — после правки утверждение обратное; про
«даже `replace` сохраняет старую задачу»; и фраза рецепта команд про форму
`AccumulationPolicy.replace`. Плюс dartdoc `AccumulationPolicy.replace`
и `SoloAccumulator.add`.

По правке **B**: строка таблицы «The first group is ready … at once» —
и не только строка, а форма таблицы: у неё один столбец `throttle(duration)`,
а режимов становится два. Дальше: dartdoc класса `AccumulationTiming` («A
throttle interval starts when a group actually starts»), вторая половина фразы
про отказ правил старта, новый абзац про цену режима и фраза о том, что
добавление не продлевает интервал, — она остаётся верной, но теперь её надо
прочитать и для трейлинга.

По правке **A**, и это длиннее, чем «10 упоминаний `adjacent`»:

- таблица правил и её подпись;
- фраза рецепта настроек про очередь — уходит;
- абзац рецепта команд про границу — остаётся, а рецепт получает явный
  `policy: AccumulationPolicy.adjacent`;
- два абзаца справочника про выбор `join`: первый объясняет выбор, который стал
  умолчанием, второй уговаривает на умолчание, которое уже действует, —
  **решение: оба переписываются**, первый в «почему это умолчание», второй
  снимается;
- явный `policy: AccumulationPolicy.join` в рецептах поиска и журнала и фраза
  «The policy section explains why this example chooses `join`» — **решение:
  явное правило в рецептах остаётся** (рецепт читают отдельно от справочника),
  а фраза переписывается под «почему умолчание такое»;
- dartdoc `collect` и `accumulate`;
- накопитель `_volume` в `README.md` и `README.ru.md` — **решение: остаётся
  на умолчании**, вызовов между событиями там нет, и рецепт от правила
  не зависит.

Каждый из этих абзацев — в русском переводе тоже; сверка
`tool/check_translations.py`.

## Критерии приёмки

### Новые тесты: обязаны краснеть до правки

1. `collect` и `accumulate` без `policy` дают `join`: событие A1, чужая задача
   B, событие A2, очередь занята — **один хэндл, один запуск, порядок
   `[A1+A2, B]`**.
2. Тот же исход у вызова без `policy` и с нулевым таймингом — по отдельности
   для `debounce(Duration.zero)` и `throttle(Duration.zero)`: один хэндл,
   порядок `[A1+A2, B]`. Сравнения вызовов друг с другом недостаточно:
   до правки они совпадают тоже.
3. `throttle(d, startAtOnce: false)`: всплеск с микрозадачей после первой
   записи даёт одну группу через `d`, а не две.
4. `throttle(d, startAtOnce: false)`: одиночное событие ждёт `d` — режим
   не отдаёт рано, и это его контракт, а не дефект.
5. `throttle(d, startAtOnce: false)` на входе, который не стихает, держит
   потолок: группы ровно через `d`, в отличие от debounce.
6. `throttle(d, startAtOnce: false)`: группа, чей интервал уже истёк и которая
   ждёт только слота, **не откладывается новым событием**. Очередь занята
   `0..1500`, `d` = 1 с, события на 0 и 1200 — группа идёт на 1500,
   а не на 2200.
7. `throttle(d, startAtOnce: false)` под `close(mode: drain)`: группа стартует
   по истечении интервала и закрытие её дожидается.
8. `throttle(d, startAtOnce: false)` и отказ `canStart`: отклонённая группа
   интервал не расходует, следующая идёт через свой полный `d` от своего `add`.
9. `replace`: пять событий в занятую очередь дают один хэндл, и он
   заканчивается `Done`.
10. `replace` с `cancellable: false`: тот же один хэндл, отмены нет.
11. `replace`, **очередь занята**: наблюдатель на пяти событиях видит ровно
    четыре события в порядке
    `start busy, finish busy Done, start a, finish a Done`. Без занятой очереди
    сценария нет: все пять добавлений успевают до микрозадачи насоса, группа
    одна, и событий два.
12. `replace` после `clear()`: событие, пришедшее следом, получает **тот же**
    хэндл, а не новый.
13. `replace`, когда группа уже в хвосте: событие получает тот же хэндл.
14. Совместные сценарии, с трассами, а не перечнем: - умолчание `join`
    с trailing throttle: два события вокруг чужой задачи, очередь занята — одна
    группа, один хэндл, старт по истечении интервала; - `replace` с trailing
    throttle: перенос не заводит новый интервал группе, которая уже ждёт слота,
    — та же трасса, что в критерии 6; - `throttle(Duration.zero)` при обоих
    значениях `startAtOnce`: группа готова сразу, как без тайминга.

### Сторожа неизменности: зелёные до правки и после

15. Свободная очередь: правило не влияет ни на что — все три дают
    `run[a1]@0, other@0, run[a2]@50`.
16. Нулевая длительность не меняет правило **при явно заданном `policy`** — для
    `debounce(Duration.zero)`, `throttle(Duration.zero)`
    и `throttle(Duration.zero, startAtOnce: false)`.
17. `throttle` с умолчанием `startAtOnce: true` ведёт себя как сегодня во всех
    трёх формах всплеска.
18. `adjacent`, заданный явно, работает как сегодня: чужая задача — граница.
    Отдельно — рецепт команд: `resume`, чужой `seek`, `pause` дают
    `[resume, seek, pause]`, а не `[pause, seek]`.
19. Группировка и моменты запуска при `replace` не меняются: те же события
    в той же группе в тот же момент, меняются только хэндл и исход.
20. Таймеры при переносе: бегущий интервал throttle не сбрасывается, окно
    debounce заводится свежим. Это всё, что переезжает из предмета двадцати
    трёх уходящих тестов.
21. Краевые случаи переноса, каждый как сегодня: `Policy.restart` с тем же
    ключом снимает группу; открытая группа не обгоняет запечатанную группу
    того же накопителя; ошибка `merge` оставляет группу и её таймер на месте;
    `clear()` не трогает защищённую группу; `cancelAll`, `close(force)`,
    `close(mode: drain)` и `add(first: true)` ведут себя как с любой очередной
    задачей; отказ `canStart` даёт `RulesCancelReason`; потеря `keepWhile`
    работающей группой новым событием не задевается.

### Работа по местам вызова

22. Все 42 места на умолчании в тестах разобраны по трём корзинам: проверяют
    само умолчание, описывают `adjacent` (дописать `policy:`), безразличны.
    Правило: место опасно тогда и только тогда, когда между двумя добавлениями
    в очереди стоит задача другого рода.
23. Тест `${kind.name} adjacent reuses the tail after B has run`
    в `accumulation_timing_lifecycle_test.dart` перестаёт быть зелёным
    по неверной причине: либо получает `policy: AccumulationPolicy.adjacent`
    и остаётся тестом про `adjacent`, либо переименовывается.

### Документы

24. Все абзацы из раздела «Что переписывается в документах» — английский
    оригинал и русский перевод, `check_translations.py` зелёный.
25. `CHANGELOG.md`: три записи в существующем разделе `Unreleased`, рядом
    с `externalSetState` и `isFinished`; две помечены **Breaking**. Запись про
    A называет, когда оставаться на `adjacent`: `merge` выбрасывает
    накопленное, а чужая задача между событиями обязана исполниться между ними.
26. Стенд `tool/accumulation_snippets.py`, пять позиций из раздела «Цена
    решения целиком». К каждому новому утверждению документа — сторож,
    и к каждому сторожу — мутация, проверенная в собранном файле до прогона.
27. Каждый драйвер стенда прогоняется **до конца**, а не до первого отказа:
    сторожа чинятся по одному, и счёт ведётся по починенным, а не по прогону.

### Гейт

28. `dart analyze` и `dart test` зелёные в `solo`, `async_job`, `flutter_solo`
    и в обоих примерах; оба стенда документации зелёные; `check_traces.py`,
    `check_doc_shape.py`, `check_translations.py`, `check_line_width.py`,
    `reflow.py --check` и сборка сайта зелёные.

## Решения владельца 2026-09-14

Открытых вопросов у спеки не осталось: все три закрыты и внесены в текст.

1. **След замены** (правка C) — `SoloBase.debug`. Одна строка в трассировку
   движка, рядом с `add` и `remove`, которые туда уже пишут. Публичный API
   не растёт: ни хука у `SoloObserver`, ни `onLog`. Разобрано в разделе «Что
   видно вместо отмен», замер там же.
2. **Имя параметра** (правка B) — `startAtOnce: false`, а не `leading: false`.
   Слово взято из справочника, где уже стоит «Starting at once has a price on
   an idle accumulator»; `leading` в документах не встречается ни разу. Флаг
   остаётся приватным полем, как `_kind`.
3. **Выпуск** — тот же, что и накопленное в `Unreleased`. Две ломающие правки
   волны ложатся рядом с `externalSetState` и `isFinished`: пакет до 1.0, цена
   ломающего выпуска уже заплачена, и пользователь мигрирует один раз,
   а не два. Это решение про записи в `CHANGELOG`; сама публикация — отдельное
   поручение владельца и в спеку не входит.

Прототип приведён к этим решениям и перезамерен целиком: флаг переименован,
строка трассировки на месте, `dart analyze` чист, красных те же 34, все прежние
трассы печатают то же, что печатали. Диф в `.artifacts/` обновлён.
