# Состояние

`Solo<S>` хранит одно неизменяемое состояние типа `S`, а `Job` объявляет,
в каких состояниях ей позволено начаться и в каких продолжаться. Четыре раздела
ниже открываются версией, к которой ведёт словарь самого API, — той, что
пишется, когда взяли `run` и на этом остановились, — и говорят, что она делает
вместо задуманного. Работающая версия идёт следом, под своим заголовком.

## Состояние и правила

Записи нужно свободное место, чтобы начаться, и она должна остановиться, когда
камеру поставили на паузу.

### Первая попытка

```dart
Job<void> record() => run<CameraState, void>(
      key: 'record',
      (ctx) async {
        final state = ctx.state;
        if (state is! Ready || state.paused) return;
        await for (final frame in device.frames) {
          await store(frame);
        }
      },
    );
```

Проверка читается хорошо и выполняется один раз. Вернуться ей неоткуда: когда
чужое обновление ставит `paused`, тело уже внутри цикла, а цикл —
не контрольная точка состояния. Кадр, пришедший до паузы, и два пришедших после
неё сохраняются одинаково.

Задача при этом и не кончается. `await for` контрольной точкой не является,
поэтому отмена, которую посылает `close`, до тела не доходит, и закрытие
контроллера ждёт, пока не кончится сам стрим.

### Правила

```dart
Job<void> record() => run<Ready, void>(
      key: 'record',
      canStart: (state) => state.free > 0,
      keepWhile: (state) => !state.paused,
      (ctx) => ctx.each(
        device.frames,
        (child, frame) => child.join(() => store(frame)),
      ).value,
    );
```

Правила — это параметры, и движок проверяет их за задачу. Рабочий тип сужается
с `CameraState` до `Ready`, поэтому тело читает то состояние, которое ему
нужно, а условие, которое первая попытка проверяла руками, стало `keepWhile`.

Первый аргумент типа у `run<W, T>` задаёт рабочий тип состояния `Job`,
`W extends S`. Его проверяет движок, поэтому `run<Ready, void>` начинается
только в `Ready`; по той же причине тело читает `ctx.state` как `W`. Когда
чужое обновление состояния устанавливает `paused` в `true`, `keepWhile`
отменяет запись; повторять это условие телу не нужно. Здесь `Ready`, `device`
и `store` принадлежат приложению, а `ctx.each` обрабатывает стрим — его
жизненный цикл разобран в [Дочерние задачи и стримы](children.md).

Используйте отдельные классы, когда состояния разрешают разные операции,
и общие базовые типы, когда операция может проходить через несколько состояний.

| Правило | Когда проверяется |
| --- | --- |
| Рабочий тип `W` | Перед стартом, при чужих обновлениях состояния во время тела и в контрольных точках состояния. |
| `canStart` | Один раз, непосредственно перед стартом `Job`. |
| `keepWhile` | Перед стартом, при чужих обновлениях состояния во время тела и в контрольных точках состояния. |

Любое из этих правил отменяет `Job` с `RulesCancelReason`; различается
`started`. Отказ на старте заканчивает `Job` с `started: false`, а нарушение
`W` или `keepWhile` во время тела — с `started: true`. Эти правила отклоняют
неподходящую работу; они не оставляют её в очереди до наступления подходящего
состояния.

### Чтение и изменение состояния

```dart
Job<void> zoomIn() => run<Ready, void>(
      (ctx) async {
        // Чтение — контрольная точка: проверяются отмена и правила.
        final state = ctx.state;

        // Единственная запись, и она синхронная.
        ctx.emit(Recording(free: state.free, zoom: state.zoom + 1));
      },
    );
```

`ctx.state`, `ctx.stateAs<T>()` и `ctx.check()` проверяют отмену и правила
состояния. Тело выше — `run<Ready, void>`, поэтому `ctx.state` в нём уже
`Ready`; `stateAs<T>()` нужен телу, у которого `W` шире, чем состояние,
требуемое в этот момент: он требует тип `T`, а несовпадение отменяет `Job`,
вместо того чтобы вернуть `null`. Методы ожидания также используют контрольные
точки состояния.

`check()` — та же проверка, только без значения для чтения. Другие методы
контекста проверяют себя сами; `check()` закрывает промежутки между ними. Голый
`await` и возврат из `ctx.uncancellable` не проверяют ничего: задача, которую
правила уже отменили, идёт дальше, пока кто-нибудь внутри не спросит:
а можем ли продолжать? Эти промежутки разобраны в [Отмене](cancellation.md) —
там `check()` стоит рядом с методами ожидания, которым он сопутствует.

`ctx.emit(next)` позволяет `Job` опубликовать состояние за пределами своего
рабочего типа: например, инициализация может закончиться публикацией `Ready`.
Следующая контрольная точка отклонит такое состояние, если оно не соответствует
`W`, поэтому такой переход должен быть последним шагом тела, которому нужен
доступ к состоянию. `canStart` проверяется один раз, при запуске, и больше
не возвращается — этот `emit` тоже его не приносит.

После обновления проверяются другие работающие тела. Собственный `emit` `Job`
исключён из этой проверки правил, но всё равно проверяет отмену до и после
записи. И `onChange` (см. [Ошибки](errors.md)), и слушатель выполняются
синхронно внутри самой записи, и у обоих нет `ctx`: единственная доступная
запись — [`externalSetState`](#externalsetstate). Хук, который так поправляет
состояние — превращая `Preparing` одной задачи прямо в `Working` — может тем
самым отменить излучающую `Job` до возврата из её собственного `emit`.

Правила перестают отменять `Job` после завершения её тела. Ручная отмена,
закрытие контроллера и отмена родителя по-прежнему могут достичь её при
ожидании детей или освобождении ресурсов. `Job` с обработчиками состояния также
продолжают проверять, разрешено ли этим обработчикам менять состояние; см.
[Состояние после ошибки или отмены](#состояние-после-ошибки-или-отмены).

### Наблюдение за состоянием

```dart
// Чтение в любой момент.
print(camera.currentState);

// Каждое изменение, синхронно, внутри самого изменения. Новое состояние
// слушатель читает сам, поэтому устареть прочитанному негде.
camera.addListener(() => print(camera.currentState));

// Каждое обновление, по порядку, через микротаску. Начальное состояние
// не повторяется, а публикация равного состояния тоже даёт событие.
// Нужен `with SoloStream` у контроллера — см. таблицу ниже.
final subscription = camera.stream.listen(print);
```

Любой код с доступом к контроллеру может читать `currentState`. К моменту
доставки события стрима `currentState` может содержать более новое значение. Он
намеренно назван не `state`: тело задачи видит все члены своего контроллера без
префикса, и чтение без чекпойнта выглядело бы там в точности как проверяющий
`ctx.state`.

| Тип | Что предоставляет |
| --- | --- |
| `Solo<S>` | Состояние, `Job`, очередь, правила и слушателей. |
| `Solo<S> with SoloStream` | Всё из `Solo` и broadcast-стрим `stream`. |
| `Solo<S> with SoloListenable` | Всё из `Solo` и интерфейс Flutter `ValueListenable<S>`. |

`addListener` — собственный член движка, он на самом `Solo`, поэтому сказанное
ниже верно для любого контроллера: и со своей доставкой, и без всякой.
Слушателей зовут синхронно, в порядке регистрации, внутри самого изменения:
после записи состояния, но раньше, чем движок переоценит правила бегущих задач,
и раньше следующей строки того кода, который изменил состояние.

Изменению, сделанному изнутри слушателя, этот порядок не достаётся. Оно встаёт
в конец очереди публикации, а не влезает вперёд:

```text
externalSetState(B)
    listeners of B
        externalSetState(C)        // слушатель пишет ещё раз
            rules, against C       // запись C спрашивает их сразу
        next line of the listener
    listeners of C                 // очередь дошла до C только здесь
    rules, against C               // запись B спрашивает их последней, о C
next line of the writer
```

То есть за вложенное изменение правила отвечают раньше, чем его писатель пойдёт
дальше, а слушатели узнают о нём последними. До переоценки как таковое внешнее
изменение не доходит вовсе: когда она запускается, спрашивают уже о вложенном.
Задачу со своими обработчиками состояния о B всё же спрашивают — в самой
записи, раньше любого слушателя, и не чтобы отменить, а чтобы решить, смогут ли
её обработчики ещё поправить состояние. B, которое её правила отвергли,
отключает их насовсем — как любое несовместимое внешнее обновление в разделе
[Допуск обработчиков и их ошибки](#допуск-обработчиков-и-их-ошибки).

Второй вопрос о C — единственный, который задаётся после слушателей C: правило,
которое читает только состояние, отвечает на него так же, как на первый,
а задачу, отменённую первым ответом, второй раз не спрашивают. `keepWhile`,
который читает ещё и поле контроллера, увидит записанное туда слушателями
только во втором.

Как и у `ChangeNotifier`, `removeListener` сравнивает через `==`,
а не по идентичности: виджет подписывается в `initState` и отписывается
в `dispose` своим же методом. Бросивший слушатель не останавливает
ни слушателей после себя, ни переоценку правил следом: его ошибка уходит
в `onListenerError`, а тот отдаёт её зоне, если наследник не решит иначе.

Движок сбрасывает слушателей насовсем, когда закрытие закончилось, а не когда
вызван `close()`, — в тот же момент `isFinished` становится истиной. После
этого `addListener` ничего не делает: слушатель не регистрируется
и не хранится, ошибки тоже нет. На той же черте останавливается и состояние:
`externalSetState` за ней бросает `StateError`.

`SoloListenable` добавляет к этому интерфейс Flutter `ValueListenable` и больше
ничего. Это миксин на `Solo`, как и `SoloStream`: виджет перестраивается
по `value`, а контроллер, которому нужны обе доставки, подмешивает оба.

### Своя доставка

Слушатели выше — движковые, и обычный случай они закрывают. `publish` остаётся
швом для доставки другого рода: стрима, сигнала, строки в логе.

```dart
/// Контроллер, записывающий каждое изменение в лог.
class Logged<S extends Object> extends Solo<S> {
  final void Function(String line) write;

  Logged(super.initialState, this.write);

  @override
  void publish(S previous, S current) {
    super.publish(previous, current);
    try {
      write('$previous -> $current');
    } on Object catch (error, stackTrace) {
      // Сразу после этого вызова движок переоценивает правила бегущих
      // задач. Ошибка, выпущенная отсюда, стоила бы задаче отмены,
      // которую ей должно новое состояние.
      Zone.current.handleUncaughtError(error, stackTrace);
    }
  }
}
```

Что должно такое переопределение — и что движок должен ему:

| Что должно | Почему |
| --- | --- |
| `super.publish` идёт первым | Это он зовёт слушателей, поэтому своя доставка идёт после движковой, а не вместо неё. Так сказано аннотацией `@mustCallSuper`, и анализатор за этим следит. |
| Ошибка не должна выходить из `publish` | Сразу после вызова переоцениваются правила бегущих задач, и выпущенная отсюда ошибка стоила бы задаче отмены, которую ей должно новое состояние. Уходит она туда же, откуда пришла запись: в тело, позвавшее `ctx.emit`; из `externalSetState` — тому, кто его позвал; в `Zone.current.handleUncaughtError` из `dart:async`, когда этот вызывающий — хук движка, куда ошибка упавшего хука уходит и так. |
| Изменения за ней публикуются всё равно | Упавшее изменение потеряно — оно ушло из очереди до вызова, а дважды не публикуется ничто, — но стоящие за ним и есть состояние, которое контроллер уже держит, и они уходят до того, как ошибка выйдет наружу. Бросается только первая ошибка прохода, остальные уходят в зону. |

`SoloStream` — это такое переопределение с broadcast-контроллером стрима
за ним, а `SoloListenable` — движковые слушатели плюс интерфейс Flutter
`ValueListenable`, из-за которого контроллер понимают билдеры
и `Listenable.merge`.

## Внешнее состояние

Независимый источник — устройство, сокет — меняется, не дожидаясь контроллера,
а контроллеру надо показать то, что там уже произошло.

### Первая попытка

```dart
Camera(this.device) : super(const Ready()) {
  _link = device.connection.listen((connected) {
    if (!connected) {
      // Факт отправляется как обычная работа, впереди всех ожидающих.
      add(
        job<Ready, void>(
          key: 'disconnect',
          (ctx) async => ctx.emit(const Disconnected()),
        ),
        first: true,
      );
    }
  });
}
```

`first: true` ставит обновление впереди всего, что ждёт, но не впереди
работающей задачи: очередь выполняет задачи по одной. Пока обновление
не началось, контроллер сообщает о подключённом состоянии, а правила работающей
задачи спрашивают при каждом изменении — но только о том состоянии, которое она
и так знала. Хуже того, сама она может ждать ответа, которого устройство уже
не даст, — и обновление, которое её освободило бы, стоит в очереди за ней же.

### externalSetState

```dart
final class Camera extends Solo<CameraState> with SoloStream {
  final Device device;
  late final StreamSubscription<bool> _link;

  Camera(this.device) : super(const Ready()) {
    _link = device.connection.listen((connected) {
      // Устройство уже отключилось: отражаем факт немедленно, а не
      // ставим Job, которая ждала бы за текущей. isFinished ложен всё
      // время, пока движок работает, включая дренаж, и истинен, когда
      // состояние стало окончательным.
      if (!connected && !isFinished) {
        externalSetState(const Disconnected());
      }
    });
  }

  // Вызывается один раз, когда последняя Job закончилась: в очереди
  // больше некому слышать устройство.
  @override
  void onClose() => unawaited(_link.cancel());
}
```

Метод помечен `@protected` и вызывается внутри наследника контроллера, обычно
из подписки, которую тот держит, — из той самой подписки на устройство,
а не из слушателя контроллера.

`externalSetState` обновляет состояние немедленно и переоценивает работающие
`Job`, поэтому `Job`, правилам которой нужно подключение, отменяется самим
отключением. Её метод ожидания определяет, когда возобновится тело и должна ли
завершиться сама операция; очередь по-прежнему ждёт тела и уборки `Job` перед
запуском следующей корневой.

Что делать с уведомлением — отразить через `externalSetState` или поставить
на него `Job`, — решает его смысл. Если оно говорит, что независимая сущность
уже изменилась, отражайте этот факт немедленно. Если оно просит контроллер
выполнить работу — обновить данные, сохранить пришедшее значение, — ставьте
обычную `Job`. Доставка события стримом сама по себе не оправдывает обход
очереди.

Подписка гасится в `onClose`: движок вызывает его один раз, когда все задачи
закончились, а состояние ещё не окончательное, каким бы режимом контроллер
ни закрывали. Погасить источник в момент вызова `close` выглядит опрятнее,
и это ничего не стоит только пока от этого факта не зависит ни одна работающая
задача. При `SoloCloseMode.drain` это стоит самого дренажа: очередь работает
и после вызова, а её задачам знать об отключении устройства нужнее всех.
`SoloCloseMode.cancel` от этого тоже не спасает: задачу `cancellable: false`,
которая ждёт ответа, а устройство его уже не даст, освобождает именно
отключение, — и `close` ждёт эту задачу. Колбэк подписки выше синхронный,
поэтому между проверкой `isFinished` и записью ничего не успевает произойти,
а после `onClose` он не вызывается вовсе. Колбэк, которому перед записью нужно
чего-нибудь дождаться, должен проверить `isFinished` после последнего `await`,
прямо перед `externalSetState`, так как проверка, сделанная раньше, может
устареть, пока он ждёт, — движок успеет закончить, и запись бросит
`StateError`.

Для собственных успеха, ошибки и отмены контроллера используйте тела `Job` и их
обработчики состояния. `externalSetState` — исключение для внешних фактов,
а не универсальный сеттер для этих операций. Он работает ровно столько, сколько
работает движок, включая дренаж, и бросает `StateError`, когда закрытие
закончилось: состояние, на котором контроллер остановился, — это состояние,
которое он хранит.

## Состояние после ошибки или отмены

Загрузка публикует `Loading` до того, как начать ждать. Что-то должно вывести
контроллер из этого состояния, когда загрузка не пришла.

### Первая попытка

```dart
run<ProfileState, String>(
  (ctx) async {
    ctx.emit(const Loading());
    try {
      final name = await ctx.wait(api.fetchName);
      ctx.emit(Loaded(name));
      return name;
    } on Object {
      ctx.emit(const Initial());
      rethrow;
    }
  },
);
```

При ошибке это работает: `catch` записывает `Initial` и передаёт ошибку дальше.
При отмене `catch` тоже срабатывает, но `emit` внутри него оказывается
контрольной точкой состояния на уже отменённой задаче и бросает вместо того,
чтобы записать. Отмените загрузку — и контроллер останется в `Loading`
навсегда: экран крутит спиннер над работой, которой уже нет.

### Обработчики

```dart
run<ProfileState, String>(
  // Оба только вычисляют состояние. Они выполняются после тела, детей
  // и уборки, и исход к этому моменту уже определён.
  onError: (state, error, stackTrace) => Failure(error),
  onCancel: (state, cancelled) => const Initial(),
  (ctx) async {
    ctx.emit(const Loading());
    return ctx.wait(api.fetchName);
  },
);
```

Параметры `onError` и `onCancel` у `run` и `job` позволяют контроллеру покинуть
временное состояние вроде `Loading`, когда операция завершилась ошибкой или
была отменена. Они получают текущее состояние типа `S`, плюс ошибку со стеком
вызовов или исход `Cancelled`, и синхронно возвращают следующее состояние.

Подходящий обработчик выполняется перед следующей `Job` из очереди и перед
хуком `onFinish`. Изменение состояния не превращает `Failed` или `Cancelled`
в `Done` и не помечает исход обработанным для сообщений об ошибках.

### Сохранение несовместимого внешнего состояния

Обработчик выше возвращает `Initial` при любой отмене загрузки. Расширим
состояния профиля из быстрого старта классом `Disconnected` в той же
библиотеке — и связь теперь может пропасть, пока загрузка идёт.

#### Первая попытка

```dart
final class Disconnected extends ProfileState {
  const Disconnected();
}

Job<String> load() => run<ProfileState, String>(
      key: 'load',
      policy: Policy.droppable,
      onError: (state, error, stackTrace) => Failure(error),
      onCancel: (state, cancelled) => const Initial(),
      (ctx) async {
        ctx.emit(const Loading());
        final name = await ctx.wait(api.fetchName);
        ctx.emit(Loaded(name));
        return name;
      },
    );
```

Здесь ничто не говорит, что у загрузки есть мнение о `Disconnected`, поэтому
она продолжает идти сквозь него. А когда её отменят — дублем, закрытым экраном,
чем угодно, — `onCancel` сделает то, ради чего написан, и вернёт `Initial`
поверх факта, о котором сообщило устройство. Контроллер показывает просто
пустой профиль, хотя случилось другое: связи нет.

Проверка `state is Disconnected` внутри обработчика залатала бы этот случай.
Задача всё равно продолжала бы работу, которую отключение сделало
бессмысленной.

#### Правила

```dart
Job<String> load() => run<ProfileState, String>(
      key: 'load',
      policy: Policy.droppable,
      keepWhile: (state) => state is! Disconnected,
      onError: (state, error, stackTrace) => Failure(error),
      onCancel: (state, cancelled) => const Initial(),
      (ctx) async {
        ctx.emit(const Loading());
        final name = await ctx.wait(api.fetchName);
        ctx.emit(Loaded(name));
        return name;
      },
    );
```

Под работающей загрузкой пропадает связь, и контроллер публикует `Disconnected`
через `externalSetState`. `keepWhile` отвергает это состояние, отменяя загрузку
и отключая оба итоговых обработчика. Поэтому `Disconnected` остаётся видимым,
а не заменяется на `Initial` или `Failure`.

Если загрузку отменили вручную, пока состояние ещё совместимо, `onCancel` может
вернуть `Initial`. Обработчикам не нужны дополнительные проверки
`state is Loading`: правила `Job` уже выражают, какие состояния допускают
операцию и её итоговую поправку.

### Допуск обработчиков и их ошибки

| Что происходит | Что становится с обработчиками |
| --- | --- |
| Несовместимое внешнее обновление, когда бы оно ни пришло | Отключены навсегда; последующее совместимое обновление их не возвращает. |
| Родитель потерял допуск | Обработчики его детей тоже заблокированы. |
| Успех, отброшенный дубликат, тело, которое не запускалось | Не выполняются вовсе. |
| Собственный `emit` `Job` | Ничего; свои обработчики он не отключает. |
| Обработчик бросил ошибку | Ошибка сообщается; исход и очередь не затронуты. |
| Правило бросило ошибку при проверке допуска | Обработчики отключены, ошибка сообщена. Освобождение ресурсов всё равно выполняется. |

Эти дополнительные проверки относятся к `Job` с собственными обработчиками
состояния. Родитель без обработчиков перестаёт проверять свои правила после
завершения тела; дети остаются под своими правилами и под уже потерянным
допуском родителя.

Держите эти функции в границах вычисления состояния: их запись — это значение,
которое они возвращают. Запись изнутри обработчика — `externalSetState` или
источник, который зовёт его синхронно, — с этим значением не складывается: если
правила задачи её принимают, результат обработчика ложится поверх, посчитанный
из состояния до неё, и факт теряется; если отвергают — результат обработчика
отбрасывается. Освобождение ресурсов — дело [API уборки](resources.md).
