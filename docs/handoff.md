# Handoff: текущее состояние проекта

Обновлено: 2026-09-18. В работе вычитка `packages/async_job/doc/children.md`:
страница переписана и ждёт чтения владельца. Ветка `docs/children`, копия
`~/development/my/solo-children`.

**В работе.** Вычитка `packages/async_job/doc/children.md` — выбор владельца
2026-09-18, объём тоже его: грабли, сторожа и проза, стенда не будет. Четыре
раздела начинаются первой попыткой: `Future.wait` над двумя `ctx.run` (и второй
попыткой — он же с `eagerError: true`), `ctx.wait(() => sources, discard: …)`
для списка, уже лежащего на руках, голый `await` в колбэке `each`
и `ctx.run(tail)` для продолжения. Три сравнения
`.wait`/`Future.wait`/`runAll`, из-за которых вычитку и заказало ревью, сведены
в один раздел с таблицей; разбор конверта переехал после примера. Одно
утверждение страницы оказалось ложным и снято: `discard` у `ctx.wait`
регистрируется как обычно, а теряется список только при отмене, пришедшей
раньше строки регистрации. Сторожа —
`packages/async_job/test/children_rakes_test.dart`, 13 тестов, у каждого своя
мутация движка, все тринадцать проверены прогоном; набор `async_job` — 443.
Коммиты в ветке: план `0e02373`, работа `1e433af`. План —
`2026-09-18-children-rakes-plan.md`, отчёт —
`2026-09-18-children-rakes-report.md`. Дальше: владелец читает страницу,
слияние в `main` — после чтения.

Владелец начал читать, и первая правка по чтению уже в ветке, `b6afcdd`. Фраза
про `child.ignore()` рядом с `ctx.run` не называла причины и была мягче правды:
этот вызов там не делает ничего, потому что `run` наблюдает ребёнка сам, ожидая
`child.value`, а ошибка приходит через future, которую `run` вернул. Сторожа —
три теста в `packages/async_job/test/zone_test.dart` с двумя мутациями; набор
`async_job` — 446. Разбор — в отчёте, раздел «Правка по чтению владельца».
Вторая правка чтения ушла на соседнюю страницу —
`packages/solo/doc/children.md`, абзац про ребёнка, которого отвернули
стартовые правила: усыновление идёт до вопроса правилам, отказ и ошибка самого
правила — разные исходы, и ошибка правила выходит из `ctx.run` синхронно.
Сторожа — `packages/solo/test/children_test.dart`, три теста с мутациями: про
future от `ctx.run` и про синхронный бросок, второй гоняется и на `canStart`,
и на `keepWhile`. Третья правка — там же, про `ctx.each`: правило «ожидания
делайте контекстом колбэка» получило причину — отмена ждёт текущий колбэк,
поэтому обычный `await` держит и ребёнка, и родителя; сторожа — два теста
в `packages/solo/test/each_test.dart`. Четвёртая — там же: причина и следствие
у открытого стрима стояли наоборот, теперь звенья названы по порядку, сторож —
в `packages/solo/test/each_child_test.dart`. Пятая и шестая — там же: «ребёнок
уже ждёт этот колбэк» названо взаимной блокировкой и дополнено советом звать
`cancel()` без `await`, а про future от `cancel()` подписки сказано, что движок
её бросает и почему. Сторожа — три теста в `packages/solo/test/each_test.dart`.
Набор `solo` — 632. Ветка, таким образом, несёт правку и в `solo`.

Записи в `docs/records/` с 2026-09-18 лежат в каталогах по месяцам
(`docs/records/2026-09/`), а новые имена — без номера за день:
`YYYY-MM-DD-<тема>-<вид>.md`. Решение владельца 2026-09-18, коммит `7e75362`
в `main`; правила — в `AGENTS.md`. Старые имена с `[N]` оставлены как были.

Прошлая работа — вычитка `cancellation.md`, слияние `50077c5`. Первыми
попытками открываются перемотка (`wait`, затем `join` без токена), платёж
с журналом (два `join`), загрузка (голый `await` в цикле и `join` в уборке)
и закрытие (простой `close()` при очереди и `await close()` из тела). Закрытие
из задачи получило ещё и вторую попытку — метод контроллера держит контроллер
открытым до строки `close()`, и `Job`, поставленная, пока идёт вызов на сервер,
стартует и отменяется уже в пути; ответ там досылка. Сторожа —
`packages/solo/test/cancellation_rakes_test.dart`, 24 теста, у каждого своя
мутация движка; набор `solo` — 623. Отчёт —
`2026-09-18[8]-cancellation-rakes-report.md`.

Сегодня в `main` закончена ещё одна работа: раздел «A screen built on the
stream» в `packages/solo/doc/flutter.md` — грабли `StreamBuilder` над
broadcast-стримом, у которого нет повтора, — и стенд
`tool/flutter_snippets.py`, который собирает код этой страницы и стоит в CI
в задании `flutter` (виджетам нужен Flutter, которого нет в `snippets`).
Перевод обновлён, запись — `2026-09-18[9]-flutter-stream-screen-report.md`.
Стенд собирается командой `python3 tool/flutter_snippets.py <каталог>` вне
дерева; своего в дереве эта сессия не оставила.

Законченное за день до этого — две работы, обе записаны. Вычитка `state.md` —
слияние `28d2848`, записи `2026-09-16[2]-state-rakes-report.md`
и `2026-09-18[7]-handoff-archive.md`. Скиллы `end` и `handoff` и правило тире
в `tool/reflow.py` — тот же `2026-09-18[7]-handoff-archive.md`, раздел «Прежний
верх».

**Что дальше.** Владелец читает `packages/async_job/doc/children.md`; после
чтения — слияние ветки `docs/children` в `main`, снятие копии и обновление
шапки `2026-09-18-children-rakes-report.md`. Бэклог владельца пуст. Кандидаты,
которые записали сессии, решения по ним нет:

- полное независимое ревью `solo`: последнее —
  `2026-09-03[1]-solo-final-review-report.md`, а с тех пор пришли слушатели
  в ядре, заморозка закрытого состояния, правила накопителя, типизация
  `droppable`, `currentState`, переименование `Solo`/`SoloStream`
  и `SoloListenable` миксином; ломающих записей на `0.3.0` у него больше, чем
  у соседей. У `flutter_solo` полного ревью не было ни разу. В задание
  ревьюеру — проверка дерева до работы: `git log -1` плюс `grep`
  на `mixin SoloListenable`;
- порядок дальнейшей вычитки — раздел «Документация: вычитка страниц doc/»:
  после `children.md` по карте идут `resources.md`, `errors.md`, `testing.md`,
  `camera.md` и шесть страниц `async_job`.

**Открытые вопросы владельцу.**

- Предложен третий круг независимого ревью скиллов `end` и `handoff`: вторую
  редакцию прошли два круга, 22 и 23 находки, третью проверил только прогон
  `end` на копии. Ответа не было. Четвёртую редакцию `end` — уборка за собой
  той же формой, `2d65017` — прочитал один ревьюер: восемь находок, приняты
  все.
- Предложено перезалить 13 файлов `~/.claude/skills`, не залитых и до правила
  о тире (`dart`, `agy`, `codex`, `whats-new`, пять ревью-скиллов, `doc-bench`,
  шаблоны заданий; `humanizer` — чужой, его не трогать), и документы других
  проектов, которые заливаются личной копией скрипта. Ответа не было.
- Побочная находка по scopo, передана владельцу 2026-09-18 и не разобрана:
  раздел `0.14.0` в его `CHANGELOG.md` говорит, что `ScopeInitState`,
  `ScopeProgress` и `ScopeReady` удалены, а в дереве тега `v0.14.0` все три
  определены в `lib/src/scope/full_scope/scope_init_state.dart`.
- Где вести вычитку `children.md` — в `main` или новой веткой. Спрошено
  2026-09-18 вместе с `cancellation.md`; про неё владелец ответил новой веткой,
  про `children.md` — нет.
- Не раньше 2026-09-19 напомнить про молчащий `info@efa.dev`, а к 2026-09-26
  без ответа — о пересылке в `support@pub.dev`; подробности — раздел «Дальше,
  когда владелец выберет новую работу».

**Решения владельца, которые действуют.**

- Выпуск `0.3.0` отложен 2026-09-15 словом «пока рано»; собранное к нему —
  раздел «На следующий релиз собрано у всех трёх». Push — только отдельным
  поручением: прошлые разрешения были на свои push и на них одни.
- Перенос владения отменён окончательно 2026-09-15:
  `2026-09-14[17]-discard-follows-the-value-design.md` не делать. Освобождение
  регистрирует получатель — `ctx.run(opener, discard: …)`; разбор —
  `2026-09-15[8]-cleanup-registers-on-arrival-report.md`.
- Кортежная форма `ctx.runAll` отложена: добавляется потом без слома.
- Накопление через `externalSetState` отклонено:
  `2026-09-10[6]-state-accumulation-design.md` не реализовывать. Досылка при
  закрытии, надёжный буфер и автоматические повторы — отдельная работа,
  не начата.
- Бэктики в русской прозе, 2026-09-15: в них берут inline-код, имена классов
  и имена переменных; future в прозе — обычное слово, строчными и без бэктиков
  (`86edb87`).
- README объясняет API новому читателю, без ссылок на прежнее поведение
  и контекст диалога; в README `async_job` сохранять `removeDisposer` и пример
  ленивого лога из `479e934`.
- «The second attempt» в `accumulation.md` остаётся без входа отмены: попытка
  должна провалиться, иначе накопитель ниже не нужен (2026-09-15).

**Грабли.**

- Два дефекта `tool/reflow.py` не починены: вложенный список под пунктом `1.`
  склеивается в абзац — писать плоские списки; старая запись, названная скрипту
  ради правки шапки, перезаливается целиком — шапку править руками и скрипту
  запись не называть.
- Миксин `SoloListenable`: в позиции типа аргумент обязателен, хотя
  `strict-raw-types` его не требует; прямое создание —
  `class C<S extends Object> = Solo<S> with SoloListenable;`, форма с `{}`
  не компилируется. Старшинство `onListenerError` —
  `2026-09-18[1]-solo-listenable-mixin-design.md`.
- Пол `sdk: ^3.6.0` не проверен никем: `lints` 6 и `flutter_lints` 6 требуют
  Dart 3.8, поэтому на 3.6 и 3.7 `pub get` в репозитории не разрешится. Нужен
  такой пол для работы — линты не поднимать, не подняв пол пакетов.
- Версии пакетов и экшенов выяснять командой — `npm view <пакет> version`,
  `gh api repos/<экшен>/git/matching-refs/tags/v`, — а не по памяти.
- Что в `async_job` держится и чего не трогать — раздел «Positive findings»
  в `2026-09-15[17]-async-job-project-review.md`.

**Не трогать.** Чужих копий и веток нет. Копия `~/development/my/solo-children`
с веткой `docs/children` — моя, вычитка `children.md`; снимается после слияния.
Копию `~/development/my/solo-cancellation` с веткой `docs/cancellation` сняла
по слову владельца сессия, которая её и заводила, — после слияния `50077c5`.
Раньше тем же днём сняты копия `~/development/my/solo-state` с веткой
`docs/state-rakes` и ветка `worktree-agent-ae5d2ff3484afdc2b`, обе влитые.

**Дерево и проверки.** Работа идёт в копии `~/development/my/solo-children`
на ветке `docs/children`: коммиты `0e02373` (план) и `1e433af` (страница,
перевод, сторожа). Проверки на `1e433af` в этой копии: `dart analyze`
в `packages/async_job` без замечаний, `dart test` — 443 зелёных, `lib/` после
мутаций побайтово совпадает с копией до них; из корня копии
`reflow.py --check`, `check_line_width.py`, `check_translations.py`
и `check_doc_shape.py` — зелёные. Зонды и раннер мутаций — вне гита,
в `packages/async_job/.artifacts/children/`
и `.artifacts/2026-09-18-children/`; снимаются в конце работы. По поручению
владельца 2026-09-18 («пушь main») отправлено `d259d8b..401384d`, четыре
коммита: два закрывающих handoff прошлой сессии, `7e75362` — каталоги
по месяцам в `docs/records/` — и `401384d`, указатель handoff на эту работу.
`origin/main` — `401384d`; первая попытка push сорвалась по сети, вторая
прошла. `gate` на `401384d` — зелёный, прогон `35378752944`, и это первый
прогон на новой раскладке записей: три питоновские проверки в задании `docs` её
принимают. Разрешение было на этот push и на него одно. Тем же поручением
`main` влит в `docs/children`: конфликт был один, в `docs/handoff.md`, и решён
в пользу ветки — её версия полнее. Вычитка `cancellation.md` влита слиянием
`50077c5`; проверки на нём: `solo` — 623 теста, анализ чистый, документные
проверки зелёные. Ветка `docs/cancellation` и её копия сняты, зонды разбора
и раннер мутаций из `.artifacts/2026-09-18-cancellation-rakes/` удалены —
запись `2026-09-18[8]-cancellation-rakes-report.md` называет их, а на диске их
уже нет. На `origin` ветка осталась на `44f46cb`, то есть до правок по чтению:
снять её можно только push'ем, а разрешения на него не было. Коммит с этой
записью — поверх `d259d8b`, он же `origin` на момент записи; отправлять его
владелец не велел (форма `/end`). По поручению владельца 2026-09-18 (форма
`/end`) отправлено вместе с прежней редакцией этой записи, по `eee3136`
включительно: от `42a3a2d` — вся ветка `docs/state-rakes`, слияние `28d2848`
и записи о нём; `origin` до отправки — `42a3a2d`. Раньше тем же днём отправлены
`c9d4997..6b6c61c` (CI зелёный, `gate` и `site`) и `42a3a2d`. Разрешения были
на эти push и на них одни; следующий — снова отдельным поручением. Наборы
на `28d2848`: `solo` 599, пример 9, анализ чистый; `flutter_solo` 94
и `async_job` 430 — по `9bd7b6c`: их код слияние не трогает, а в `solo/lib`
изменился только dartdoc. Документные проверки — `reflow.py --check`,
`check_line_width.py`, `check_translations.py`, `check_doc_shape.py` — зелёные
на коммите с этой записью. По второму поручению владельца 2026-09-18 (снова
форма `/end`) отправлено вместе с этой записью: `c796b92` с записью
`2026-09-18[8]-cancellation-rakes-report.md` и коммит с этой поправкой поверх
него; `origin` до отправки — `eee3136`. CI — по полному хэшу:
`rtk proxy gh run list --commit "$(git rev-parse <хэш>)"`. По третьему
поручению владельца 2026-09-18 («пушь») отправлены `main` до `14e4aa7` — правки
handoff о ветке и `376fb63` соседней сессии, `origin` до отправки —
`4792be4`, — и ветка `docs/cancellation`. CI `gate` на `14e4aa7` — зелёный,
прогон `35355608760`. Разрешение было на этот push и на него одно; эта поправка
handoff сделана после него и не отправлена. `~/.claude/skills` отправлен
целиком по поручению владельца 2026-09-18, `origin` там — `8372247`: скиллы
`end` и `handoff`, правка `reflow`, уборка за собой в форме `end` (`2d65017`)
и урок в `doc-reader` (`8372247`). Синхронизация скиллов claude.ai выключена
владельцем 2026-09-18 (`syncClaudeAiSkills: false`
в `~/.claude/settings.json`), `.trash/` и `synced/` из `~/.claude/skills`
удалены. Урок про стенд на виджет-тестах дописан в скилл `doc-bench`
и закоммичен там локально (`a5175f5`), в `origin` скиллов не отправлен:
разрешение было на `main` этого репозитория. Там же локально `e583e79`: `end`
про отказ `git branch -d` у ветки, влитой в `HEAD`, но обгоняющей свою
удалённую, и `doc-reader` про цену второй версии, которую называют по прогону,
а не на глаз. По четвёртому поручению владельца 2026-09-18 (форма `/end`)
отправлено `14e4aa7..56e3f0c`, 32 коммита: мои `38ba0bb` (раздел `flutter.md`
про экран на стриме и стенд `tool/flutter_snippets.py`) и `56e3f0c` с этой
записью, остальные тридцать — соседней сессии, вся вычитка `cancellation.md`
со слиянием `50077c5` включительно. `origin` до отправки — `14e4aa7`; поправка
этого абзаца ушла следом, тем же поручением. Проверки на `38ba0bb`: анализ
пакета стенда чистый, `flutter test --concurrency=1` — 8 тестов,
`check_traces.py` — четыре трассы сходятся с выводом; код пакетов не менялся,
наборы `solo` и `flutter_solo` не гонялись. `gate` на `56e3f0c` — зелёный,
прогон `35371691322`, и это первый прогон со стендом `flutter.md` внутри
задания `flutter`; `site` там же зелёный, `35371691313`. На `213b112`, где
поправлен этот абзац, `gate` тоже зелёный — прогон `35371961406`. Прогон
коммита с этой записью не дождан: она и есть последняя. Слияние `50077c5`
и запись о нём ушли на `origin` в той же пачке: `origin/main` — `213b112`,
и push был один, той сессии. Следом она отправила `d259d8b`; `gate` на нём
зелёный, прогон `35372516145`.


## На следующий релиз собрано у всех трёх

Ломающие правки есть в каждом пакете, поэтому все трое идут на `0.3.0`.
Развёрнуто — в разделах `## Unreleased` их `CHANGELOG.md`; коротко (счёт сверен
2026-09-18): у `async_job` пять ломающих, громкие из них — протектед
`JobBase.inUncancellableSection` и починенный `ctx.join`; у `solo` двенадцать
(`SoloBase` → `Solo` вместе с переездом стрима в `SoloStream`, `currentState`,
`SoloCloseMode`, `SoloTransition`, `errorHandler`, свои слушатели,
`isFinished`, финальное состояние закрытого, умолчание накопителя `join`,
перенос вместо отмены у `AccumulationPolicy.replace` и две про `droppable`)
плюс `Solo.pending`; у `flutter_solo` пять ломающих и новые `SoloSelection`,
`SoloSelector`, `select`/`listen`, `SoloSelection.of`, `SoloBuilder`
и `SoloSelectBuilder`.

Порядок связки задан зависимостями: `solo` уже пользуется новым
`inUncancellableSection`, поэтому в дереве и живут четыре
`pubspec_overrides.yaml`. Сперва `async_job`, затем `^0.3.0`
в `packages/solo/pubspec.yaml` и `solo`, затем `^0.3.0`
в `packages/flutter_solo/pubspec.yaml` и `flutter_solo`. Оверрайды снимаются
и добавляются в индекс — порядок
в `2026-09-11[1]-solo-release-readiness-report.md`.

Версии в трёх `pubspec.yaml` пока `0.2.0`, заголовки в `CHANGELOG.md` —
`## Unreleased`; и то и другое меняется при подготовке. Вместе с ними — строка
про стенд во вступлении `vs-bloc` (оригинал и перевод): там названа версия
исходников, на которых собраны фрагменты, и сейчас это `0.2.0`.

## Документация: вычитка страниц doc/

Начата сквозная вычитка страниц, которых она ещё не касалась: вычитаны
`packages/async_job/README.md`, `packages/solo/doc/vs-bloc.md`,
`packages/solo/doc/accumulation.md`, `packages/solo/doc/jobs.md`
и `packages/solo/doc/state.md` — прозой, граблями и чтением владельца.
`cancellation.md` — первые попытки и правки прозы в ветке `docs/cancellation`,
владелец читает; отчёт — `2026-09-18[8]-cancellation-rakes-report.md`. Стенда
у страницы нет, как и у `state.md`: сторожа —
`packages/solo/test/cancellation_rakes_test.dart`, и правка страницы идёт
вместе с ним. Разбор `jobs.md` и прозы `state.md` —
в `2026-09-18[6]-handoff-archive.md`, граблей и чтения —
в `2026-09-16[2]-state-rakes-report.md` и `2026-09-18[7]-handoff-archive.md`.
Порядок — карта из `2026-09-11[16]-docs-structure-design.md`: `jobs.md`,
`state.md`, `cancellation.md`, `resources.md`, `children.md`, `errors.md`,
`testing.md`, `camera.md`, `flutter.md`, затем шесть страниц `async_job` и три
README.

`children.md` вычитана в ветке `docs/children` — той самой находкой ревью она
и была заказана: три сравнения `.wait`/`Future.wait`/`runAll` в одной странице
и разбор конверта раньше примера. Стенда у страницы нет, сторожа —
`packages/async_job/test/children_rakes_test.dart`; отчёт —
`2026-09-18-children-rakes-report.md`. Владелец читает.

**Код `jobs.md` не собирается ничем.** Стенды есть только у `vs-bloc.md`
(`tool/doc_snippets.py`) и `accumulation.md` (`tool/accumulation_snippets.py`);
у остальных страниц фрагменты не компилируются ни в гейте, ни перед коммитом.
Фрагменты `jobs.md` собраны отдельным проектом вручную — компилируются все, —
но на дереве такой проверки нет. Рецепт паузы при этом сторожит
`packages/solo/test/queue_pause_recipe_test.dart`, семь сторожей.

**Стенда у `state.md` тоже нет, сторожа — тестом.** Каждую первую попытку
страницы и каждое утверждение, добавленное чтением владельца, держит
`packages/solo/test/state_rakes_test.dart`, и у каждого сторожа своя мутация
движка. Правка страницы идёт вместе с ним.

## Требования к README, которые нужно сохранить

- При описании API на русском называть `Job` прямо. Слово «задача» уместно для
  самой работы, но не должно подменять имя класса в объяснении его
  возможностей.
- `ctx.join(Database.open)` объясняется самостоятельно до сравнения с `wait`:
  вызывает операцию, ждёт её завершения и проверяет отмену. `wait`
  предлагается, когда нужно прекратить ожидание сразу при отмене.
- `discard` после `return` объясняется на примере ожидания дочерней задачи:
  тело вернулось, но весь `Job` ещё не завершился.
- В примере миграции `onCancel` передаёт сигнал токену, `join` ждёт фактической
  остановки, а `uncancellable` защищает шаг от самого сигнала.
- Внешние асинхронные операции в теле следует ожидать через контекст. Прямой
  `await` допустим, но сам не проверяет отмену. Внутри действия, переданного
  контексту, и при уборке обычный `await` уместен с учётом описанных в README
  границ.
- Упоминание `solo` следует из границ ядра: в `async_job` нет управления
  состоянием, очереди и правил планирования; `solo` добавляет их.
- Два примера проброса `Cancelled` показывают отдельную ветку `catch`
  и проверку типа внутри общего обработчика. Оба способа подходят и для
  `Exception`, и для `Object`; выбор типа не различает эти способы.
- Принятая отмена самого `Job` сохраняется после перехвата исключения. Код
  после `catch` может выполниться, следующая контрольная точка снова бросает
  отмену, а при завершении исход остаётся `Cancelled`. Перехваченный
  `Cancelled` от операции или дочерней задачи сам по себе родителя не отменяет:
  возможен `Done`.

Число блоков кода в README больше не годится как ориентир: материал разъехался
по страницам `doc/`. Форму держат `check_translations.py` (двадцать пар)
и `check_doc_shape.py`. Примеры `then`, его контракт и переводы обновлены
вместе с реализацией.

## Дальше, когда владелец выберет новую работу

Материал и замеры: `2026-09-09[9]-todo-review.md`. Готовый стенд может лежать
в `.artifacts/2026-09-09-wave2-review/a/stand`; восстановление —
`python3 tool/doc_snippets.py <каталог>`. При проверке внешней операции
дожидаться самой операции: снимок состояния сразу после отмены `Job`
не показывает её окончательный результат.

Пожелания владельца находятся в `docs/backlog.md`. После окончания текущей
реализации предложить выбрать оттуда работу; самостоятельно записи не менять.
Другие ранее отложенные примеры для `solo`: `stateAs`, `check`, retry/backoff.
Их брать только после выбора владельца.

На 2026-09-12 `info@efa.dev` молчит — владелец проверил. Напомнить о нём снова
**начиная с 2026-09-19, но не раньше**, при старте сессии или выборе новой
работы. Если к 2026-09-26 ответа так и нет, напомнить о пересылке
в `support@pub.dev`. Переписку самостоятельно не отправлять. После решения
обновить `2026-09-05[8]-job-name-request-report.md` и убрать напоминание. Это
не блокирует нынешнее имя `async_job`.

## Ограничения и общение


- Не приписывать владельцу ответы на формы или выбор вариантов, которые он
  не давал. Собственную интерпретацию запроса называть своей.
- Владелец просит минимум служебного вывода: ему нужны сообщения по делу,
  а не журналы запусков. Заголовки инструментов формирует интерфейс; не обещать
  скрыть их, сокращать число вызовов и объём вывода.
- Текст рабочих документов переносить на 79 символах. Правило и порядок
  проверок сохранены в `AGENTS.md`.
- Английский README и русский перевод править вместе. Связность и смысл важнее
  косметического сокращения; подтверждённые условия не выкидывать.
- Не менять заодно согласованную семантику отмены.
- `ctx.run` принимает `Job<T>`, а не только `DeferredJob<T>`: библиотечные
  классы на основе `JobBase` тоже могут быть дочерними. Автоматически
  запускающийся обычный `Job` отвергается.
- Не расширять защищённые `owner` и `parent` без подтверждённой нужды
  потребителя. `isDisposing` не выносить в публичный `Job` без нового решения.
- Буфер ранних событий в `each` сохранять; прежние альтернативы теряли события
  при подписке. При ошибке тела дочерние задачи дорабатывают; при самоотмене
  тела отмена передаётся детям.
- Соседний `scopo` ведётся отдельно. Его прежний переход на опубликованное ядро
  0.1.0 выполнен; обновление на новый API не входит в эту работу. Архивирование
  `stream_future` тоже отдельная внешняя задача.
- `/Users/user/development/yandex/yx_camera` не трогать: это чужой проект.
- Не запускать push, публикацию, теги или релизные коммиты без отдельного
  поручения. Правила связки публикации полностью заданы в `AGENTS.md`.

## Архив

- Законченное до 2026-09-18 — `2026-09-18[6]-handoff-archive.md`.
- Законченное до 2026-09-18, второй вынос — `2026-09-18[7]-handoff-archive.md`.
- Прежняя большая версия handoff — в git на `659512a`.
