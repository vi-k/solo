# Handoff: текущее состояние проекта

Обновлено: 2026-09-14, шаг 1 плана про отмену под параллельным ожиданием
сделан: ядро разбирает конверт. Дальше — шаг 2, документы. Раньше: состояние
закрытого контроллера заморожено и отправлено в `origin/main`; план слушателей
пройден целиком, `vs-bloc` вычитан владельцем и принят, чтение контроллера
переименовано в `currentState`, `SoloListenable` перенесён на `SoloBase`, гейт
запускает CI и собирает код документации, цепочки описаны, `vs-bloc` перестроен
вокруг ошибки, абзацы заливаются жадно.

## Спека в ходу: отмена под параллельным ожиданием

Вопрос владельца 2026-09-14: что даст внутри задания `await [f1, f2].wait`,
если одна ветка кончится исключением, а другая — отменой. Ответ: отмена
перестанет быть отменой. `[...].wait` заворачивает ошибки веток
в `ParallelWaitError`, а ядро ловит отмену по типу — исход становится `Failed`,
фоновые дети доигрывают, ошибка уходит в зону. Ломается написанное обещание
`doc/children.md`: оно держится для `await ctx.run(child)` и не держится для
`[ctx.run(a), ctx.run(child)].wait`.

Спека: `2026-09-14[4]-parallel-wait-cancellation-design.md`, **третья
редакция**. Два круга ревью, в каждом по два ревьюера одновременно в разных
клонах — `agy` (`gemini-3.8-flash-high`) и Codex (`gpt-6-astra:xhigh`). Записи:
`2026-09-14[5]` и `[6]` — первый круг, четырнадцать находок; `2026-09-14[7]`
и `[8]` — второй, одиннадцать. Все приняты, вердикты стоят в записях. Обе
первые редакции круги признали негодными для плана.

Что дал первый круг: критерий про отменённого соседа был невыполним — `.wait`
ждёт все ветки; мест, где читается тип пойманной ошибки, оказалось пять,
а не три; правило исхода сузилось до «отмена решает исход, только если
в конверте нет настоящего отказа»; строка `job_stream.dart:88` не меняется
вовсе; обход стал итеративным; запись в `CHANGELOG` — `Breaking`.

Что дал второй круг. Главное: приёмка второй редакции требовала от правила
обратного тому, что правило обещает — критерии 1, 2 и 7 ждали `Cancelled`
от `[boom(), giveUp()].wait`, где `boom` и есть настоящий отказ. Причина
названа там же: таблицу приёмки я перенёс из первого круга механически,
не пересчитав под изменившуюся политику. Требование «последовательно
и параллельно кончается одинаково» после сужения правила стало неверным
и заменено точным правилом про чистый и смешанный конверт. Контракт обхода
дописан до восьми правил: активный путь против законченного подграфа (общая
ссылка без цикла законна и встречается сама собой), защищённое чтение
(`cast`-список бросает `TypeError`, а бросок из обхода внутри `catch` не даёт
заданию закончиться), результат разбора вместо одной отмены. Миграция теперь
называет `ctx.wait(dispose:)` первым путём. И оба ревьюера независимо нашли
ошибку в моём вердикте первого круга — она исправлена, в шапке записи
`2026-09-14[5]` это отмечено.

Оба вопроса спеки закрыты решением владельца: ранней отмены веток не будет
вовсе, выпуск — вместе с накопленным, версией `0.3.0`.

**План написан:** `2026-09-14[9]-parallel-wait-cancellation-plan.md`, два
коммита — ядро с тестами и документы. Радиус проверен пробой, а не взят
из ревью: черновая классификация в `_execute` оставила зелёными все четыре
набора (303 / 526 / 9 / 87). Значит старого поведения не сторожит ни один тест,
и мутации из плана — единственная проверка того, что новые кусаются.

**Шаг 1 сделан.** Ядро разбирает конверт: новый `part`
`packages/async_job/lib/src/envelope.dart`, правка ветки `on Object catch`
в `_execute` и правка `_toZone`. Тестов в `async_job` стало 341 (было 303),
соседние наборы не изменились: 526 / 9 / 87.

Шаг делали одновременно два исполнителя в разных клонах — `agy`
(`gemini-3.8-flash-high`, 867 с) и Codex (`gpt-reserve:high`, 1771 с). Обе
реализации прошли перекрёстную проверку: тесты одного зелены на реализации
другого, и наоборот. В дерево ушла раскладка Codex — итеративный обход кадрами
с индексом читается лучше, — но карта состояний взята у `agy`:
`Map.identity()`. Это не вкусовщина, а дефект, найденный своим зондом:
`ParallelWaitError` не `final`-класс, и подкласс с переопределённым `==` выдаёт
вложенный конверт за цикл. У Codex такой конверт давал `Failed` вместо
`Cancelled`. На это поведение теперь есть тест, которого не написал ни один
исполнитель.

**Следующий шаг — шаг 2 плана:** `doc/children.md`, его перевод и запись
`**Breaking:**` в `CHANGELOG` пакета. Как правка выглядит со стороны `solo`,
уже померено обоими концами: раньше чистый конверт давал
`Failed(ParallelWaitError)` и уходил в `onError`, теперь даёт
`Cancelled(handler: …)` и уходит в `onCancel`.

## Последняя работа: слушатели в ядре, билдеры и заморозка

Владелец спросил, можно ли сделать `SoloBuilder` для `SoloBase`, чтобы виджет
не зависел от стрима и подписки и чтобы у чужих реализаций был один инструмент.
Оказалось, что подписаться к `SoloBase` снаружи нечем, и спека — про шов
в ядре, а не про виджет: `2026-09-13[2]-solo-listeners-design.md`, третья
редакция.

Три круга независимого ревью, в каждом по два ревьюера одновременно в разных
клонах — Codex (`gpt-6-astra:xhigh`) и `agy` (`gemini-3.8-flash-high`). Записи:
`2026-09-13[3]` и `[4]` — первый круг, пятнадцать находок; `[5]` и `[6]` —
второй, одиннадцать; `[7]` и `[8]` — третий, восемь. Все приняты, вердикты
стоят в записях.

Третий круг снял два блокирующих: второе расширение `select` сделало бы
`controller.select(...)` неоднозначным у `SoloListenable` (он и `SoloBase`,
и `ValueListenable`), а именованные конструкторы от `SoloBase` не пишутся —
у выборок `S` без границы, у `SoloBase` `S extends Object`. Отсюда в четвёртой
редакции: расширение остаётся одно, а контроллер принимают статическая фабрика
`SoloSelection.of` и новый виджет `SoloSelectBuilder`.

Владелец взял все три новых имени — `SoloBuilder`, `SoloSelectBuilder`,
`SoloSelection.of` — и велел писать план. План:
`2026-09-13[9]-solo-listeners-plan.md`, семь шагов, восемь мутаций.

Все семь шагов сделаны и смержены. Слушатели живут в `SoloBase` вместе
с защищёнными `hasListeners` и `onListenerError`; `SoloListenable` сведён
к лицу `ValueListenable`; у выборки появилась `SoloSelection.of`, и закрылось
окно первого значения; в `flutter_solo` добавлены `SoloBuilder`
и `SoloSelectBuilder`.

Каждый шаг делали одновременно два исполнителя в разных клонах — `agy`
(`gemini-3.8-flash-high`) и Codex (`gpt-reserve:high`). Приёмка шла своими
прогонами, своими зондами и своими мутациями сверх заказанных; так нашлись
`identical` вместо `==` в снятии слушателя, синхронное уведомление изнутри
`addListener` у выборки и `==` вместо `identical` при смене контроллера
у `SoloBuilder`.

Седьмым шагом переписаны документы и переводы. «Наблюдение за состоянием»
в `packages/solo/doc/state.md` показывает `addListener` рядом с `currentState`
и стримом и называет обещания движка; «Своя доставка» стала про доставку
другого рода и про то, как уходить со старого рецепта.
`packages/solo/doc/flutter.md` и `README` пакета `flutter_solo` называют оба
билдера. Из `CHANGELOG` `solo` убрана запись о старом рецепте: он не доживёт
до выпуска.

**Работа закрыта.** После неё владелец решил ещё две вещи.

Первая: `SoloSelector` и `SoloSelectBuilder` больше не заворачивают
`ValueListenableBuilder` вокруг своей выборки, а ведут подписку сами, как
`SoloBuilder`. Публичного API и поведения это не меняет; коммит `c7f0203`.

Вторая, и она сделана: **состояние не меняется после того, как закрытие
закончилось.** До этой работы `externalSetState` после закрытия двигал
`currentState`, звал `onChange` и не доходил ни до кого — ни слушатели,
ни закрытый стрим события не получали, — и каждый читатель был обязан сам
выбирать между «последним объявленным» и «тем, что в контроллере сейчас».
Теперь за чертой запись бросает `StateError`, по образцу `ctx.emit` после конца
задачи. Спека: `2026-09-13[10]-closed-state-freeze-design.md`. Черта — конец
работы движка, там же, где сбрасываются слушатели, и это не то же самое, что
завершение `Future` у `close()`.

Круг ревью пройден, двумя ревьюерами одновременно в разных клонах — Codex
(`gpt-6-astra:xhigh`) и `agy` (`gemini-3.8-flash-high`). Записи:
`2026-09-13[11]-closed-state-freeze-review.md`
и `2026-09-13[12]-closed-state-freeze-review-2.md`, вердикты стоят в записях.
Круг снял два блокирующих. Первое: защита наследника через `isClosed`
не работает — он истинен с первой строки `close()` и глушит внешний источник
на всё время `drain`; отсюда во второй редакции публичный признак `isFinished`,
от которого первая отказалась. Второе: «конец закрытия» и «конец публичного
`close()`» — не одно и то же, у `Solo` за базовым закрытием идёт закрытие
стрима. Радиус правки тоже был занижен: краснеет восемь тестов, а не один,
и семь из них переписываются, а не снимаются.

Второй круг прошёл тем же порядком; записи
`2026-09-13[14]-closed-state-freeze-review-3.md`
и `2026-09-13[15]-closed-state-freeze-review-4.md`. Блокирующего в нём нет: оба
ревьюера подтвердили зондами, что `isFinished` закрывает дренаж целиком и что
штатного пути к `StateError` у корректной программы не остаётся. Находок
четыре, все приняты: снимается два теста, а не один; список устаревающих мест
дорос с семи до тринадцати; переписывать тесты механически нельзя — мутация
показала, что `close removes every listener` проходит и с сохранённым списком
слушателей, если не проверять `hasListeners` прямо; и `isFinished` надо
проверять после последнего `await`, иначе черта успевает наступить
в промежутке.

Владелец решил снять кеш `SoloBuilder` — той же правкой, что и заморозка,
не раньше.

План написан: `2026-09-14[1]-closed-state-freeze-plan.md`, два коммита и семь
мутаций. Первый коммит неделим — запрет живёт в `solo`, а краснеют от него
и тесты `flutter_solo`, поэтому ядро, виджет и все тесты идут вместе. Радиус
проверен пробой, а не взят из ревью: временный запрет в `externalSetState`
уронил ровно восемь тестов, один в `solo` и семь в `flutter_solo`, а примеры
обоих пакетов остались зелёными. Проба же показала то, чего в спеке не было:
тест про микротаску из `observer.onClose` падает дважды — отвергнутая запись
бросает внутри микротаски, и ошибка уходит в зону, — поэтому бросок ловится
там же, внутри неё.

Шаг 1 сделан и смержен: черта проведена, признак называется `isFinished`, кеш
`SoloBuilder` снят, восемь тестов разобраны — два сняты, шесть переписаны
с названным предметом, — и добавлено двенадцать новых. Наборы: `solo` 526,
пример 9, `flutter_solo` 87, оба анализа чисты, `dart format` не меняет ничего.

Работали два исполнителя одновременно в несмежных клонах — codex
(`gpt-reserve:high`, 963 с) и `agy` (`gemini-3.8-flash-high`, 491 с), задание
одно. В дерево ушла версия codex, в неё перенесено лучшее от `agy` (проверка
сброса слушателей **внутри** микротаски) и добавлено своё: тест на уборщика
задачи, которую дожидается закрытие, и тест на виджет, под которым закрыли
контроллер, — второе покрытие пропало вместе со снятым тестом, первого не было
вовсе.

Мои мутации, не заказанные исполнителям, все убиты: запрет снят целиком; бросок
заменён тихим выходом; проверка по `isClosed` вместо черты; запрет есть,
а список слушателей не сброшен; черта перенесена выше `observer.onClose`.
Последняя из них — та самая, что во втором круге ревью проходила мимо
механически переписанного теста. Отдельно проверено и то, что план обещал:
возвращённый кеш `SoloBuilder` не убивает ни одного теста — различать его
больше нечем.

Шаг 2 сделан следом: `doc/state.md` с переводом — рецепт `Camera` защищает
источник признаком вместо того, чтобы гасить его перед `super.close()`,
и там же разобрано, почему при `drain` прежний порядок стоит самого дренажа;
инвариант 8 в `docs/architecture.md`; две ломающие записи в `CHANGELOG` ядра
и две у `flutter_solo`; комментарий в примере; разделы «Закрытие»
и «`SoloBuilder`» в `2026-09-13[2]-solo-listeners-design.md` помечены
пересмотренными.

По просьбе владельца исполнение прошло круг ревью — двое одновременно
в изолированных клонах, Codex (`gpt-6-astra:xhigh`, 1332 с) и `agy`
(`gemini-3.8-flash-high`, 483 с). Записи:
`2026-09-14[2]-closed-state-freeze-review-5.md`
и `2026-09-14[3]-closed-state-freeze-review-6.md`, вердикты стоят в записях.
Одиннадцать находок, все приняты и закрыты.

Существенного три. **Четырнадцатое место нашлось дважды:** `doc/vs-bloc.md`
с переводом велел гасить внешний источник перед закрытием — тот самый порядок,
который эта работа развернула, — и сам `docs/handoff.md` описывал старое
поведение в настоящем времени. Оба исправлены, сниппет `vs-bloc` получил защиту
`if (!isFinished)`.

**Тест закрытой выборки не сторожил свой предмет** — единственная находка
по коду. Мутация, возвращающая закрытому источнику последнее объявленное
значение вместо чтения состояния, проходила все 87 тестов. Теперь источник
двигается при `compare`, который считает изменение тем же значением, и та же
мутация падает.

**Совместимая коллизия `isFinished`** не ловится компилятором: наследник
с собственным булевым `isFinished` — «загрузка завершена» — собирается, его
защита начинает отвечать доменным флагом, а движок продолжает отказывать
в записи. В `CHANGELOG` ядра появилось прямое требование переименовать.

Остальное — точность формулировок: дартдок `SoloBuilder` привязывал сброс
слушателей к завершению `close()`, запись `CHANGELOG` у `flutter_solo` всё ещё
обещала кеш, «where `close` returns» называло не тот момент, обещание
«слушателя зовут раньше следующей строки» не оговаривало вложенную запись,
а ломающих пометок и рецепта миграции терминального состояния не хватало.

**Работа закончена целиком. Следующую выбирает владелец.**

Что осталось открытым — ниже.

Вычитка `vs-bloc` закончена и принята владельцем —
`2026-09-13[1]-vs-bloc-proofreading-report.md`. Бэклог пуст.

Открытое, чего владелец пока не решил:

- нормализовать ли заимствованные существительные в русских документах
  (`future`/`Future`, `emitter`, `completer`, `listenable`) — они сейчас
  вразнобой по `docs/ru/**` и `async_job/children.md`;
- в 7-м разделе `vs-bloc` требование «переименование должно завершиться» кодом
  не выражено: `rename` выживает только потому, что не попал в предикат
  `removeWhere`. Вариант — `queue.clear()` плюс `cancellable: false`
  у `rename`; тогда выбор переедет из предиката во флаг `Job`. Предложено,
  ответа нет;
- код в `packages/solo/doc/state.md` не собирается ничем: стенд
  `tool/doc_snippets.py` берёт только `doc/vs-bloc.md`. Рецепт `Camera` в нём я
  после правки собрал вручную отдельным проектом и прогнал в обоих режимах
  закрытия — сегодня он верен, но следующая правка ядра сломает его молча.
  Расширить стенд на `state.md` — отдельная работа. Предложено, ответа нет.

**`solo` отправлен** 2026-09-14 по поручению владельца: `main` совпадает
с `origin/main` на коммите заморозки. Разрешение было на этот push и на него
одно; следующий — снова отдельным поручением. **`~/.claude/skills`
не отправлен:** там три коммита впереди `origin/main` (два мои, один чужой)
и незакоммиченная правка `codex/SKILL.md` от параллельной сессии — её
не трогать.

Личные скиллы, выросшие из этой работы, живут в отдельном репозитории
`~/.claude/skills` (русский, свои коммиты): `doc-bench` — стенд, который
прогоняет код документа; `doc-reader` — как писать для читателя, а не про свою
работу; `reflow` — заливка абзацев.

## На следующий релиз собрано у всех трёх

Ломающие правки есть в каждом пакете, поэтому все трое идут на `0.3.0`.
Развёрнуто — в разделах `## Unreleased` их `CHANGELOG.md`; коротко:
у `async_job` протектед `JobBase.inUncancellableSection` и починенный
`ctx.join`, у `solo` пять ломающих (`currentState`, `SoloCloseMode`,
`SoloTransition`, `errorHandler`, `ArgumentError` у `droppable`) плюс
`SoloBase.pending`, у `flutter_solo` три ломающих и новые `SoloSelection`,
`SoloSelector`, `select`/`listen`, `SoloSelection.of`, `SoloBuilder`
и `SoloSelectBuilder`.

Порядок связки задан зависимостями: `solo` уже пользуется новым
`inUncancellableSection`, поэтому в дереве и живут четыре
`pubspec_overrides.yaml`. Сперва `async_job`, затем `^0.3.0`
в `packages/solo/pubspec.yaml` и `solo`, затем `^0.3.0`
в `packages/flutter_solo/pubspec.yaml` и `flutter_solo`. Оверрайды снимаются
и добавляются в индекс — порядок
в `2026-09-11[1]-solo-release-readiness-report.md`.

Версии в трёх `pubspec.yaml` пока `0.2.0`, заголовки в `CHANGELOG.md` —
`## Unreleased`; и то и другое меняется при подготовке. Вместе с ними — строка
про стенд во вступлении `vs-bloc` (оригинал и перевод): там названа версия
исходников, на которых собраны фрагменты, и сейчас это `0.2.0`.

## Абзацы заливаются жадно

Переносы внутри абзаца больше не решение: текст заливается до 79 колонок
`tool/reflow.py`, и слово, которое влезает в предыдущую строку, стоит в ней.
Правило — в `AGENTS.md`, раздел «Ширина и перенос строк»; проверка `--check`
стоит в гейте, задание `docs`. Отчёт — `2026-09-12[9]-greedy-fill-report.md`.

Разом перезалито 48 файлов. Равенство содержимого проверено отдельным разбором
на блоки до и после: расхождений ноль, изменились только переносы.

## Документация: vs-bloc начинается с ошибки

Каждый из одиннадцати разделов `packages/solo/doc/vs-bloc.md` теперь
открывается «Первой попыткой»: код, к которому ведёт словарь API, и измеренное
доказательство, что требование он не держит. Только потом идут `### Bloc`
и `### Solo`. В разделах 1 и 9 попыток две. Отчёт —
`2026-09-12[8]-vs-bloc-first-attempt-report.md`.

Восемь ошибочных фрагментов новые; два из них до этого жили литералами
в стенде. Раздел 1 вдобавок показывает порядок выполнения, которым теряется
заметка, — одна трасса объясняет обе первые попытки.

Работа вскрыла две гнили и починила обе. Фрагменты раздела 2 не компилировались
с 0.2.0: `onChange` там принимал `previous, current` вместо `SoloTransition`.
Сам стенд `tool/doc_snippets.py` не работал — читал `controller.state`
и резолвил `async_job` с pub.dev вместо дерева; заодно переведён с номерных
ключей фрагментов на именные.

Отдельной сквозной правкой `Job` в русской прозе взят в бэктики (235 мест)
и приведён к женскому роду (34 места); в английской, наоборот, 24 голых `Job`
стали строчными `job`.

Стенд документации теперь входит в гейт — задание `snippets`: собирает код
`vs-bloc.md` в два пакета, анализирует их и прогоняет все 22 драйвера.
Проверено двумя мутациями: испорченная сигнатура `onChange` даёт `dart analyze`
код 3, снятый `onCancel` в снимке `RefreshController` — `dart run` код 255.
Трассы с прогоном он не сверяет: драйверы печатают, а не проверяют.

## Документация: цепочка — не ребёнок

Вопрос владельца: запускается ли цепочка задач через `ctx.run`. Ответ — нет,
и записан он в `packages/async_job/doc/children.md`, раздел
`### A chain is not a child`; на стороне `solo` дополнен раздел «Chaining
completed work». Отчёт — `2026-09-12[7]-chain-vs-child-report.md`.

Коротко: усыновляется только голова цепочки; родитель ждёт детей, а не то, что
на них навешано, и медленный хвост переживает родителя; очередь контроллера
хвоста тоже не ждёт; отмена до хвоста доходит через источник; необслуженная
ошибка хвоста уходит в зону, создавшую цепочку.

## CI: гейт машиной

`.github/workflows/gate.yml` — то же, что агент гоняет перед коммитом: задания
`dart` (ядро и его пример), `flutter` (`flutter_solo` и его пример) и `docs`
(три проверки документов и сборка сайта). Push в `main`, pull request, ручной
запуск. Отчёт — `2026-09-12[6]-ci-gate-report.md`.

Прогон перед коммитом это не отменяет: CI говорит уже после того, как правка
в `main`.

**Найдено по дороге:** у `packages/flutter_solo/example` не было своего
`pubspec_overrides.yaml`, и он разрешался против опубликованного `solo` 0.2.0 —
то есть его зелёный анализ ничего не значил. Файл добавлен, список
в `AGENTS.md` дополнен причиной.

Первый прогон показал, что тулчейном не может быть пол пакетов: `pub get`
в папке пакета разрешает и dev-зависимости, а `lints ^6.1.0`
и `flutter_lints ^6.0.0` хотят Dart 3.8.0 и выше. Задания переведены
на `stable` — как и локально. Пол (`sdk: ^3.6.0`) остаётся никем
не проверенным: для этого нужен отдельный пакет-потребитель на прибитом 3.6.0.

## solo: рецепт «своя доставка»

На вопрос владельца, нужен ли аналог `SoloListenable` для чистого Dart, ответ —
класс не нужен, нужен рецепт. Отчёт —
`2026-09-12[5]-own-delivery-recipe-report.md`.

Точка расширения `SoloBase.publish` была и работала, но не упоминалась
ни в README, ни в `doc/` — только в `docs/architecture.md` и в записи замысла.
Секция `### A delivery of your own` в `doc/state.md` и перевод закрывают это:
наследник со слушателями, вызываемыми внутри изменения, и таблица трёх правил
такого переопределения. Фрагмент собран и запущен вне дерева; прогон нашёл
в первой версии рецепта пропущенную проверку снятого по ходу прохода слушателя.

API не добавлялось.

## flutter_solo: контроллер без стрима

Поручение владельца: «в `SoloListenable` stream не нужен, только интерфейс
`ValueListenable`». Отчёт — `2026-09-12[4]-listenable-on-base-report.md`.

`SoloListenable<S> extends SoloBase<S> implements ValueListenable<S>`. Ушли
`StreamController.broadcast()` на экземпляр, `add` на каждое изменение
и закрытие стрима; слушатели остались единственной доставкой. Решение
2026-09-02 «цепочка линейная, а не вилка от базы» отменено, шапка
`2026-09-02[1]-solo-design.md` помечена.

Ломающее: контроллер больше не подходит туда, где объявлен `Solo<S>`, — общий
тип теперь `SoloBase<S>`. Запись под `## Unreleased` `flutter_solo`, версию
не трогали.

## solo: чтение контроллера — `currentState`

Поручение владельца: сделать так, чтобы разработчик не писал `state` вместо
`ctx.state`. Из четырёх способов выбран один — убрать имя из области видимости,
с объяснением в README. Отчёт — `2026-09-12[3]-current-state-rename-report.md`.

`SoloBase.state` стал `SoloBase.currentState`. Тело задачи — замыкание внутри
метода контроллера, поэтому `state` в нём был виден и читался без чекпойнта;
теперь это ошибка компиляции (`undefined_identifier`, проверено черновым
пакетом). Чтения через контекст, параметры правил и `externalSetState`
не тронуты; `SoloListenable.value` остался и отдаёт `currentState`.

Устаревшего синонима нет намеренно: он компилировался бы в теле и оставил бы
дыру открытой. Записи `**Breaking:**` легли под `## Unreleased` обоих пакетов,
версии не трогали.

## flutter_solo: два импорта

Поручение владельца: расширения, связанные с listenable, — отдельным экспортом;
и взять из `scopo` то, что позволяет не держать `select` в `State`. Сделано,
отчёт — `2026-09-12[2]-listenable-export-report.md`.

- `package:flutter_solo/flutter_solo.dart` — `SoloListenable`, `SoloSelection`,
  новый виджет `SoloSelector` и весь `solo`;
- `package:flutter_solo/listenable.dart` — методы `select` и `listen`,
  `SoloSubscription`, `SoloSubscriptions`.

Расширения вернулись на типы фреймворка (`ValueListenable` и `Listenable`),
а `SoloSelection` стала проекцией над любым `ValueListenable`. Столкновение
с одноимёнными расширениями `scopo` теперь решает импорт, а не сужение
получателя.

Версию не трогали: записи легли под `## Unreleased`.

## Текущая работа

Публичная документация перестроена. Устройство и правило —
`2026-09-11[16]-docs-structure-design.md`: **секция начинается с кода**, README
становится стартовой страницей, справочный материал уезжает в `doc/`
по странице на предмет. Повод — README `solo` на 1519 строк и замер: 17 его
секций из 41 не содержали кода вовсе.

Сделано для всех трёх пакетов.

| Файл | было | стало | доля кода |
| --- | --- | --- | --- |
| `packages/solo/README.md` | 1519 | 286 | 40% → 60% |
| `packages/async_job/README.md` | 808 | 166 | 30% → 35% |
| `packages/flutter_solo/README.md` | 325 | 319 | 51% |

У `solo` девять новых страниц `doc/` плюс две прежние, у `async_job` — шесть.
Зеркала в `docs/ru/solo/` и `docs/ru/async_job/`. `PAIRS`
в `tool/check_translations.py` выросли с пяти пар до двадцати. `flutter_solo`
переносить не понадобилось: заметки в конце его README стали таблицей, и всё.

Правило закреплено новой проверкой `tool/check_doc_shape.py` и описано
в `docs/conventions.md`. Прогонять её вместе с остальными.

Каждый фрагмент, добавленный в этой работе, собран и запущен отдельным проектом
с путевой зависимостью до того, как попал в документ. Так нашлась ошибка:
пример работы с очередью был написан снаружи контроллера, а `queue` помечен
`@protected`.

Сайт документации собран на **Astro Starlight**, выбор владельца. Живёт
в `site/`, устройство — в `site/README.md`.

Главное про него: страницы сайта **генерируются**, а не хранятся.
`python3 tool/build_site.py` читает README и `doc/` пакетов, снимает заголовок
`#` в frontmatter, переписывает ссылки между файлами в ссылки между страницами
и складывает результат в `site/src/content/docs/`. Эта папка в `.gitignore`:
источник правды — сами пакеты, разойтись сайту не с чем. Ссылка «Edit this
page» ведёт на исходный файл в пакете, а не на копию.

Настройки в одном файле `site/site.json`, его читают и скрипт, и конфиг Astro.
Пока там `base: "/solo"` — адрес проектной страницы `vi-k.github.io/solo/`. Под
свой домен меняются два поля (`base` на `/`, `site` на домен), в `site/public/`
кладётся `CNAME`, и владелец сам добавляет DNS-записи; порядок записан
в `site/README.md`.

Поиск встроенный (Pagefind, индексирует собранный HTML при сборке, без сервисов
и ключей), по каждому языку отдельно.

Для агентов стоит `starlight-llms-txt`: `/llms.txt` — точка входа,
`/llms-full.txt` — вся английская документация простым текстом (около 195 КБ),
`/llms-small.txt` — то же без сносок. Только английский: плагин берёт локаль
по умолчанию. Текст шапки `llms.txt` задан в `site/astro.config.mjs`.

При установке плагина всплыло, что версии в `package.json` я выписал по памяти:
стояли `astro ^5` и `starlight ^0.36`, а плагину нужен Starlight не ниже 0.41.
Подняли до `astro 7.3` и `starlight 0.42`; сборка и вид страниц не изменились,
проверено в браузере. Версии выяснять через `npm view <пакет> version`,
а не по памяти.

**Сайт двуязычный.** Решение владельца, принято после того, как сайт уже встал
на английском. Английский — корневая локаль, русский под `/ru/`; берётся
из `README.ru.md` пакетов и `docs/ru/<пакет>/`. Переключатель языка и русский
интерфейс даёт сам Starlight, пары страниц сходятся потому, что их держит
`check_translations.py`. На pub.dev это не влияет: `.pubignore` и раскладка
переводов прежние; записано в `AGENTS.md`.

Публикует `.github/workflows/site.yml` при пуше в `main`, задевающем README,
`doc/`, `site/` или сборщик. Settings → Pages → Source стоит на «GitHub
Actions»: переключено перед первым запуском, дальше сайт выкладывается сам.

Версии экшенов подняты 2026-09-12: `checkout@v7`, `setup-node@v7`,
`upload-pages-artifact@v5`, `deploy-pages@v5`. До этого стояли `v4`, `v4`, `v3`
и `v4`, и GitHub уже писал на них «Node.js 20 is deprecated» — когда поддержку
снимут, деплой встанет. Версии выяснены через
`gh api repos/<экшен>/git/matching-refs/tags/v`, а не по памяти: то же правило,
что и для `npm view <пакет> version`. `node-version: 22` оставлен как был.

Домен привязан: **https://docs.yet-another.dev/**. DNS — одна запись
`CNAME docs -> vi-k.github.io` у porkbun, апекс не тронут (он занят
репозиторием `flutter_curves`). Сертификат Let's Encrypt выпущен, Enforce HTTPS
включён.

Подвох, на который налетели: при деплое через GitHub Actions файл `CNAME`
из артефакта **не читается** как настройка. Домен задаётся настройкой Pages —
руками в Settings или `gh api -X PUT repos/vi-k/solo/pages -f cname=...`.
`site/public/CNAME` оставлен как запись намерения.

Ссылки на сайт стоят в README всех трёх пакетов, в их переводах (на `/ru/`)
и в корневом README.

Сводный список по трём сторонним ревью закрыт целиком: девять пунктов сделаны,
пункт 7 отклонён владельцем. Отчёты — от `2026-09-11[9]-review-fixes-report.md`
до `2026-09-11[15]-drain-report.md`.

**Вернулись три `pubspec_overrides.yaml`** — у `solo`, у его примера
и у `flutter_solo`. Правка `join` лежит в `async_job` и не опубликована, а тест
на неё в `solo` идёт через ядро; без оверрайда монорепозиторий её не видит. Это
обычное состояние разработки между выпусками. **При подготовке следующего
выпуска их снова снимать**, и удалённые файлы добавлять в индекс — иначе
`pub publish` ругается на то, чего в дереве уже нет; порядок
в `2026-09-11[1]-solo-release-readiness-report.md`.

Версии в `pubspec.yaml` не трогали: записи легли под `## Unreleased` в трёх
`CHANGELOG.md`. Номер версии — часть подготовки выпуска.

## Завершённый timing

Владелец поручил: «продолжи debounce и throttle». Реализация завершена
по `2026-09-10[12]-accumulation-timing-design.md`
и `2026-09-10[16]-accumulation-timing-plan.md`, база — `9493c31`.

- Обычный debounce без maxWait закрывает группу после паузы событий.
- Throttle отсчитывает интервал от фактического старта предыдущей группы
  того же накопителя. Первый старт готов сразу, следующие — по интервалу.
- Collect хранит все события; accumulate сохраняет результат merge.
- Ожидающие группы остаются в queue и пропускают готовые посторонние Job.
  Выполнение обработчиков, детей и уборки остаётся последовательным.
- Timing необязателен; Duration.zero сохраняет поведение без таймеров.
- Close отменяет таймеры и очередь без досылки.

Алгоритм и все тесты готовы. Основные timing-тесты: 10; отдельные
lifecycle-тесты: 46. Полный solo: 433 зелёных, анализ чистый. Пример solo: 9
тестов; flutter_solo: 8; анализ обоих и Flutter-примера чистый. Извлечённые
рецепты и README Search проверены с FakeAsync. Dartdoc solo без предупреждений
и ошибок; формат и переводы проверены.

Task review алгоритма завершён после исправления лишней пустой прокачки
throttle и добавления непрерывного debounce-теста. Обе находки закрыты.
Финальное ревью закрыто после уточнения dartdoc collect: снимок создаётся при
закрытии состава. Открытых замечаний нет. Код, тесты и документы включены
в main общим коммитом; незавершённых поручений по timing нет. Отчёт:
`2026-09-10[17]-accumulation-timing-report.md`. Итоги ревью сохранены в отчёте,
папка исполнения SDD удалена. Выполненный пункт окна задержки удалён
из backlog; буфер и досылка остаются отдельной работой.

Последнее уточнение владельца: README должен объяснять API новому читателю, без
ссылок на прежнее поведение и контекст диалога. В обоих README async_job убрано
«run не создаёт другой Job» и сравнение с отдельными join/check. Описываются
запуск, ожидание и проверка отмены. Сохранять removeDisposer и пример ленивого
лога из `479e934`.

## Основа: ctx.run

Переход ctx.run на Future<T> завершён в `9493c31`. Метод синхронно запускает
дочерний Job, ждёт child.value и проверяет отмену родителя перед выдачей
успешного результата в активное тело. Защищённый startChild отделяет запуск
от ожидания; each и SoloBase.run возвращают хэндл сразу. Future от ctx.run
нужно ожидать или обработать. План: `2026-09-10[14]-context-run-plan.md`. Отчёт
и ревью: `2026-09-10[15]-context-run-report.md`. Повторно эту работу
не начинать.

## Накопители и границы работы

Базовые collect/accumulate реализованы в `ff5ed8d`; повторно их не создавать.
Состояние меняет обработчик через ctx.emit, а collect собирает вход в список
и accumulate сразу применяет чистую merge. Совместимость групп определяется
идентичностью накопителя, не key.

Adjacent использует текущий хвост; join сохраняет позицию последней открытой
группы; replace переносит данные в новый Job в хвосте, обязательно отменяя
прежний даже при cancellable:false. Новая группа видна до хуков отмены старой.
Закрытые и работающие группы не дополняются.

Базовые записи: `2026-09-10[7]-event-accumulation-design.md`,
`2026-09-10[8]-event-accumulation-plan.md`,
`2026-09-10[9]-event-accumulation-report.md`. Рецепт:
`packages/solo/doc/accumulation.md`, перевод `docs/ru/solo/accumulation.md`.

Досылка при закрытии, надёжный буфер и автоматические повторы остаются
отдельной работой. Аккумуляция через externalSetState отклонена:
`2026-09-10[6]-state-accumulation-design.md` не реализовывать. Then, each
и вычитка README ядра завершены; повторно не начинать:
`2026-09-10[2]-job-then-report.md`, `2026-09-09[18]-each-child-report.md`,
`2026-09-09[14]-async-job-readme-report.md`.

## Требования к README, которые нужно сохранить

- При описании API на русском называть `Job` прямо. Слово «задача» уместно для
  самой работы, но не должно подменять имя класса в объяснении его
  возможностей.
- `ctx.join(Database.open)` объясняется самостоятельно до сравнения с `wait`:
  вызывает операцию, ждёт её завершения и проверяет отмену. `wait`
  предлагается, когда нужно прекратить ожидание сразу при отмене.
- `discard` после `return` объясняется на примере ожидания дочерней задачи:
  тело вернулось, но весь `Job` ещё не завершился.
- В примере миграции `onCancel` передаёт сигнал токену, `join` ждёт фактической
  остановки, а `uncancellable` защищает шаг от самого сигнала.
- Внешние асинхронные операции в теле следует ожидать через контекст. Прямой
  `await` допустим, но сам не проверяет отмену. Внутри действия, переданного
  контексту, и при уборке обычный `await` уместен с учётом описанных в README
  границ.
- Упоминание `solo` следует из границ ядра: в `async_job` нет управления
  состоянием, очереди и правил планирования; `solo` добавляет их.
- Два примера проброса `Cancelled` показывают отдельную ветку `catch`
  и проверку типа внутри общего обработчика. Оба способа подходят и для
  `Exception`, и для `Object`; выбор типа не различает эти способы.
- Принятая отмена самого `Job` сохраняется после перехвата исключения. Код
  после `catch` может выполниться, следующая контрольная точка снова бросает
  отмену, а при завершении исход остаётся `Cancelled`. Перехваченный
  `Cancelled` от операции или дочерней задачи сам по себе родителя не отменяет:
  возможен `Done`.

Число блоков кода в README больше не годится как ориентир: материал разъехался
по страницам `doc/`. Форму держат `check_translations.py` (двадцать пар)
и `check_doc_shape.py`. Примеры `then`, его контракт и переводы обновлены
вместе с реализацией.

## Состояние API и версий

`async_job` 0.2.0, `solo` 0.2.0 и `flutter_solo` 0.2.0 опубликованы на pub.dev.
Релизные коммиты — 547819a, a71641a и bd26a35; теги — async_job-v0.2.0,
solo-v0.2.0 и flutter_solo-v0.2.0.

В текущем API `job.whenCancelled(callback)` регистрирует синхронный обработчик,
передаёт ему весь `Cancelled` и возвращает функцию снятия. Поздняя регистрация
вызывает обработчик сразу. Это больше не future. Реализация и ревью:
`2026-09-09[11]-when-cancelled-report.md`.

`CancelReason` — открытый абстрактный класс. Встроенные причины ядра:
`ManualCancelReason`, `ParentCancelReason`, `HandlerCancelReason`,
`ChainCancelReason`. У `solo`: `RulesCancelReason`, `ClosedCancelReason`,
основа `SoloCancelReason`. Свои классы могут хранить ошибку, стектрейс и другие
данные. `name` служит меткой журнала; проверка по типу, равенства по имени нет.
Переданная в `cancel(reason: ...)` причина сохраняет идентичность. Связи
`ParentCancelReason.cause` и `HandlerCancelReason.cause` сохраняют исходный
`Cancelled` при каскаде и распространении отмены ребёнка.
`ChainCancelReason.cause` сохраняет отмену соседнего звена `then`. Реализация
и ревью: `2026-09-09[13]-cancel-reason-report.md`.

`solo` зависит от `async_job: ^0.2.0`, `flutter_solo` от `solo: ^0.2.0`.
Оверрайдов в семействе не осталось. `solo`, `flutter_solo` и их примеры берут
опубликованные пакеты с pub.dev. Оба примера видят свой пакет по пути:
`solo: path: ../` и `flutter_solo: path: ../` прямо в `pubspec.yaml`, без
`pubspec_overrides.yaml`. Так проверки примера работают против дерева,
а не против выпущенной версии, и `example/` внутри архива всё равно разрешается
у скачавшего пакет — проверено на копии вне репозитория.

Когда семейство снова разъедется по версиям, оверрайд ядра или `solo` вернётся
на время подготовки следующего выпуска. Тогда же помнить: `pub` смотрит индекс
git, и удаление файла оверрайда без `git add` оставляет предупреждение
в dry-run.

Порядок выпусков был: solo, затем flutter_solo. Оба выпущены, незакрытых
хвостов выпуска нет.

Dev-зависимости линтов подняты 2026-09-12 разом во всех пяти местах: `lints`
5.1.1 → 6.1.0 у `async_job`, `solo` и примера `solo`, `flutter_lints` 5.0.0 →
6.0.0 у `flutter_solo` и его примера. Новых правил три —
`strict_top_level_inference`, `unnecessary_underscores`,
`use_null_aware_elements`, — и ни одно на этом коде не сработало: анализ чист
во всех пяти, 825 тестов зелёные, формат не поплыл. Записи в `CHANGELOG.md` нет
намеренно: dev-зависимость не входит в разрешение у потребителя.

Подвох, который при этом появился: `lints` 6 и `flutter_lints` 6 требуют
`sdk: ^3.8.0`, а пакеты заявляют `^3.6.0`. Потребителя это не задевает —
dev-зависимости к нему не едут, и `pub publish --dry-run` на несовпадение
молчит. Задевает того, кто возьмётся править репозиторий на Dart 3.6 или 3.7:
у него `pub get` не разрешится. Если такой пол для работы над пакетами нужен,
поднимать линты нельзя, не подняв заодно пол самих пакетов.

Замечание для подготовки flutter_solo: удалить файл оверрайда мало. Пока
удаление не попало в индекс git, `dart pub publish --dry-run` продолжает
считать файл частью пакета и предупреждает о checked-in и ignored файлах.

## Проверки и дерево

Работа в main. Наборы после шага 1 по замораживанию: `packages/solo` — 526,
`packages/solo/example` — 9, `packages/flutter_solo` — 87; анализ чист везде,
`dart format --set-exit-if-changed` не меняет ничего. Ниже — проверки
документов, они от той волны правок, когда кода работа не трогала.

- `python3 tool/check_translations.py` — двадцать пар совпадают;
- `python3 tool/check_line_width.py` — ни одной строки шире 79;
- `python3 tool/check_doc_shape.py` — каждая секция начинается с кода или
  таблицы;
- `python3 tool/doc_snippets.py` — 22 файла из `doc/vs-bloc.md`; собирать
  в чистый каталог, прогонять **все** драйверы обоих пакетов и читать вывод:
  часть из них падает нарочно, когда сторожимое свойство пропадает;
- `python3 tool/build_site.py && npm run build` в `site/` — 22 страницы,
  Pagefind проиндексировал 21; страницы и ссылки проверены в браузере;
- `dart analyze` в `solo` и `async_job`, `flutter analyze` в `flutter_solo` —
  чисто;
- все новые фрагменты собраны и запущены отдельными проектами с путевой
  зависимостью.

Ширину `AGENTS.md` и изменённых записей проверять отдельно: общий скрипт их
пропускает.

Для команд использовать `rtk`, для неподдерживаемых команд `rtk proxy`.
`git diff --check` выполнять через `rtk proxy git diff --check`: фильтр
`rtk git diff` может скрыть диагностический вывод. Обёртки Dart и Flutter
требуют доступ к SDK и кешам вне песочницы; автоматическое ревью разрешило эти
проверки через escalation.

## Дальше, когда владелец выберет новую работу

Апекс `yet-another.dev` снова работает — проверено 2026-09-12: страница отдаёт
`<base href="/">`, `main.dart.js` приходит с кодом 200. Прежняя запись здесь
называла `vi-k/flutter_curves` чужим репозиторием — это неверно, он владельца,
и починен там же пересборкой с `--base-href /`; разбор — в его
`docs/records/2026-09-12[4]-pages-deploy-report.md`.

Рецепт паузы очереди написан 2026-09-12: раздел «Pausing the queue»
в `packages/solo/doc/jobs.md` и его перевод, плюс строка в таблице рецептов
обоих README. Четыре обещания раздела закреплены тестом
`packages/solo/test/queue_pause_recipe_test.dart`: ворота держат очередь
и `resume` пускает её в порядке добавления; `queue.clear()` очередь опустошает,
а паузу оставляет; `close()` возвращается без `resume`;
`cancelAll(force: true)` уносит ворота вместе со всем, и очередь идёт дальше.
Тест сторожит не рецепт, а поведение очереди, на которое рецепт опирается.

Материал и замеры: `2026-09-09[9]-todo-review.md`. Готовый стенд может лежать
в `.artifacts/2026-09-09-wave2-review/a/stand`; восстановление —
`python3 tool/doc_snippets.py <каталог>`. При проверке внешней операции
дожидаться самой операции: снимок состояния сразу после отмены `Job`
не показывает её окончательный результат.

Пункт бэклога про отладочную диагностику закрыт 2026-09-12 **рецептом,
а не API**. Меряется задержка между тем, как отмена стала действующей,
и исходом задачи: наблюдатель отмечает время в `Job.whenCancelled` и вычитает
в `onFinish`, отметка живёт в `Expando`. Зона, считающая таймеры, отвергнута —
её вокруг тела нет вовсе, а таймеры не признак. Хук в ядре был сделан
и откачен: выяснилось, что связка уже работала на публичном API, и ломающее
изменение в трёх классах покупало удобство в пять строк. Разбор —
`2026-09-12[1]-cancel-delay-report.md`.

Пожелания владельца находятся в `docs/backlog.md`. После окончания текущей
реализации предложить выбрать оттуда работу; самостоятельно записи не менять.
Другие ранее отложенные примеры для `solo`: `stateAs`, `check`, retry/backoff.
Их брать только после выбора владельца.

На 2026-09-12 `info@efa.dev` молчит — владелец проверил. Напомнить о нём снова
**начиная с 2026-09-19, но не раньше**, при старте сессии или выборе новой
работы. Если к 2026-09-26 ответа так и нет, напомнить о пересылке
в `support@pub.dev`. Переписку самостоятельно не отправлять. После решения
обновить `2026-09-05[8]-job-name-request-report.md` и убрать напоминание. Это
не блокирует нынешнее имя `async_job`.

## Ограничения и общение

- Не использовать всплывающие формы вопросов: владелец сообщил, что они
  появляются и сразу исчезают, ответить невозможно. Вопросы писать обычным
  сообщением в чате и ждать ответа. Причина исчезновения не установлена:
  инструмент подтвердил приём вопроса (`accepted`), но не сообщал о закрытии
  формы или ответе через неё.
- Не приписывать владельцу ответы на формы или выбор вариантов, которые он
  не давал. Собственную интерпретацию запроса называть своей.
- Владелец просит минимум служебного вывода: ему нужны сообщения по делу,
  а не журналы запусков. Заголовки инструментов формирует интерфейс; не обещать
  скрыть их, сокращать число вызовов и объём вывода.
- Текст рабочих документов переносить на 79 символах. Правило и порядок
  проверок сохранены в `AGENTS.md`.
- Английский README и русский перевод править вместе. Связность и смысл важнее
  косметического сокращения; подтверждённые условия не выкидывать.
- Не менять заодно согласованную семантику отмены.
- `ctx.run` принимает `Job<T>`, а не только `DeferredJob<T>`: библиотечные
  классы на основе `JobBase` тоже могут быть дочерними. Автоматически
  запускающийся обычный `Job` отвергается.
- Не расширять защищённые `owner` и `parent` без подтверждённой нужды
  потребителя. `isDisposing` не выносить в публичный `Job` без нового решения.
- Буфер ранних событий в `each` сохранять; прежние альтернативы теряли события
  при подписке. При ошибке тела дочерние задачи дорабатывают; при самоотмене
  тела отмена передаётся детям.
- Соседний `scopo` ведётся отдельно. Его прежний переход на опубликованное ядро
  0.1.0 выполнен; обновление на новый API не входит в эту работу. Архивирование
  `stream_future` тоже отдельная внешняя задача.
- `/Users/user/development/yandex/yx_camera` не трогать: это чужой проект.
- Не запускать push, публикацию, теги или релизные коммиты без отдельного
  поручения. Правила связки публикации полностью заданы в `AGENTS.md`.

Большая прежняя версия handoff сохранена в Git на `659512a`. Подготовка
к текущему рестарту описана в `2026-09-10[11]-debounce-restart-report.md`.
