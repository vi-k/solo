# Handoff: текущее состояние проекта

Обновлено: 2026-09-11, выпуски 0.2.0 завершены; сводный список закрыт,
по пункту 7 спека на ревью; оверрайды разработки вернулись.

## Текущая работа

Три сторонних ревью разобраны, сводный список с приоритетами —
`2026-09-11[8]-external-reviews-plan.md`. Пункты 1–4 сделаны и
задокументированы в `2026-09-11[9]-review-fixes-report.md`:

- `ctx.join` в `async_job` отпускает значение, чем бы ни бросил чекпойнт
  после действия, а не только на `Cancelled`;
- бросивший слушатель `SoloListenable` уходит в
  `FlutterError.reportError`, проход продолжается, и переоценка правил в
  `_setState` больше не срывается;
- рассылка слушателей линейна: рядом со списком счётчик регистраций
  вместо `contains` по всему списку;
- `Policy.droppable` бросает `ArgumentError` до того, как пометит новую
  задачу добавленной, вместо `TypeError` после её похорон.

Каждая правка закрыта регрессионным тестом на тот путь, которым находка
была найдена. Ядро `SoloBase` не трогали: дартдок говорит, что `publish`
— дело подкласса, и это по-прежнему верно.

**Вернулись три `pubspec_overrides.yaml`** — у `solo`, у его примера и у
`flutter_solo`. Правка `join` лежит в `async_job` и не опубликована, а
тест на неё в `solo` идёт через ядро; без оверрайда монорепозиторий её
не видит. Это обычное состояние разработки между выпусками. **При
подготовке следующего выпуска их снова снимать**, и удалённые файлы
добавлять в индекс — иначе `pub publish` ругается на то, чего в дереве
уже нет; порядок в `2026-09-11[1]-solo-release-readiness-report.md`.

Версии в `pubspec.yaml` не трогали: записи легли под `## Unreleased` в
трёх `CHANGELOG.md`. Номер версии — часть подготовки выпуска.

Пункт 6 тоже сделан — `2026-09-11[10]-selection-report.md`:
`SoloSelection<S, T>` и расширение `select` на `SoloListenable`.
Расширение, а не член класса: `select` — имя, которое контроллер домена
захочет себе, а новый член базового класса ломает подкласс. `value`
читает состояние каждый раз, хранимое значение служит только отсечке
уведомлений. Сравнение — `compare`, `true` значит «изменилось», как в
`scopo`.

Пункт 5 сделан по выбранному владельцем варианту Б —
`2026-09-11[11]-error-handler-report.md`. Появился `SoloBase.errorHandler`
(`SoloErrorHandler`), статический, один на процесс. Наблюдатель теперь
только наблюдает: без обработчика бездомная ошибка идёт в зону, с ним —
к нему. Ломающее. Миграция понадобилась и самому репозиторию: `runSolo`
рядом с журналом ставит `journal.answerForErrors`, иначе девять тестов
ловят ошибки в зоне `fakeAsync`.

Там же разобрано, что взято из `scopo/lib/src/utils/listenable`: имя и
полярность `compare` (в проекции было `equals` с обратным смыслом),
порядок записи выбранного значения и находка про бросившего слушателя.
Не взяты `ListenableSelector`, `ListenableView` и `StateAsNotifier`.

Связка `listen` перенесена по отдельной просьбе владельца —
`2026-09-11[12]-subscription-report.md`: `SoloSubscription`,
`SoloSubscriptions` и `listen` на контроллере и на проекции. Висит не на
`Listenable`, а на собственных типах пакета, и типы названы не как в
`scopo`: два расширения с одним именем члена на один тип — ошибка
компиляции, и проект владельца с обоими пакетами перестал бы собираться.
Прогон в записи. Повторная отмена здесь ничего не делает, а не жалуется
ассертом, как в `scopo`, — по укладу репозитория.

Пункт 8 сделан — `2026-09-11[13]-transition-report.md`. Хуки изменения
берут `SoloTransition<S>` вместо пары состояний: `previous`, `current`,
`job` (автор изменения, `null` для внешнего, у дочерней это она сама) и
`revision`. Замена, а не второй хук: держать два хука об одном событии
навсегда хуже, а ломающим это было бы в любом случае. `publish` не
тронут — это канал доставки, и его сигнатуру держит `flutter_solo`.
Ломающее.

Пункт 9 сделан — `2026-09-11[14]-pending-report.md`. `SoloBase.pending`
отдаёт `SoloPending`: задача, фаза (`body`, `children`, `cleanup`,
`unknown`), несомая отмена, придержана ли она секцией, отклоняет ли
задача отмены. `unknown` — намеренно: тело на голом `await` движку не
видно, и догадка туда не пишется. В `async_job` для этого открыт
`JobBase.inUncancellableSection`, `@protected`, ломающее по правилу о
новом члене базового класса.

Запись `docs/backlog.md` про таймеры мимо контрольной точки этим **не**
закрыта: там другой механизм — зона вокруг тела, считающая созданные им
таймеры.

Пункт 10 сделан — `2026-09-11[15]-drain-report.md`.
`close({SoloCloseMode mode})`: `cancel` — прежнее поведение, `drain`
выполняет очередь и закрывается после неё. `isClosed` истинно с вызова,
так что `add` отказывает новым сам; поменялись два места в насосе, оба
обязаны были. Таймеры при досылке не отменяются — иначе группа debounce
никогда не станет готовой и досылка зависнет. Признак конца —
`_queue._jobs.isEmpty`, а не «нет готовой задачи». Обычный `close()`
поверх досылки её останавливает; обёртки `Solo` и `SoloListenable` учтены
— они кешировали future и до движка эскалация бы не дошла. Ломающее в
обоих пакетах: параметр ломает переопределения `close`, вызовы целы.

Запись `docs/backlog.md` про буфер событий удалена по слову владельца:
ядро даёт только досылку, буфер строится снаружи. Пункт из `TODO.txt`
«запустить последнее событие при диспоузе» закрыт целиком.

По пункту 7 написана спека — `2026-09-11[16]-declared-operation-design.md`.
Кода нет и в работу она не бралась. Суть: объявлять операцию полем
контроллера, `late final _load = operation<W, A, T>(...)`, а публичный
метод оставлять обычным. Идентичность несёт сам объект, поэтому тип
результата связан с операцией по построению, а политику нельзя задать
разную для одной операции. Прецедент в пакете уже есть — `collect` и
`accumulate` объявляются ровно так.

В спеке восемь открытых вопросов; главный — идентичность операции против
идентичности запроса: `_load('a')` и `_load('b')` при `droppable` сегодня
были бы одной операцией. Спека ждёт независимого ревью перед планом.

## Завершённый timing

Владелец поручил: «продолжи debounce и throttle». Реализация завершена
по `2026-09-10[12]-accumulation-timing-design.md` и
`2026-09-10[16]-accumulation-timing-plan.md`, база — `9493c31`.

- Обычный debounce без maxWait закрывает группу после паузы событий.
- Throttle отсчитывает интервал от фактического старта предыдущей группы
  того же накопителя. Первый старт готов сразу, следующие — по интервалу.
- Collect хранит все события; accumulate сохраняет результат merge.
- Ожидающие группы остаются в queue и пропускают готовые посторонние Job.
  Выполнение обработчиков, детей и уборки остаётся последовательным.
- Timing необязателен; Duration.zero сохраняет поведение без таймеров.
- Close отменяет таймеры и очередь без досылки.

Алгоритм и все тесты готовы. Основные timing-тесты: 10; отдельные
lifecycle-тесты: 46. Полный solo: 433 зелёных, анализ чистый.
Пример solo: 9 тестов; flutter_solo: 8; анализ обоих и Flutter-примера
чистый. Извлечённые рецепты и README Search проверены с FakeAsync.
Dartdoc solo без предупреждений и ошибок; формат и переводы проверены.

Task review алгоритма завершён после исправления лишней пустой прокачки
throttle и добавления непрерывного debounce-теста. Обе находки закрыты.
Финальное ревью закрыто после уточнения dartdoc collect: снимок создаётся
при закрытии состава. Открытых замечаний нет. Код, тесты и документы
включены в main общим коммитом; незавершённых поручений по timing нет.
Отчёт: `2026-09-10[17]-accumulation-timing-report.md`.
Итоги ревью сохранены в отчёте, папка исполнения SDD удалена.
Выполненный пункт окна задержки удалён из backlog; буфер и досылка
остаются отдельной работой.

Последнее уточнение владельца: README должен объяснять API новому
читателю, без ссылок на прежнее поведение и контекст диалога. В обоих
README async_job убрано «run не создаёт другой Job» и сравнение
с отдельными join/check. Описываются запуск, ожидание и проверка отмены.
Сохранять removeDisposer и пример ленивого лога из `479e934`.

## Основа: ctx.run

Переход ctx.run на Future<T> завершён в `9493c31`.
Метод синхронно запускает дочерний Job, ждёт child.value и проверяет
отмену родителя перед выдачей успешного результата в активное тело.
Защищённый startChild отделяет запуск от ожидания; each и SoloBase.run
возвращают хэндл сразу. Future от ctx.run нужно ожидать или обработать.
План: `2026-09-10[14]-context-run-plan.md`.
Отчёт и ревью: `2026-09-10[15]-context-run-report.md`.
Повторно эту работу не начинать.

## Накопители и границы работы

Базовые collect/accumulate реализованы в `ff5ed8d`; повторно их
не создавать. Состояние меняет обработчик через ctx.emit, а collect
собирает вход в список и accumulate сразу применяет чистую merge.
Совместимость групп определяется идентичностью накопителя, не key.

Adjacent использует текущий хвост; join сохраняет позицию последней
открытой группы; replace переносит данные в новый Job в хвосте,
обязательно отменяя прежний даже при cancellable:false. Новая группа
видна до хуков отмены старой. Закрытые и работающие группы не дополняются.

Базовые записи: `2026-09-10[7]-event-accumulation-design.md`,
`2026-09-10[8]-event-accumulation-plan.md`,
`2026-09-10[9]-event-accumulation-report.md`.
Рецепт: `packages/solo/doc/accumulation.md`, перевод
`docs/ru/solo/accumulation.md`.

Досылка при закрытии, надёжный буфер и автоматические повторы остаются
отдельной работой. Аккумуляция через externalSetState отклонена:
`2026-09-10[6]-state-accumulation-design.md` не реализовывать.
Then, each и вычитка README ядра завершены; повторно не начинать:
`2026-09-10[2]-job-then-report.md`,
`2026-09-09[18]-each-child-report.md`,
`2026-09-09[14]-async-job-readme-report.md`.

## Требования к README, которые нужно сохранить

- При описании API на русском называть `Job` прямо. Слово «задача»
  уместно для самой работы, но не должно подменять имя класса в объяснении
  его возможностей.
- `ctx.join(Database.open)` объясняется самостоятельно до сравнения
  с `wait`: вызывает операцию, ждёт её завершения и проверяет отмену.
  `wait` предлагается, когда нужно прекратить ожидание сразу при отмене.
- `discard` после `return` объясняется на примере ожидания дочерней
  задачи: тело вернулось, но весь `Job` ещё не завершился.
- В примере миграции `onCancel` передаёт сигнал токену, `join` ждёт
  фактической остановки, а `uncancellable` защищает шаг от самого сигнала.
- Внешние асинхронные операции в теле следует ожидать через контекст.
  Прямой `await` допустим, но сам не проверяет отмену. Внутри действия,
  переданного контексту, и при уборке обычный `await` уместен с учётом
  описанных в README границ.
- Упоминание `solo` следует из границ ядра: в `async_job` нет управления
  состоянием, очереди и правил планирования; `solo` добавляет их.
- Два примера проброса `Cancelled` показывают отдельную ветку `catch`
  и проверку типа внутри общего обработчика. Оба способа подходят
  и для `Exception`, и для `Object`; выбор типа не различает эти способы.
- Принятая отмена самого `Job` сохраняется после перехвата исключения.
  Код после `catch` может выполниться, следующая контрольная точка снова
  бросает отмену, а при завершении исход остаётся `Cancelled`.
  Перехваченный `Cancelled` от операции или дочерней задачи сам по себе
  родителя не отменяет: возможен `Done`.

Оба README ядра имеют по 21 блоку кода, оба README `solo` — по 28.
Примеры `then`, его контракт и переводы обновлены вместе с реализацией.

## Состояние API и версий

`async_job` 0.2.0, `solo` 0.2.0 и `flutter_solo` 0.2.0 опубликованы на
pub.dev. Релизные коммиты — 547819a, a71641a и bd26a35; теги —
async_job-v0.2.0, solo-v0.2.0 и flutter_solo-v0.2.0.

В текущем API `job.whenCancelled(callback)` регистрирует синхронный
обработчик, передаёт ему весь `Cancelled` и возвращает функцию снятия.
Поздняя регистрация вызывает обработчик сразу. Это больше не future.
Реализация и ревью: `2026-09-09[11]-when-cancelled-report.md`.

`CancelReason` — открытый абстрактный класс. Встроенные причины ядра:
`ManualCancelReason`, `ParentCancelReason`, `HandlerCancelReason`,
`ChainCancelReason`.
У `solo`: `RulesCancelReason`, `ClosedCancelReason`, основа
`SoloCancelReason`. Свои классы могут хранить ошибку, стектрейс и другие
данные. `name` служит меткой журнала; проверка по типу, равенства по имени
нет. Переданная в `cancel(reason: ...)` причина сохраняет идентичность.
Связи `ParentCancelReason.cause` и `HandlerCancelReason.cause` сохраняют
исходный `Cancelled` при каскаде и распространении отмены ребёнка.
`ChainCancelReason.cause` сохраняет отмену соседнего звена `then`.
Реализация и ревью: `2026-09-09[13]-cancel-reason-report.md`.

`solo` зависит от `async_job: ^0.2.0`, `flutter_solo` от `solo: ^0.2.0`.
Оверрайдов в семействе не осталось. `solo`, `flutter_solo` и их примеры
берут опубликованные пакеты с pub.dev. Оба примера видят свой пакет по
пути: `solo: path: ../` и `flutter_solo: path: ../` прямо в
`pubspec.yaml`, без `pubspec_overrides.yaml`. Так проверки примера
работают против дерева, а не против выпущенной версии, и `example/`
внутри архива всё равно разрешается у скачавшего пакет — проверено на
копии вне репозитория.

Когда семейство снова разъедется по версиям, оверрайд ядра или `solo`
вернётся на время подготовки следующего выпуска. Тогда же помнить:
`pub` смотрит индекс git, и удаление файла оверрайда без `git add`
оставляет предупреждение в dry-run.

Порядок выпусков был: solo, затем flutter_solo. Оба выпущены,
незакрытых хвостов выпуска нет. Про dev-зависимости `lints` и
`flutter_lints`, отстающие на мажор, — отдельная работа разом во всех
пакетах; на потребителей они не влияют.

Замечание для подготовки flutter_solo: удалить файл оверрайда мало.
Пока удаление не попало в индекс git, `dart pub publish --dry-run`
продолжает считать файл частью пакета и предупреждает о checked-in
и ignored файлах.

## Проверки и дерево

Работа в main, база timing — `9493c31`. Код, тесты и документы timing
включены в общий коммит этой работы. Параллельная чужая правка
`.vscode/launch.json` остаётся в дереве и в этот коммит не входит.
Накопленные коммиты и README each до `b97fbc5` отправлены в origin/main.
Документы подготовки и релизный коммит 547819a отправлены в origin/main
вместе с тегом async_job-v0.2.0. Пакет async_job 0.2.0 опубликован.
Итог выпуска сохраняется отдельным docs-коммитом после публикации.

- solo: анализ чистый, 433 теста, включая 56 новых timing-тестов;
- пример solo: анализ чистый, 9 тестов;
- flutter_solo: анализ чистый, 8 тестов;
- пример Flutter: анализ чистый;
- формат семи изменённых Dart-файлов: 0 изменений;
- dartdoc solo: 0 предупреждений и ошибок;
- извлечённые примеры настроек, логов и поиска выполнены с FakeAsync;
- пять пар переводов совпадают; ширина Markdown и diff проверены.

Всего 450 тестов этого изменения. AGENTS.md и записи проверяются
по ширине отдельно. Отчёт: `2026-09-10[17]-accumulation-timing-report.md`.
Проверки предыдущего run сохранены в `2026-09-10[15]-context-run-report.md`.

Для команд использовать `rtk`, для неподдерживаемых команд `rtk proxy`.
`git diff --check` выполнять через `rtk proxy git diff --check`:
фильтр `rtk git diff` может скрыть диагностический вывод. Проверку ширины
`AGENTS.md` и изменённых записей делать отдельно: общий скрипт их пропускает.
Обёртки Dart и Flutter требуют доступ к SDK и кешам вне песочницы;
автоматическое ревью разрешило эти проверки через escalation.

## Дальше, когда владелец выберет новую работу

Остаётся ненаписанным рецепт паузы очереди задачей, ждущей `Completer`.
`canStart` не создаёт паузу: неподходящие задачи отменяются, и очередь
опустошается. Брать после выбора владельца.

Материал и замеры: `2026-09-09[9]-todo-review.md`. Готовый стенд может
лежать в `.artifacts/2026-09-09-wave2-review/a/stand`; восстановление —
`python3 tool/doc_snippets.py <каталог>`. При проверке внешней операции
дожидаться самой операции: снимок состояния сразу после отмены `Job`
не показывает её окончательный результат.

Пожелания владельца находятся в `docs/backlog.md`. После окончания текущей
реализации предложить выбрать оттуда работу; самостоятельно записи не менять.
Другие ранее отложенные примеры для `solo`: `stateAs`, `check`, retry/backoff.
Их брать только после выбора владельца.

Начиная с 2026-09-12, но не раньше, при старте сессии или выборе новой
работы напоминать о возможном ответе `info@efa.dev` по имени `job`.
Если к 2026-09-26 ответа нет, напомнить о пересылке в `support@pub.dev`.
Переписку самостоятельно не отправлять. После решения обновить
`2026-09-05[8]-job-name-request-report.md` и убрать напоминание.
Это не блокирует нынешнее имя `async_job`.

## Ограничения и общение

- Не использовать всплывающие формы вопросов: владелец сообщил, что они
  появляются и сразу исчезают, ответить невозможно. Вопросы писать
  обычным сообщением в чате и ждать ответа. Причина исчезновения
  не установлена: инструмент подтвердил приём вопроса (`accepted`),
  но не сообщал о закрытии формы или ответе через неё.
- Не приписывать владельцу ответы на формы или выбор вариантов,
  которые он не давал. Собственную интерпретацию запроса называть своей.
- Владелец просит минимум служебного вывода: ему нужны сообщения по делу,
  а не журналы запусков. Заголовки инструментов формирует интерфейс;
  не обещать скрыть их, сокращать число вызовов и объём вывода.
- Текст рабочих документов переносить на 79 символах. Правило и порядок
  проверок сохранены в `AGENTS.md`.
- Английский README и русский перевод править вместе. Связность и смысл
  важнее косметического сокращения; подтверждённые условия не выкидывать.
- Не менять заодно согласованную семантику отмены. Вопрос об ожидании
  секции без `await` `uncancellable` остаётся в бэклоге.
- `ctx.run` принимает `Job<T>`, а не только `DeferredJob<T>`: библиотечные
  классы на основе `JobBase` тоже могут быть дочерними. Автоматически
  запускающийся обычный `Job` отвергается.
- Не расширять защищённые `owner` и `parent` без подтверждённой нужды
  потребителя. `isDisposing` не выносить в публичный `Job` без нового решения.
- Буфер ранних событий в `each` сохранять; прежние альтернативы теряли
  события при подписке. При ошибке тела дочерние задачи дорабатывают;
  при самоотмене тела отмена передаётся детям.
- Соседний `scopo` ведётся отдельно. Его прежний переход на опубликованное
  ядро 0.1.0 выполнен; обновление на новый API не входит в эту работу.
  Архивирование `stream_future` тоже отдельная внешняя задача.
- `/Users/user/development/yandex/yx_camera` не трогать: это чужой проект.
- Не запускать push, публикацию, теги или релизные коммиты без отдельного
  поручения. Правила связки публикации полностью заданы в `AGENTS.md`.

Большая прежняя версия handoff сохранена в Git на `659512a`.
Подготовка к текущему рестарту описана в
`2026-09-10[11]-debounce-restart-report.md`.
