# Handoff: текущее состояние проекта

Обновлено: 2026-09-10, уточнены примеры README async_job.

## Текущая работа

По новым замечаниям владельца обновлены README ядра и русский перевод:
пример ожидания ребёнка использует `ctx.join(() => child.value)`
с проверкой отмены родителя; функция снятия уборщика называется
`removeDisposer`; ленивое сообщение передаётся колбэком, который вызывает
показанный `JobObserver.onLog`. Без наблюдателя интерполяции нет.
Реализация API не менялась. Владелец уточнил: речь о проверке внутри
самого run, без отдельного ctx.join. Предыдущий пример решал задачу
средствами существующего API, но не отвечал на вопрос о его изменении.
Предложение агента: run возвращает Future<T>, ждёт child.value и перед
выдачей успешного результата проверяет отмену родителя. Вызов тогда
выглядит как await ctx.run(child); исходный child остаётся хэндлом.
Сейчас run синхронно возвращает Job<T>, поэтому это изменение контракта,
затрагивающее вызовы и внутренний запуск each. Оно пока не согласовано
и не реализовано; следующий шаг — обсуждение этого предложения.
Подробности: `2026-09-10[13]-readme-child-logging-report.md`.

## Ожидает согласования: debounce и throttle

Подготовлена спецификация для согласования:
`2026-09-10[12]-accumulation-timing-design.md`.
Следующий шаг — ответ владельца по конкретному контракту, затем план
реализации. Последнее «ок» утверждает независимость хранения и времени,
неблокирующее ожидание и последовательное выполнение обработчиков.
Предлагаемые ниже детали после этого ответа ещё не согласованы.

Выбрано владельцем:

- обычный debounce и throttle; maxWait не добавлять;
- collect сохраняет все события, accumulate использует пользовательскую
  merge для выбора сохраняемых данных; время ничего само не отбрасывает;
- ожидающая группа A пропускает готовую B; ожидание временного срока
  не занимает слот, обработчики выполняются последовательно.

Спецификация предлагает timing: AccumulationTiming.debounce(Duration)
или AccumulationTiming.throttle(Duration) для collect/accumulate.
Без timing и при Duration.zero сохраняется прежнее поведение.
Отрицательная длительность даёт ArgumentError в фабрике настройки.
Обычные job/add/run в этой работе новых параметров не получают.

Debounce: таймер каждой группы перезапускается при принятом событии;
его callback закрывает состав и делает группу готовой, даже если слот
занят. Ошибка merge не перезапускает таймер. maxWait отсутствует.

Throttle: предложен общий интервал между фактическими стартами групп
одного накопителя. Первый старт без временной задержки. Накопленное
готово после интервала без нового add; данные открыты до извлечения.
Слот, занятый другой задачей, сдвигает старт и следующий интервал.
Это предложение агента, а не уже полученный выбор владельца.

Предложенный механизм — оставлять группы в очереди и извлекать первую
готовую. Хэндлы, отмена и queue видят все группы. Спека явно описывает,
как обход влияет на adjacent/replace/join; без timing порядок прежний.
close отменяет таймеры и ожидающие группы без досылки.

Сравнение пакетов и уточнения владельца:
`2026-09-10[10]-debounce-comparison-report.md`.
Предыдущая точка рестарта:
`2026-09-10[11]-debounce-restart-report.md`.
Код не менялся. Пункт бэклога открыт. Накопители повторно не реализовывать.
Не возвращать блокирующее ожидание и не переспрашивать выбор хранения.

## Завершённая основа: накопители

Накопители событий реализованы по поручению владельца «ок. приступай».
Работа завершена; код, 90 новых тестов и документы сохранены
в `main` коммитом `ff5ed8d`.
База работы — `fc751e3`. Незавершённых поручений по накопителям нет.

Спека: `2026-09-10[7]-event-accumulation-design.md`.
План: `2026-09-10[8]-event-accumulation-plan.md`.
Реализация, проверки и два независимых ревью без находок:
`2026-09-10[9]-event-accumulation-report.md`.

`collect` собирает список, `accumulate` немедленно объединяет события
через чистую синхронную `merge`. Фабрики возвращают стабильный
`SoloAccumulator<E, T>` с `.add(event)`. Состояние меняет только
обработчик через `ctx.emit`.

Вход `A1, B, A2` при трёх значениях `AccumulationPolicy`:

- `adjacent` по умолчанию: `[A1, B, A2]`;
- `replace`: `[B, A(A1 + A2)]`, данные передаются новой группе;
- `join`: `[A(A1 + A2), B]`, остаётся прежний хэндл.

Совместимость определяется идентичностью накопителя; `key` остаётся
обычным ключом задачи. Извлечение закрывает приём до стартовых правил
и хуков. Работающие группы не дополняются. Замена ожидающей группы
обязательна даже при `cancellable: false`; новый хэндл виден до хуков
отмены прежнего. Старые хэндлы не перенаправляются.

Рецепт настроек и логов: `packages/solo/doc/accumulation.md`, перевод
`docs/ru/solo/accumulation.md`. Примеры извлечены из Markdown
и проверены на локальном API. Следующая выбранная работа — окно
задержки перед запуском; обсуждение описано выше.

В выполненную аккумуляцию debounce, досылка, надёжный буфер
и автоматические повторы не входили. Бэклог пока не закрывается.
Аккумуляция через `externalSetState` отклонена владельцем;
`2026-09-10[6]-state-accumulation-design.md` не реализовывать.

`then`, `each` и вычитка README ядра завершены; не начинать их заново.
Записи: `2026-09-10[2]-job-then-report.md`,
`2026-09-09[18]-each-child-report.md`,
`2026-09-09[14]-async-job-readme-report.md`.
Владелец требует связного объяснения: прежде чем редактировать абзац,
проверять, что читатель уже знает и как текст связан с примером.

## Требования к README, которые нужно сохранить

- При описании API на русском называть `Job` прямо. Слово «задача»
  уместно для самой работы, но не должно подменять имя класса в объяснении
  его возможностей.
- `ctx.join(Database.open)` объясняется самостоятельно до сравнения
  с `wait`: вызывает операцию, ждёт её завершения и проверяет отмену.
  `wait` предлагается, когда нужно прекратить ожидание сразу при отмене.
- `discard` после `return` объясняется на примере ожидания дочерней
  задачи: тело вернулось, но весь `Job` ещё не завершился.
- В примере миграции `onCancel` передаёт сигнал токену, `join` ждёт
  фактической остановки, а `uncancellable` защищает шаг от самого сигнала.
- Внешние асинхронные операции в теле следует ожидать через контекст.
  Прямой `await` допустим, но сам не проверяет отмену. Внутри действия,
  переданного контексту, и при уборке обычный `await` уместен с учётом
  описанных в README границ.
- Упоминание `solo` следует из границ ядра: в `async_job` нет управления
  состоянием, очереди и правил планирования; `solo` добавляет их.
- Два примера проброса `Cancelled` показывают отдельную ветку `catch`
  и проверку типа внутри общего обработчика. Оба способа подходят
  и для `Exception`, и для `Object`; выбор типа не различает эти способы.
- Принятая отмена самого `Job` сохраняется после перехвата исключения.
  Код после `catch` может выполниться, следующая контрольная точка снова
  бросает отмену, а при завершении исход остаётся `Cancelled`.
  Перехваченный `Cancelled` от операции или дочерней задачи сам по себе
  родителя не отменяет: возможен `Done`.

Оба README ядра имеют по 19 блоков кода, оба README `solo` — по 27.
Примеры `then`, его контракт и переводы обновлены вместе с реализацией.

## Состояние API и версий

Готовится несовместимый выпуск `async_job` 0.2.0. По состоянию последней
публикации ядро на pub.dev имеет версию 0.1.0; текущий 0.2.0 не опубликован.
`solo` и `flutter_solo` в дереве тоже на 0.2.0 и не опубликованы.

В текущем API `job.whenCancelled(callback)` регистрирует синхронный
обработчик, передаёт ему весь `Cancelled` и возвращает функцию снятия.
Поздняя регистрация вызывает обработчик сразу. Это больше не future.
Реализация и ревью: `2026-09-09[11]-when-cancelled-report.md`.

`CancelReason` — открытый абстрактный класс. Встроенные причины ядра:
`ManualCancelReason`, `ParentCancelReason`, `HandlerCancelReason`,
`ChainCancelReason`.
У `solo`: `RulesCancelReason`, `ClosedCancelReason`, основа
`SoloCancelReason`. Свои классы могут хранить ошибку, стектрейс и другие
данные. `name` служит меткой журнала; проверка по типу, равенства по имени
нет. Переданная в `cancel(reason: ...)` причина сохраняет идентичность.
Связи `ParentCancelReason.cause` и `HandlerCancelReason.cause` сохраняют
исходный `Cancelled` при каскаде и распространении отмены ребёнка.
`ChainCancelReason.cause` сохраняет отмену соседнего звена `then`.
Реализация и ревью: `2026-09-09[13]-cancel-reason-report.md`.

`solo` зависит от `async_job: ^0.2.0`, `flutter_solo` от `solo: ^0.2.0`.
Локальные оверрайды ядра восстановлены у `solo`, его примера,
`flutter_solo` и его примера. У Flutter-пакета также оверрайд `solo`,
у его примера — обоих пакетов и `flutter_solo`. Пока ядро 0.2.0 не
опубликовано, эти оверрайды нужны.

Прежние dry-run для hosted-ядра 0.1.0 не подтверждают готовность текущего
дерева к публикации. Когда владелец отдельно поручит выпуск, порядок:
ядро, затем `solo`, затем `flutter_solo`. После публикации зависимости
снимать соответствующие оверрайды и повторять разрешение зависимостей,
проверки и dry-run. Саму связку выпуска выполнять только по `AGENTS.md`.

## Проверки и дерево

Работа в `main`. Спецификация timing сохранена в `d50b22a`;
правки README сохранены в `479e934`. После уточнения владельца меняются
только handoff и отчёт обсуждения. Код библиотек
после `ff5ed8d` не менялся; пакетные проверки ниже относятся к готовой
аккумуляции.
Чужих правок нет. Push, теги и публикация не выполнялись.

Примеры README проверены отдельным стендом с локальным API:
`/private/tmp/async-job-readme-check-20260910/`.
Анализатор чистый, четыре сценария FakeAsync прошли: обычное ожидание
и join при отказе ребёнка от отмены, ленивый лог с наблюдателем и без.
В обоих сценариях отмены внешний читатель получает успех ребёнка;
join останавливает дальнейшее выполнение тела отменённого родителя.
Пять пар переводов, ширина Markdown и diff проверены; AGENTS.md
и новая запись проверяются отдельно. Полные пакетные тесты не повторялись:
изменены только документы.

Текущие проверки накопителей:

- `async_job`: анализатор чистый, 296 тестов;
- `solo`: анализатор чистый, 375 тестов, из них 90 новых;
- пример `solo`: анализатор чистый, 9 тестов;
- `flutter_solo`: анализатор чистый, 8 тестов;
- пример Flutter: анализатор чистый;
- извлечённые примеры: анализатор чистый, настройки, логи и три трассы
  правил приёма прошли одноразовую проверку;
- переводы: пять пар совпадают; общая ширина Markdown проверена.

Всего 688 тестов. Dartdoc `solo`: 0 предупреждений и ошибок;
форматирование пяти файлов Dart без изменений. Оба ревью завершены
без находок; собственных фоновых процессов не осталось.
Отчёт исполнителя: `/private/tmp/solo-accumulator-implementation-report.md`.
Стенд примеров: `/private/tmp/solo-accumulation-doc-check/`.
Итоговые результаты сохранены в записи работы. Временные отчёты
реализации и ревью могут оставаться в `/private/tmp/`; папка SDD
выполненного плана уже удалена. Не повторять ревью
автоматически без новых изменений или поручения владельца.

Сравнение пакетов: девять проверок значений и времени прошли;
ограничения стенда сохранены в записи сравнения. Throttle дополнительно
сверен по исходникам, новых исполняемых проверок пока нет. При подготовке
спецификации пакетные тесты не повторяются: реализации timing ещё нет.
Проверены ширина рабочих документов, отдельно AGENTS.md и изменённые
записи, ссылки и diff. Сценарии новой реализации перечислены в спеке.
Все агенты прошлой работы завершены; активных фоновых запусков нет.

Для команд использовать `rtk`, для неподдерживаемых команд `rtk proxy`.
`git diff --check` выполнять через `rtk proxy git diff --check`:
фильтр `rtk git diff` может скрыть диагностический вывод. Проверку ширины
`AGENTS.md` и изменённых записей делать отдельно: общий скрипт их пропускает.
Обёртки Dart и Flutter требуют доступ к SDK и кешам вне песочницы;
автоматическое ревью разрешило эти проверки через escalation.

## Дальше, когда владелец выберет новую работу

Остаётся ненаписанным рецепт паузы очереди задачей, ждущей `Completer`.
`canStart` не создаёт паузу: неподходящие задачи отменяются, и очередь
опустошается. Брать после выбора владельца.

Материал и замеры: `2026-09-09[9]-todo-review.md`. Готовый стенд может
лежать в `.artifacts/2026-09-09-wave2-review/a/stand`; восстановление —
`python3 tool/doc_snippets.py <каталог>`. При проверке внешней операции
дожидаться самой операции: снимок состояния сразу после отмены `Job`
не показывает её окончательный результат.

Пожелания владельца находятся в `docs/backlog.md`. После окончания текущей
реализации предложить выбрать оттуда работу; самостоятельно записи не менять.
Другие ранее отложенные примеры для `solo`: `stateAs`, `check`, retry/backoff.
Их брать только после выбора владельца.

Начиная с 2026-09-12, но не раньше, при старте сессии или выборе новой
работы напоминать о возможном ответе `info@efa.dev` по имени `job`.
Если к 2026-09-26 ответа нет, напомнить о пересылке в `support@pub.dev`.
Переписку самостоятельно не отправлять. После решения обновить
`2026-09-05[8]-job-name-request-report.md` и убрать напоминание.
Это не блокирует нынешнее имя `async_job`.

## Ограничения и общение

- Не использовать всплывающие формы вопросов: владелец сообщил, что они
  появляются и сразу исчезают, ответить невозможно. Вопросы писать
  обычным сообщением в чате и ждать ответа. Причина исчезновения
  не установлена: инструмент подтвердил приём вопроса (`accepted`),
  но не сообщал о закрытии формы или ответе через неё.
- Не приписывать владельцу ответы на формы или выбор вариантов,
  которые он не давал. Собственную интерпретацию запроса называть своей.
- Владелец просит минимум служебного вывода: ему нужны сообщения по делу,
  а не журналы запусков. Заголовки инструментов формирует интерфейс;
  не обещать скрыть их, сокращать число вызовов и объём вывода.
- Текст рабочих документов переносить на 79 символах. Правило и порядок
  проверок сохранены в `AGENTS.md`.
- Английский README и русский перевод править вместе. Связность и смысл
  важнее косметического сокращения; подтверждённые условия не выкидывать.
- Не менять заодно согласованную семантику отмены. Вопрос об ожидании
  секции без `await` `uncancellable` остаётся в бэклоге.
- `ctx.run` принимает `Job<T>`, а не только `DeferredJob<T>`: библиотечные
  классы на основе `JobBase` тоже могут быть дочерними. Автоматически
  запускающийся обычный `Job` отвергается.
- Не расширять защищённые `owner` и `parent` без подтверждённой нужды
  потребителя. `isDisposing` не выносить в публичный `Job` без нового решения.
- Буфер ранних событий в `each` сохранять; прежние альтернативы теряли
  события при подписке. При ошибке тела дочерние задачи дорабатывают;
  при самоотмене тела отмена передаётся детям.
- Соседний `scopo` ведётся отдельно. Его прежний переход на опубликованное
  ядро 0.1.0 выполнен; обновление на новый API не входит в эту работу.
  Архивирование `stream_future` тоже отдельная внешняя задача.
- `/Users/user/development/yandex/yx_camera` не трогать: это чужой проект.
- Не запускать push, публикацию, теги или релизные коммиты без отдельного
  поручения. Правила связки публикации полностью заданы в `AGENTS.md`.

Большая прежняя версия handoff сохранена в Git на `659512a`.
Подготовка к текущему рестарту описана в
`2026-09-10[11]-debounce-restart-report.md`.
