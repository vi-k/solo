# Handoff: текущее состояние проекта

Обновлено: 2026-09-07.

## Где мы

Фаза: **ядро вынесено, три пакета готовы к первой публикации.** В дереве
`packages/jobs` 0.1.0 — исполнительное ядро задачи, `packages/solo` 0.2.0
поверх него и `packages/flutter_solo` 0.2.0. Не опубликовано ничего.

Публикация — только по правилам `AGENTS.md`, отдельным запросом владельца,
состоящим только из просьбы опубликовать. Ничего из связки «коммит
`Release X.Y.Z` — тег — push — `dart pub publish`» не запущено и без такого
запроса не запускается.

Как сюда пришли. Пакет `conveyor` 1.0.0 (нигде не опубликован,
потребителей не было) переделан в пакет `solo`: то же ядро
последовательных задач с гарантией монопольного владения состоянием, но с
публичным API на обычных `async`-функциях вместо генераторов. Спецификация
`docs/records/2026-09-02[1]-solo-design.md`, план
`docs/records/2026-09-02[2]-solo-plan.md` — 20 задач, выполнен целиком
(`03c8255`..`3e3b299`), поверх одна волна правок по финальному ревью
(`2026-09-03[1]-solo-final-review-report.md`). Реализация `conveyor` 1.x
удалена в задаче 1b, осталась в истории (`c884c38`).

Потом из `solo` вынуто исполнительное ядро задачи — всё, что не знает ни
про состояние, ни про очередь, ни про правила. Спека
`docs/records/2026-09-05[4]-jobs-design.md`, план
`docs/records/2026-09-05[9]-jobs-plan.md` — 15 задач, выполнен целиком
(`b855815`..`5532982`). Владелец решил 2026-09-05 делать вынос **до**
публикации, а не после, как было записано в плане.

Публичное API `solo` изменилось в шести местах, все совместимы по
исходникам для кода, написанного по README; разбор — в `CHANGELOG.md`
пакета, подраздел «Since 0.1.0» секции 0.2.0 (сама секция переписана как
первый публичный релиз: 0.1.0 на pub.dev не было).

Поверх этого 2026-09-06 прошли три круга ревью документации — по одному на
пакет, глазами пользователя каждого (`2026-09-06[4]`, `[5]`, `[6]`), — и
одна смена семантики: `uncancellable` придерживает отмену вместо
окончательного отказа. Она меняет поведение, а не сигнатуры: код, писанный
по README, компилируется как прежде, но задача с `cancellable: true`,
которую отменили во время защищённого шага, теперь заканчивается отменой, а
не доигрывает до конца.

## Следующие шаги

Уборка сделана и смержена: план `2026-09-07[5]-dispose-plan.md` исполнен
целиком, двенадцать коммитов. Публичное API обоих пакетов при этом
поменялось — у `wait` и `join` вместо `ifCancelled` появились `dispose` и
`discard`, у контекста `onDispose`, `onDiscard` и `disown`, у задачи
`ifCancelled` больше нет, — и это была единственная причина держать
публикацию. Её больше нет.

2026-09-07 ядро прошло полное независимое ревью
(`2026-09-07[7]-jobs-project-review.md`), и все тридцать шесть его находок
закрыты в тот же день, `1ed53da`..`77e9bbc`. Саму волну правок прочитал
ещё один круг (`2026-09-07[8]-jobs-fixes-review.md`) — шесть регрессий,
включая одну тяжёлую, все закрыты в `fcc7409` и `eeb90d2`. Поверх — третий,
узкий круг по двум местам, которых никто не читал
(`2026-09-07[9]-jobs-narrow-review.md`): ещё четыре находки, закрыты в
`a788733` и `9dab456`. И четвёртый, второй узкий, по тем же двум местам
целиком (`2026-09-07[10]-jobs-narrow-review-2.md`): десять находок,
закрыты в `607868a`. И пятый, третий узкий, по одному месту — `ctx.each`
целиком, тремя ревьюерами с разными прицелами
(`2026-09-07[11]-jobs-each-review.md`): тринадцать находок, разобраны
все. Публикацию это больше не держит.

Нумерованные шаги ниже — за владельцем; агент сам их не берёт.

1. **Публикация.** Связка «коммит `Release X.Y.Z` — тег — push —
   `dart pub publish`» по правилам `AGENTS.md`, отдельным запросом,
   состоящим только из просьбы опубликовать. Порядок и его причина:
   опубликованный пакет может зависеть только от hosted-пакетов, поэтому
   сначала ядро и лишь потом те, кто на нём стоит.

   1. `jobs` 0.1.0 — оверрайдов у него нет, dry-run чист.
   2. `solo` 0.2.0 — **сначала удалить**
      `packages/solo/pubspec_overrides.yaml` целиком: `jobs: ^0.1.0` после
      первого шага резолвится с pub.dev. Затем `dart pub get`, проверки,
      dry-run.
   3. `flutter_solo` 0.2.0 — так же: удалить его
      `pubspec_overrides.yaml`, `flutter pub get`, проверки, dry-run.

   Пока оверрайды на месте, dry-run у `solo` и `flutter_solo` даёт hint
   «Non-dev dependencies are overridden». Он не зависит от того, лежат они
   в `pubspec.yaml` или в `pubspec_overrides.yaml` (проверено 2026-09-06),
   и уходит только вместе с самим оверрайдом; сам `pubspec_overrides.yaml`
   в архив не попадает.

   После публикации подчистить оверрайды там, где они станут не нужны:
   `packages/solo/example/pubspec_overrides.yaml`,
   `packages/flutter_solo/example/pubspec_overrides.yaml` и шаблон
   `SOLO_PUBSPEC` в `tool/doc_snippets.py`. Всё три — только уборка, в
   архив не едет ни один: оверрайд примера `solo` переехал из
   `example/pubspec.yaml` в `pubspec_overrides.yaml` и закрыт `.pubignore`
   пакета (иначе опубликованный пример не собирался бы ни у кого — находка
   1 `2026-09-06[5]-solo-docs-review.md`), у примера `flutter_solo` он так
   и заведён.

   Заново проверить, свободны ли имена: `solo` и `flutter_solo` были
   свободны на 2026-09-02, `jobs` — на 2026-09-06. Между публикацией
   `jobs` и `solo` ссылка `pub.dev/packages/solo` в README ядра будет
   битой — терпимо, если публиковать подряд.

   После push и до `dart pub publish` проверить, что `repository` каждого
   пакета открывается: `curl -I -L
   https://github.com/vi-k/solo/tree/main/packages/<пакет>` должен дать
   200. Пока `packages/jobs` не запушен, он даёт 404, и pana снимает за
   это 10 баллов из 160 — ревью документации, находка 14
   (`2026-09-06[4]-jobs-docs-review.md`).
2. **Имя пакета.** Решение владельца 2026-09-06: **публиковать ядро как
   `jobs`, не дожидаясь ответа про `job`.** Причина — `solo` и `scopo`
   нужны на pub.dev сейчас, а без опубликованного ядра ни один из них не
   выложить: pub.dev принимает только hosted-зависимости. Если `job` потом
   освободится, тот же код выходит как `job` 0.1.0, `jobs` помечается
   discontinued с указанием замены, `solo` уходит на 0.3.0, `scopo` меняет
   строку импорта и pubspec. Письмо держателю `job` остаётся в силе,
   занятый `jobs` ему не мешает; разбор имён и текст письма —
   `2026-09-05[8]-job-name-request-report.md`.

   Напоминание владельцу: **начиная с 2026-09-12** (раньше не тревожить)
   на старте сессии и при выборе следующей задачи спрашивать, не ответил
   ли `info@efa.dev`. Спрашивать до тех пор, пока владелец не скажет, чем
   кончилось. Если на 2026-09-26 ответа нет — сказать, что пора переслать
   тред на `support@pub.dev`. Когда исход известен, записать его в
   `2026-09-05[8]-job-name-request-report.md` и убрать это напоминание
   отсюда. Ответ больше не задерживает публикацию — он решает только,
   будет ли переименование с `discontinued` у `jobs`.

3. **Судьба оставшихся пунктов `TODO.txt`** (перечислены ниже, в
   «Состоянии дерева и проверок»): взять в `docs/backlog.md` или забыть.
4. **Перевести `scopo` на ядро** — работа в соседнем репозитории
   `/Users/user/development/my/scopo` (у него свой `AGENTS.md`; версия
   0.14.0, не выпущена). Владелец сказал 2026-09-06, что `scopo` нужен на
   pub.dev с новым `Job`, — то есть после публикации ядра это следующая
   работа. Кода не написано.

   Как подключать, пока ядро не на pub.dev: в `pubspec.yaml` — `jobs:
   ^0.1.0`, а рядом `pubspec_overrides.yaml` с `path: ../solo/packages/jobs`
   (`scopo` — сосед `solo`). После публикации ядра оверрайд не нужен.

   Что в переводе, по спеке (шаг 3 и «Что достаётся scopo»):
   `ScopeInitCancelled` → `Cancelled`, `ScopeInitHandle` → `Job`,
   `ScopeInitContext` → наследник `JobContextBase` со своим `progress`,
   синхронный старт → `Job.deferred(...)..start()`, `cancel()`, который не
   ждёт тела, → просто не дожидаться возвращённой future,
   `FlutterError.reportError` → адаптер `JobObserver`, `_runInitBody` →
   `switch (await job.done)`, `releaseLateData` → `ctx.onDiscard(...)` по
   спеке уборки (`2026-09-07[1]`), зарегистрированный **после** того, как
   значение построено: регистрация помеченной задаче не отказывает, и на
   этом свойстве `scopo` держится целиком. Вместе с этим — правка,
   названная ревью: `_data`/`_hasData` сегодня пишутся внутри тела
   (`async_data_scope_core.dart:139-140`), а должны писаться из ветки
   `Done` у `switch (await job.done)`. Иначе отмена, пришедшая после
   записи поля, уронит `discard` на значение, которое поле уже держит, и
   освободит его второй раз.
   Три разницы, которые `scopo` принимает на себя: дети у ядра — поддерево
   завершения, а не только область отмены; `run` под уже отменённым
   родителем бросает отмену в тело, тогда как `childOf` молча возвращал
   отменённого ребёнка; сужение `covariant` идёт к приватной реализации
   контекста.
5. **Архивировать `stream_future`** (шаг 4 спеки ядра) — тоже вне этого
   репозитория.

Как делалось ядро, если нужна предыстория: спека прошла три круга ревью
(`2026-09-05[5..7]`), план — два (`2026-09-05[10]`, `[11]`), код — три
(`2026-09-06[1..3]`), документация всех трёх пакетов — по одному
(`2026-09-06[4]`, `[5]`, `[6]`). Вердикт стоит у каждой находки в каждой
записи.

## В работе прямо сейчас

Ничего не начато. За 2026-09-07 ядро прошло пять кругов независимого
ревью подряд, и все находки всех пяти разобраны; ничего не висит.

На столе — **два решения владельца**, оба из пятого круга и оба записаны
там же с разбором (`2026-09-07[11]-jobs-each-review.md`, находки 12 и
13):

1. **Ошибка обработчика, перекрытая отменой, которую вызвал сам `each`.**
   Источник, отменяющий свою задачу из собственного `onCancel`, забирает
   у тела его же ошибку: тело получает `Cancelled`, ошибка уходит
   наблюдателю. По существу это решённый владельцем приоритет отмены над
   исходом, и правка, которая бы это изменила, портит соседний угол —
   поэтому не взято, а поведение закреплено тестом. Оставить так или
   считать дефектом — за владельцем.
2. **`each` из диспозера называет `onCancel`.** Контракт (`StateError`)
   держится, ломается только текст сообщения. Чистая правка — вынести
   `isDisposing` из защищённой поверхности `JobBase` в публичный `Job`;
   это расширение публичной поверхности, добавить геттер позже — правка
   не ломающая.

Динамика по тяжести за день: 3 High в первом круге, 1 во втором, 0 в
третьем и четвёртом, 1 в пятом — и та лечится формулировкой. Критерий
остановки, о котором договорились с владельцем (круг, вернувший только
документацию и Low, — последний), пятым кругом **не выполнен**: он вернул
две Medium с правкой кода. Следующий круг по тому же месту — предложение
агента, не решение владельца.

### Пять кругов ревью 2026-09-07

Полное ревью (`2026-09-07[7]-jobs-project-review.md`) вели четыре ревьюера
Fable 5.1 по разным областям — движок, контекст, API с документацией,
тесты, — не зная выводов друг друга. Тридцать шесть находок; три высокой
серьёзности и одна спорная перепроверены отдельными прогонами в чистой
копии. Сьюту мерили мутантами: 30 из 49 мутаций ловилось, 19 выживало.

Закрыто всё, шестью коммитами `1ed53da`..`77e9bbc`. Тридцать три находки
исправлены, одна (23, квадратичное ожидание детей) при проверке не
подтвердилась, две закрыты решением владельца без правки кода. Вердикт с
исходом стоит у каждой находки.

Что чинили в коде. Реентерабельная отмена роняла `cancel()` с
`Bad state: Future already completed` — задача теперь помечается до
каскада. Ребёнок, не сумевший стартовать, вешал родителя навсегда —
теперь он попадает в список ожидания только после успешного старта.
Поздняя отмена съедала `Failed` вместе с ошибкой тела — перекрытая ошибка
уходит в зону, если наблюдателя нет. `each` оставлял подписку на задаче,
кончившейся без отмены, — отписка легла на стек уборки. Плюс: `run`
отказывает после конца тела, отложенные `discard` раскручиваются в
порядке стека, бросивший `finished()` не держит задачу открытой, список
детей у наследника только на чтение, позднее значение `wait` убирается на
тех же условиях, что у `join`, задача без ключа больше не `Job(null)`.

Три решения владельца по существу: перекрытую ошибку отдавать в зону, а
не менять приоритет исхода; каскадить на детей только при самоотмене тела
(`throw Cancelled`), упавшее тело детей не трогает; сменить сигнатуру
`onData` у `each` на `FutureOr<void> Function(T)` — `each` дожидается
обработчика, придерживая доставку. Не расширять защищённую поверхность
(`owner`, `parent`) до того, как этого попросит `scopo`.

Поверх этого — круг ревью самой волны, двумя ревьюерами: движок с
контекстом и отдельно `each` с влиянием на `solo`
(`2026-09-07[8]-jobs-fixes-review.md`). Шесть регрессий, все
воспроизведены прогонами до правки и закрыты в `fcc7409` и `eeb90d2`.
Тяжёлая была своя, из этой же волны: отчёт о перекрытой ошибке не смотрел,
что случилось раньше, и любая задача, чьё тело превращает свою отмену в
другое исключение, шумела в зону при каждой штатной отмене. Рядом: каскад
самоотмены бежал до конца тела и снова пускал детей под решённый исход;
отвергнутый бросившим правилом ребёнок оставался усыновлённым и
автостартующий шёл призраком; `_markCancelled` оказался достижим второй
раз через прямой `finish` движка домена.

`each` переписан ещё раз: события, которые синхронный broadcast отдаёт из
`onListen`, шли мимо всех трёх гарантий — порядка, неповторного вызова
после броска и сохранности ошибки, — и теперь буферизуются с
проигрыванием по одному. Владелец решил оставить буфер, а не переходить на
`stream.asyncMap(...).listen(...)`: замер показал, что и `asyncMap`, и
`StreamIterator` теряют эти события молча, потому что подписываются на
источник изнутри собственного `listen`. Причина записана комментарием в
`each`.

В `solo` закрыта своя находка того же класса: бросившее правило в пампе
оставляло задачу без исхода и останавливало очередь навсегда.

Третий круг был узким и по адресу: два места, которые я назвал сам,
потому что своей проверке там не доверял. Владелец просил взять Codex; тот
упал на лимите аккаунта на седьмой минуте, не дойдя до находок, и круг
переигран на Fable. В `solo` нашлось, что `add` пускал обратно задачу,
которую памп уже вынул из очереди: очевидный retry-хук в `onError`
останавливал контроллер навсегда — тот же класс, который чинила правка
волны. В `each` буфер ранних событий переживал освобождение стрима и
доигрывал после отмены и даже после конца задачи, а `cancelOnError`
держал ошибку до конца уборки источника — источник с незавершающимся
`onCancel` вешал задачу насмерть.

Отдельно стоит запомнить, чем это вышло: флаг `letGo` — это тот самый
флаг, который я утром убрал как мёртвый, потому что три мутации на него
выжили. Выжили они потому, что ни один тест не отменял задачу посреди
проигрывания буфера. Мутация показывает, чего не видит сьюта, и никогда —
чего нет в коде.

Четвёртый круг перечитал те же два места целиком и нашёл ещё десять.
Из них две существенные, и обе — о том, куда девается ошибка. В `each`
ожидание заводилось после `listen`, поэтому источник, отдающий ошибку
изнутри `listen`, завершал его раньше, чем на него кто-то подписался, и
ошибка уходила в зону мимо наблюдателя; теперь ожидание заводится первым.
В ядре отчёт о перекрытой ошибке выходил при наличии наблюдателя — а
`_reportUnobserved`, которому он подражает, на наблюдателя не смотрит; в
`solo`, где наблюдатель есть у каждой задачи всегда, ошибка тела из-за
этого пропадала совсем. Осталось одно условие, то же, что у обычного
необслуженного `Failed`.

Там же закрыты: усыновление ребёнка теперь забирает хэндл так же, как
`add` (иначе правило ребёнка могло положить его в очередь и подвесить
контроллер — та же дверь, что закрыл флаг кругом раньше); правило,
закрывающее контроллер, больше не запускает свою же задачу; ранний буфер
`each` не принимает события после ранней ошибки; завершённая задача не
держит колбэки отмены.

Сьюта ядра выросла со 119 тестов до 179, у `solo` — с 244 до 250. Каждый
новый тест проверен мутацией. Из мутаций по `each` три эквивалентны —
ставят или читают `letGo` там, где подписка уже снята или на паузе;
четвёртая, которую я тоже записал в эквивалентные, таковой не была, и
четвёртый круг это показал: у асинхронного контроллера событие из
`onListen` приходит после возврата `listen`, и без стража обработчик
звался на уже помеченной задаче.

Пятый круг взял одно место — `ctx.each` целиком — тремя ревьюерами с
разными прицелами: машина состояний раннего буфера, паузы с доставкой,
контракт с покрытием. Тринадцать находок; три из них нашли независимо по
двое-трое, и это лучший признак, что место читали всерьёз.

Починено четыре вещи в коде. Ошибка уборки источника уходила в зону мимо
наблюдателя: `unawaited(sub.cancel())` — только маркер для линтера,
слушателя он не вешает, и падение `onCancel` источника роняло изолят;
теперь `.ignore()`. Бросок `listen` поверх уже отданной им ошибки
оставлял ожидание без слушателя — ровно то, от чего ожидание заводили
первым кругом раньше; теперь осиротевшее ожидание гасится там же. Окно
`listen` принимало события после конца стрима, хотя после ошибки не
принимало; теперь окно закрывают оба конца одинаково. И две переменные
ранней ошибки сведены в одну запись, отчего мёртвая ветка с подменой
стека исчезла вместе с возможностью им разъехаться.

Пять находок оказались не дефектами кода, а обещаниями, которых код
сдержать не может, и переписан дартдок. Главная — конец стрима: Dart
объявляет его только после того, как источник закончил отмену подписки,
поэтому источник с невозвращающимся `onCancel` вешает задачу насмерть, а
дартдок обещал и «возврат по концу стрима», и «подписку не дожидаются».
Обойти изнутри `listen` нечем — `await for` виснет так же, — поэтому
чинится формулировка, и то же сказано в README `solo` и в переводе.

Сьюта ядра выросла со 179 тестов до 192: семь дыр, найденных мутациями у
ревьюера, и по тесту на каждую правку и каждое решение. Проверено своим
прогоном: десять мутаций по этим местам ловятся все.

До ревью: уборка ресурсов доведена до конца и смержена. Спека
`2026-09-07[1]-dispose-design.md` с тремя дополнениями прошла три круга
ревью (`[2]` — семнадцать находок, `[3]` — девять, две критические, `[4]`
— девять), план `[5]` — ещё один круг, с исполнением на клоне (`[6]`,
восемнадцать находок, вторая редакция), и сам план исполнен целиком:
`2e91577`..`cb2e4be`, двенадцать коммитов. Вердикт стоит у каждой находки
во всех четырёх записях, отличия исполнения от плана — в конце `[5]`.
Дерево чистое, все проверки зелёные, ничего не опубликовано.

Что круги ревью поменяли в решениях по ходу дела. Второй: контекст тела в
фазе уборки закрыт целиком (чтение состояния в уборщике переворачивало исход
успешной задачи в `Cancelled(rules)`); передачу владения через `emit` держит
не неотменяемая секция, а синхронная тройка `check` → `disown` → `emit`;
значение, пришедшее после конца тела, убирают без условия по исходу — до
тела оно не доехало. Третий: границу «тело кончилось» ставит флаг в
`_execute` до ожидания детей; чтения закрыты только в уборке, а после
`finish` контракт цел; и — смена семантики `solo` — после конца тела правила
задачу не смотрят, потому что стеречь нечего, а терять есть что.

Что решала уборка: `try/finally` в теле не масштабируется на несколько
ресурсов, идёт голым `await` на пути отмены, работает раньше детей и не
дотягивается за `return`. Вместо него — стек регистраций, который
раскручивает движок после детей и до завершения: `dispose` при любом
исходе, `discard` — когда значение никому не досталось. Значение из
вызова регистрирует сам вызов (`wait`, `join`), остальное — члены
контекста; снимают функцией или `disown(value)`.

Что поменялось сверх этого. У задачи появилась четвёртая, внутренняя фаза
— уборка: тела нет, исход решён, задача не завершена; контекст тела в ней
закрыт целиком, и `isCancelled` в ней говорит по решённому исходу, потому
что движок кладёт отмену в `_pendingCancel` до раскрутки. Правила `solo`
после конца тела задачу больше не смотрят — раньше внешняя установка
состояния переворачивала успешный исход в `Cancelled(rules)` и
выбрасывала возвращённое значение, а уборка это окно только удлиняла.
Прямой `finish` из движка домена стек не раскручивает и пишет об этом в
`JobBase.debug`.

Последнее по времени — 2026-09-06, смена семантики `uncancellable`:
отклоняемую отмену, пришедшую внутрь секции, теперь придерживают, а не
отклоняют навсегда. Вопрос поднял владелец: задача с `cancellable: true`
доигрывала до конца, а `cancel()` пропадал молча. Пока секция открыта,
задачу не помечают вовсе — иначе сработали бы `onCancel` и каскад на
детей, то есть то, от чего шаг и защищают, — а на закрытии внешней секции
отмена применяется, и ближайший член контекста её бросает. `cancellable:
false` из конструктора остался окончательным отказом, правила solo как
проходили сквозь секцию, так и проходят, `close` по-прежнему дожидается
тела. Цена для тела: хвост после защищённого шага не выполнится, если
отмена пришла во время шага, — хвост, который обязан выполниться, кладут в
ту же секцию. Разбор и решение — в дополнении к спеке
`2026-09-05[4]-jobs-design.md`; в сьютах четыре новых теста у ядра и два
переписанных у `solo`.

Перед этим — ревью документации всех трёх пакетов
глазами их пользователей. У `solo` (`2026-09-06[5]-solo-docs-review.md`)
четырнадцать находок, все приняты; блокирующей была одна и не про прозу:
`example/pubspec.yaml` вёз в архив `dependency_overrides` на `../../jobs`,
и опубликованный пример не собрался бы ни у кого — оверрайд уехал в
`example/pubspec_overrides.yaml`, закрытый `.pubignore`. У `flutter_solo`
(`[6]`) двенадцать находок и один настоящий дефект: README был заготовкой
на 55 строк и ставил `^0.1.0`, примера не было вовсе, слова `close` не
было ни разу, а слушатель, подписавшийся после `close()`, всё ещё получал
уведомления. README переписан, пример заведён, дефект починен кодом с
тестом и мутантом.

Перед ними — четвёртый круг ревью `jobs`, тоже глазами пользователя
пакета: README, пример, дартдок, `pubspec.yaml`,
вид на pub.dev (`2026-09-06[4]-jobs-docs-review.md`). Кода находки не
касались; принято и внесено всё — четырнадцать находок и список «Что
дописать». Главная: README ни разу не говорил, что отмена приходит в тело
брошенным `Cancelled`, а `Cancelled` — это `Exception`, поэтому широкий
`catch` глотает отмену молча, тело идёт дальше, а исход остаётся честным.
Рядом: «Quick start» отменялся до старта тела и не исполнял ничего из
показанного, `ctx.each` в README не было вовсе, а дартдок `each` звал
`ctx.emit` из `solo`. В README добавились раздел «Testing», ответ «нужен
ли второй пакет» в «Why» и границы пакета; `example/example.dart` стал
«Quick start» дословно; в `pubspec.yaml` появились `topics` и
`issue_tracker`.

Перед ним — подготовка к публикации: решение публиковать ядро как `jobs`
(пункт 2 «Следующих шагов»), оверрайды уехали из `pubspec.yaml` обоих
зависимых пакетов в `pubspec_overrides.yaml`, и порядок связки записан по
шагам (пункт 1). Публикация ждёт отдельного запроса владельца.

Последняя законченная работа — вынос ядра в `packages/jobs` и два круга
независимого ревью кода по нему. Первый (`2026-09-06[1]-jobs-review.md`):
критических находок нет, четыре важные — неидемпотентный `finish`, две
дыры в `each` (утечка подписки и вечное ожидание при самоотмене тела) и
неверный дартдок `Failed`; оба дефекта `each` приехали из solo дословно и
до выноса никем не читались. Второй (`2026-09-06[2]-jobs-fixes-review.md`)
читал уже правки и нашёл регрессию от первого круга: колбэк отмены в
`each` мог сработать до присвоения подписки. Вердикт стоит у каждой
находки во всех трёх записях, все приняты и внесены; сьюта ядра выросла с
55 до 82 тестов. Третий круг (`2026-09-06[3]-jobs-third-review.md`) читал
правки второго: находок по коду нет, вердикт — идти на публикацию
можно.

До этого — решения владельца по трём последним находкам ревью переезда и
перевод дерева на 0.1.0 (`d95a423`, `0c75d71`): `emit(S)` ратифицирован в
разделе 3.7 спецификации, `ctx.cancellable` решено не заводить, версия
обоих пакетов и зависимость `flutter_solo` переведены на 0.1.0. Тогда же
поправлен пример `track` в README: он излучал состояние вне своего
рабочего типа — ровно та ошибка, от которой в 1.x защищал тип.

До этого — `ctx.each`: подписка на стрим, живущая ровно столько, сколько
задача (`docs/records/2026-09-05[3]-stream-subscription-design.md`). Ответ
на F2, единственную находку ревью, которую оба ревьюера назвали главной;
сделана extension'ом на `JobContext`, а не членом, поэтому интерфейс не
тронут.

Ещё раньше — волна правок по находкам двух ревью переезда
(`d61d263`..`1cf146e`). Кодом закрыты две: завершившийся контекст
больше не запускает действие (`wait`, `join`, `uncancellable` бросают
`StateError`, как `emit` и `run`), и `emit` проверяет отмену не только до
записи, но и после неё. Документацией закрыты восемь, включая две ошибки в
свежих текстах: `upload` в пункте 3 `vs-bloc.md` переведён на `join`, а
рецепт «Таймаут» в README переписан — `Future.timeout` не отменяет
исходную future, и `join` вокруг него дожидался только обёртки.

До неё — `ifCancelled` у `ctx.wait` и `ctx.join`
(`docs/records/2026-09-05[2]-resource-ownership-design.md`), находка
владельца; а до того третий член семьи ожидания `ctx.join` и правило «тело
не делает `await` само по себе: каждый вызов идёт через контекст»
(`docs/records/2026-09-04[4]-join-design.md`), а следом перевод примера с
камерой и его копии в README на `join`.

Сами ревью — Fable 5.1 и Codex, по одинаковому заданию, не зная ответов
друг друга — лежат в
`docs/records/2026-09-05[1]-generators-migration-review.md`: переезд верен,
критических находок нет, вердикт стоит у каждой находки.

Устройство пакета — в `docs/architecture.md`. История правок — в `git log`
и в записях `docs/records/`; у волн правок `vs-bloc.md` записи нет, они
описаны в сообщениях коммитов.

## Открытые вопросы

Из финального ревью не осталось ничего: список «Не чинили» закрыт
2026-09-04 (`docs/records/2026-09-04[1]-review-leftovers-report.md`), оба
вопроса о бросающих хуках — тогда же
(`docs/records/2026-09-04[2]-hook-isolation-report.md`). Вердикт по каждой
находке стоит в самом отчёте ревью.

Открытых вопроса к владельцу два, оба из пятого круга ревью
2026-09-07 и оба перечислены выше, в «В работе прямо сейчас»: оставить
ли как есть ошибку обработчика, перекрытую отменой, которую вызвал сам
`each`, и выносить ли `isDisposing` в публичный `Job` ради текста одного
сообщения. Разбор обоих — `2026-09-07[11]-jobs-each-review.md`, находки
12 и 13. Ни один публикацию не держит.

Решения 2026-09-05: публиковать `solo`
как 0.1.0, а не 1.0.0; `ctx.cancellable(action)` не заводить; `emit(S)`
оставить (раздел 3.7 спецификации); вынос `Job` в отдельный пакет делать —
и делать его **до** публикации, а не после (решение того же дня, поверх
записанного в плане обратного условия). Разбор соседнего пакета
`stream_future`, который этим ядром заменяется, —
`2026-09-03[3]-stream-future-report.md`.

Решение владельца 2026-09-06 по вопросу, который он же поднял:
`uncancellable` придерживает отмену вместо окончательного отказа
(`439c8fb`); разбор, цена и отличие от `join` — в дополнении к спеке
`2026-09-05[4]-jobs-design.md`. Вопрос закрыт.

Решения владельца 2026-09-07, принятые по ходу четырёх кругов ревью. Из
кода они не выводятся, поэтому здесь:

- **Ошибку, перекрытую поздней отменой, отдавать в зону**, а приоритет
  отмены над исходом оставить как был. Второй вариант — не давать отмене
  перебивать `Failed` — отклонён: он ломает обещание `whenCancelled`
  закрываться только на исходе `Cancelled`.
- **Каскад на детей — только при самоотмене тела** (`throw Cancelled`).
  Упавшее тело детей не трогает: README каскада для него не обещает.
- **Сигнатуру `onData` у `each` сменить** на
  `FutureOr<void> Function(T)` — до публикации это бесплатно, после
  ломающе.
- **Защищённую поверхность (`owner`, `parent`) не расширять**, пока
  `scopo` не покажет, что они ему нужны: добавить защищённый член позже —
  правка не ломающая.
- **Буфер ранних событий в `each` оставить**, а не переходить на
  `stream.asyncMap(...).listen(...)`: замерено, что и `asyncMap`, и
  `StreamIterator` теряют события окна `listen` молча. Причина записана
  комментарием в самом `each`.

Не написанное из «Что дописать» кругов документации, если владелец
захочет: у `solo` — фрагменты на `stateAs` и `check` (названы в
перечислениях, но не показаны) и рецепт «повтор с отступом», отложенный
ещё 2026-09-03. У `jobs` и `flutter_solo` списки закрыты целиком.

Из независимого ревью README (`2026-09-03[2]-readme-review.md`):

- Рецепта «повтор с отступом» (retry/backoff) в README нет. Отложен.
- Бейджей, оглавления и ссылки на API-доки в README `solo` нет (находка
  M14, отклонена; при желании владельца — вернуть). В README `jobs`
  ссылка на справку появилась 2026-09-06: там она держит раздел «Building
  on the core», половина которого живёт только в справке.

## Не трогать

- `/Users/user/development/yandex/yx_camera` — чужой проект, владелец в нём
  не участвует. Зависит от собственной копии `conveyor` по пути
  `packages/conveyor` внутри своего репозитория, с этим репозиторием не
  связан. Использовался только как источник данных о боевом применении
  (разбор в спецификации).

## Состояние дерева и проверок

Дерево чистое, все проверки зелёные (перепроверено 2026-09-07 после
пятого круга ревью: `analyze`, `format`, `test` 457, `doc`,
`check_translations.py` — всё сходится с записанным ниже).
`origin/main` — `3a66646`
(2026-09-05); всё, что после, лежит локально, 81 коммит, push будет
fast-forward и делается только по слову владельца. Из-за этого
`repository` у `jobs` сегодня отдаёт 404: `packages/jobs` есть только в
локальных коммитах.

Раскладка: корень — `AGENTS.md`, `CLAUDE.md`, `docs/`, `tool/`, `LICENSE`,
`README.md`, `.vscode/`. Пакеты в `packages/`, каждый со своими `LICENSE`,
`README.md`, `README.ru.md`, `CHANGELOG.md`, `analysis_options.yaml`,
`.pubignore`. Локально Dart SDK 3.13.0, Flutter 3.47.0 (fvm); пол во всех
пакетах — `sdk: ^3.6.0`.

Переводы: `README.ru.md` лежит рядом с оригиналом в каждом пакете и закрыт
его `.pubignore`; остальные переводы — в `docs/ru/<пакет>/`
(`docs/ru/solo/vs-bloc.md`). Правка оригинала и правка перевода идут одним
коммитом (`AGENTS.md`). Сверка: `python3 tool/check_translations.py` из
корня — четыре пары, порядок заголовков, число блоков кода и сам код
внутри блоков. В архив публикации русский текст не попадает, английские
документы на переводы не ссылаются.

### `packages/jobs`

`name: jobs`, `version: 0.1.0`, не опубликован. Исполнительное ядро
задачи: `Job` с фабрикой и `Job.deferred`, `JobContext`, исходы, дети,
семья ожидания, `each`, `JobObserver`, наследуемые `JobBase` и
`JobContextBase`. Зависимости: `meta: ^1.15.0`; dev — `fake_async: ^1.3.1`,
`lints: ^5.1.1`, `test: ^1.26.3`. В `pubspec.yaml` есть `issue_tracker` и
`topics`: `async`, `cancellation`, `concurrency`.

Проверки из папки пакета:

```sh
dart analyze                  # No issues found!
dart format --set-exit-if-changed .
dart test                     # 192 теста, все зелёные
dart doc .                    # Found 0 warnings and 0 errors
dart pub publish --dry-run    # 0 warnings; только проверка
```

`README.md` пакета: Why, Install, Quick start, Outcomes, Cancellation,
Children, Cleanup, Observer, Deferred start, Testing, Building on the core,
solo. Тринадцать блоков кода; собраны и прогнаны в стенде 2026-09-06, раздел
«Cleanup» переписан 2026-09-07 вместе с работой по уборке, а «Cancellation»
и «Children» — в тот же день по ревью ядра: `each` с асинхронным
обработчиком, каскад при самоотмене тела, границы `cancellable: false`,
чужой `Cancelled` из `child.value`. `example/example.dart` — «Quick start»
дословно, с фальшивой `Database` вокруг: запускается, печатает `database
closed` и `Cancelled(manual)`. В конце «Building on the core» — ссылка на
справку `JobBase` на pub.dev, она заработает после публикации.

### `packages/solo`

`name: solo`, `version: 0.2.0`. Зависимости: `jobs: ^0.1.0` (плюс
`dependency_overrides` по пути, пока ядро не опубликовано) и
`meta: ^1.15.0`; dev — `fake_async: ^1.3.1`, `lints: ^5.1.1`,
`test: ^1.26.3`. Новее только `lints` 6.1.0, но его пол SDK `^3.8.0` не
проходит по правилу проекта.

Проверки из папки пакета:

```sh
dart analyze                  # No issues found!
dart format --set-exit-if-changed .
dart test                     # 250 тестов, все зелёные
dart doc .                    # Found 0 warnings and 0 errors
dart pub publish --dry-run    # только проверка, ничего не публикует
```

README и пример прошли круг документации 2026-09-06
(`2026-09-06[5]-solo-docs-review.md`, четырнадцать находок): в README
появились отмена глазами вызывающего, фрагмент с `keepWhile`, границы
пакета в «Why», рецепт слежения за другим контроллером и раздел про
тестирование отмены; монополия владения сведена к корневым задачам;
`CHANGELOG` открывается первым публичным релизом. Двадцать семь блоков
кода, прогнаны в стенде; абзац «Taking ownership» переписан 2026-09-07
вместе с работой по уборке — стек вместо `try/finally`, тройка
`check`/`disown`/`emit` для передачи владения, состояние на выходе и
видимость ошибок уборщика. Оверрайд `jobs` у примера лежит в
`example/pubspec_overrides.yaml` и в архив не едет.

`README.md` пакета: Why, Install, Quick start, Concepts, Rules, Errors,
Testing, Recipes, Flutter, Coming from bloc, Example. Сравнение с bloc живёт
отдельно, в `packages/solo/doc/vs-bloc.md`: восемь сценариев из разных
предметных областей, в каждом сначала рабочее решение на bloc с его ценой,
потом то же на `solo`.

Каждый фрагмент `vs-bloc.md` попадает в текст дословно и проверяется
прогоном. Стенды собираются из репозитория:

```sh
python3 tool/doc_snippets.py            # пишет в /tmp/solo-doc-check
cd /tmp/solo-doc-check/bloc_check && dart pub get && dart analyze bin/v
dart run bin/v/item1.dart               # и так до item8
cd ../solo_check && dart pub get && dart analyze bin/v
```

`bloc_check` — против bloc 9 с bloc_concurrency, `solo_check` — против
локального пакета под его же `analysis_options.yaml`. За каждым
утверждением о поведении стоит прогон; для bloc — либо прогон-проба, либо
названная строка исходника. Тайминги в фейках настоящие, с запасом, не
`FakeAsync`.

Фрагменты README проверялись такими же одноразовыми стендами; постоянного
генератора для них нет.

### `packages/solo/example`

Отдельный пакет `solo_example` (`publish_to: none`) с фейковой камерой. У
него свой `README.md` — pub.dev показывает его на вкладке Example, потому
что точка входа лежит в `bin/`, а не в `lib/` или корне примера.
Проверки из его папки:

```sh
dart analyze                  # No issues found!
dart format --set-exit-if-changed .
dart test                     # 7 тестов, все зелёные
dart run bin/main.dart        # печатает журнал в реальном времени
```

Отказ железа изнутри операции задаётся картой `failures` у
`FakeCameraHardware`: ключ — имя операции без аргументов (`open`, `close`,
`zoom`, `focus`, `capture`), значение — что бросить после `latency`.

### `packages/flutter_solo`

`name: flutter_solo`, `version: 0.2.0`, `environment: flutter: '>=3.27.0'`.
`solo: ^0.2.0` плюс `dependency_overrides` на `solo` и на `jobs` по пути —
оверрайды нужны, пока пакеты не опубликованы, и дают ожидаемые hint'ы в
dry-run. В `pubspec.yaml` есть `issue_tracker` и те же четыре топика, что у
`solo`.

`README.md` переписан 2026-09-06 по ревью документации: 240 строк, девять
разделов (Why, Install, Usage, The controller's life, Outcomes, Testing,
Notes, solo), все восемь блоков кода прогнаны в стенде, виджетные — как
настоящие `testWidgets`. `example/` — отдельный пакет с `lib/main.dart`:
тот же фрагмент, что в Usage, с фальшивым API и экраном вокруг; его
локальные пути лежат в `example/pubspec_overrides.yaml` и закрыты
`.pubignore` пакета.

Проверки из папки пакета:

```sh
flutter analyze               # No issues found!
dart format --set-exit-if-changed .
flutter test                  # 8 тестов, все зелёные
dart doc .                    # Found 0 warnings and 0 errors
flutter pub publish --dry-run # только проверка, ничего не публикует
```

Из `packages/flutter_solo/example`: `flutter analyze` чист.

### Замечания dry-run

- `repository` заполнен во всех трёх `pubspec.yaml`; `homepage` не задан —
  pub.dev берёт `repository`, отдельная страница проекта владельцем не
  заводилась.
- У `solo` — hint про `dependency_overrides` (оверрайд на `jobs`), у
  `flutter_solo` — два таких hint'а. Ожидаемы и остаются, пока `jobs` и
  `solo` не опубликованы. Сами оверрайды уже лежат в
  `pubspec_overrides.yaml` (`446a3c6`), в архив не едут, и hint уходит
  только вместе с удалением файла на шаге публикации.

### Прочее

- `dart doc` кладёт результат в `doc/api` каждого пакета; шаблон
  `**/doc/api/` в `.gitignore` ловит их во всех пакетах, в дерево они не
  коммитятся и в архив публикации не попадают.
- Файлы `.DS_Store` и `build/` игнорируются через `.gitignore`.
- Пункты `packages/solo/TODO.txt`, не вошедшие в спецификацию (сам файл
  удалён в задаче 1b, в `docs/backlog.md` не переносились — решение за
  владельцем): пауза очереди (раздел 10 спецификации явно относит её к
  отложенному); метод взятия не первой задачи из очереди; аккумуляция
  однотипных задач (включая частный случай — метрики/логи); запуск
  последнего события при диспоузе очереди (чтобы новые не добавлялись); идея
  отмены рутовых `Future` через вложенные `runZoned`; API-удобство вида
  `state.scope`/`state.map().run` для матчинга состояния без повторов
  `state.isA`; тест на `debugInfo`; тест на «неудаляемые» события (в новом
  дизайне такого понятия нет, есть только «неотменяемые» — уже покрыто
  спецификацией); нюанс `restartable`: должен ли он удалять более ранние
  задачи в момент постановки в очередь или только при старте — в новом
  движке очередь устроена иначе, решение не перенесено.
