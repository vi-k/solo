# Handoff: текущее состояние проекта

Обновлено: 2026-09-02.

## Где мы

Пакет `conveyor` 1.0.0 (нигде не опубликован, потребителей нет)
переделывается в пакет `solo`: то же ядро последовательных задач с
гарантией монопольного владения состоянием, но с новым публичным API на
обычных `async`-функциях вместо генераторов.

Фаза: **дизайн согласован, спецификация записана, ждёт прочтения
владельцем.** Реализация не начата.

Спецификация: `docs/records/2026-09-02[1]-solo-design.md`. Секции 1–4
подтверждены владельцем в диалоге, секция 5 (тесты и пример) представлена
без возражений. Владелец ещё не читал документ целиком.

Код в `lib/` — старая реализация `conveyor` 1.x. Она рабочая и служит
источником семантики для переноса, но публичный API будет заменён целиком.

## Следующие шаги

1. Владелец читает спецификацию и даёт правки или добро.
2. После добра: план реализации в `docs/records/` (kind `plan`).
3. Реализация по плану, TDD: новое ядро в `lib/src/`, перенос тестов,
   пакет `example/` с фейковой камерой, `README.md` и `CHANGELOG.md`
   по-английски, `pubspec.yaml` с именем `solo`.

## В работе прямо сейчас

Ничего. Сессия остановилась между дизайном и планом.

## Открытые вопросы

- Номер первой версии `solo`: `1.0.0`, `2.0.0` как продолжение `conveyor`
  или `0.x` до стабилизации API. Решает владелец.
- Переименовывать ли папку репозитория `conveyor` в `solo`. Решает владелец;
  на код не влияет.
- Публиковать ли на pub.dev вообще. Имена `solo` и `conveyor` на pub.dev
  свободны (проверено 2026-09-02). Публикация только по правилам из
  `AGENTS.md`.

## Не трогать

- `/Users/user/development/yandex/yx_camera` — чужой проект, владелец в нём
  не участвует. Зависит от собственной копии `conveyor` по пути
  `packages/conveyor` внутри своего репозитория, с этим репозиторием не
  связан. Использовался только как источник данных о боевом применении
  (разбор в спецификации).

## Состояние дерева и проверок

- Dart SDK локально 3.13.0, в `pubspec.yaml` ограничение `^3.3.4`.
- `dart test`: 123 теста, зелёные.
- `dart analyze`: одно info в `lib/src/linked_list/linked_list.dart`
  (`parameter_assignments`), уйдёт вместе со связным списком.
- `README.md` устарел: на русском, про провайдер состояния 1.x. Переписать
  по-английски при реализации.
- `example/conveyor_example.dart` целиком закомментирован, черновик ранней
  идеи. Заменяется пакетом `example/`.
- `TODO.txt` — заметки владельца времён 1.x. Пункты, вошедшие в дизайн,
  отмечены в спецификации; файл убрать при реализации, перенеся остаток в
  `docs/backlog.md` только по просьбе владельца.
