# Handoff: текущее состояние проекта

Обновлено: 2026-09-26. Владелец читает `packages/async_job/doc/observing.md`
и её русскую версию `docs/ru/async_job/observing.md`. Ревью `solo`
и `flutter_solo` 2026-09-19 закрыто целиком: вердикт каждой находки
в `2026-09-19-solo-project-review.md` говорит, чем она закрыта, а `AGENTS.md`
с 2026-09-26 требует такого итога у каждой закрытой находки. `main`
до `fd75c39` отправлен на `origin`.

**В работе.** Чтение владельцем `observing.md`. Страница вычитана 2026-09-25
прямо в `main`, и каждый вопрос владельца — правка обеих версий, сторож и пункт
в разделе «По чтению владельца» `2026-09-25-observing-rakes-report.md`;
на 2026-09-26 там семь пунктов. Сторож страницы —
`packages/async_job/test/observing_rakes_test.dart`, 39 тестов, последний
сверяет цитаты страницы. Правка по чтению, задевшая другую страницу, пишется
и в её отчёт: так `children.md` получила одиннадцатую и двенадцатую правки
в `2026-09-18-children-rakes-report.md`. Больше у этой сессии в работе ничего
нет.

**Что дальше.** Владелец продолжает чтение `observing.md`. После неё по карте —
`packages/async_job/doc/extending.md` и README `solo` и `flutter_solo`, раздел
«Документация: вычитка страниц doc/»; следующую страницу берёт владелец.
В `docs/backlog.md` одна запись — общий отлов зависаний; по ней решено оставить
рецептами, пока не выполнится условие, названное в самой записи.

**Открытые вопросы владельцу.**

- Поле в `Cancelled`, которое несло бы покрытый провал читателю исхода:
  владелец рассмотрит его отдельно, позже (2026-09-25).
- Остаток ревью `solo`, по которому решений нет: прибор, который назвал бы, чем
  держится `close` при `pending == null` (M4 закрыт минимумом), и список «Чего
  не хватает» в `2026-09-19-solo-project-review.md` — запись внешнего факта
  после закрытия без броска, признак «работу отбросили» у `droppable`, проверка
  выпуска по архиву без оверрайдов, `assert` на заморозку в `_setState`, единая
  таблица миграции. 2026-09-26 предложено, если что-то брать, взять прибор;
  ответа не было.
- Dartdoc `async_job` по-прежнему говорит «marked», 17 строк `///` в `lib/`
  на 2026-09-26: по второму вопросу чтения `cancellation.md` слово ушло
  со страниц, а dartdoc в ту правку не входил. Отдельно не предлагалось.

**Решения владельца, которые действуют.**

- Судьба оставшихся находок ревью решена 2026-09-19 ответами на вопросы: пять
  Medium, список «что лишнее» и четыре Low, которые ревью оставило владельцу.
  Полный список — раздел «Решения владельца» в конце
  `2026-09-19-solo-project-review.md`; волны A, B, C и D сделали их все. Против
  моей рекомендации одно: `job`, `add`, `run`, `collect` и `accumulate`
  защищённые с `0.3.0` — сделано волной C.
- Хвост «Cleanup order and late results» сведён 2026-09-19: порядок шагов
  описан и хранится в `packages/async_job/doc/cleanup.md`,
  а `packages/solo/doc/resources.md` держит таблицу — её требует правило
  «раздел начинается с кода или таблицы» — и говорит рядом, что первые три шага
  принадлежат движку, а контроллер добавляет последний. Дублирующая проза про
  второй проход из `solo` убрана. Ссылка между пакетами — абсолютная на GitHub,
  как в `flutter_solo/README.md`: сайт превращает её в свою страницу и держит
  локаль.
- «leaks» в английской `packages/solo/doc/resources.md` остаётся как есть,
  2026-09-19: замену, сделанную в русском переводе вместо метафоры «течёт»,
  на оригинал не переносить.
- Правка личных скиллов идёт порциями, 2026-09-19: одна проверенная правка —
  один коммит, а рецепт из команд встаёт в текст только после прогона
  на тяжёлом случае. Правило записано в `README.md` репозитория скиллов
  коммитом `866c1ea`; повод — `2026-09-19-end-handoff-skills-review-3.md`.
- Выпуск `0.3.0` отложен 2026-09-15 словом «пока рано»; собранное к нему —
  раздел «На следующий релиз собрано у всех трёх». Push — только отдельным
  поручением: прошлые разрешения были на свои push и на них одни.
- Перенос владения отменён окончательно 2026-09-15:
  `2026-09-14[17]-discard-follows-the-value-design.md` не делать. Освобождение
  регистрирует получатель — `ctx.run(opener, discard: …)`; разбор —
  `2026-09-15[8]-cleanup-registers-on-arrival-report.md`.
- Кортежная форма `ctx.runAll` отложена: добавляется потом без слома.
- Накопление через `externalSetState` отклонено:
  `2026-09-10[6]-state-accumulation-design.md` не реализовывать. Досылка при
  закрытии, надёжный буфер и автоматические повторы — отдельная работа,
  не начата.
- Имя дренажа по-русски, 2026-09-22: `SoloCloseMode.drain` в русской прозе
  зовётся «дренаж», и это единственное существительное для него — по нему
  читатель догадывается сам, а «слив» такого не даёт. «Досылка» и «слив»
  из документов убраны; где проза обходится без существительного, она называет
  сам вызов `close(mode: SoloCloseMode.drain)`, а состояние — `isDraining`.
- Бэктики в русской прозе, 2026-09-15: в них берут inline-код, имена классов
  и имена переменных; future в прозе — обычное слово, строчными и без бэктиков
  (`86edb87`).
- README объясняет API новому читателю, без ссылок на прежнее поведение
  и контекст диалога; в README `async_job` сохранять `removeDisposer` и пример
  ленивого лога из `479e934`.
- «The second attempt» в `accumulation.md` остаётся без входа отмены: попытка
  должна провалиться, иначе накопитель ниже не нужен (2026-09-15).
- Старые записи `docs/records/` не переименовывать, 2026-09-19: имена
  `YYYY-MM-DD[N]-…` остаются как есть, предложение отклонено.
- scopo не трогать, 2026-09-19. Находка о его `CHANGELOG.md` — раздел `0.14.0`
  говорит об удалении `ScopeInitState`, `ScopeProgress` и `ScopeReady`,
  а в дереве тега они есть — передана владельцу и закрыта этим решением.
- Упоминание Fable в `end/SKILL.md` оставлено по слову владельца, 2026-09-19:
  оно внутри цитаты испорченного handoff'а.

**Грабли.**

- Старая запись, названная `tool/reflow.py` ради правки шапки, перезаливается
  целиком — шапку править руками и скрипту запись не называть. Склейка
  вложенных списков починена 2026-09-19 в обеих копиях скрипта, `verify.py`
  скилла `reflow` её теперь видит; сторожа — `tool/reflow_test.py`, шаг
  в задании `docs` гейта.
- Миксин `SoloListenable`: в позиции типа аргумент обязателен, хотя
  `strict-raw-types` его не требует; прямое создание —
  `class C<S extends Object> = Solo<S> with SoloListenable;`, форма с `{}`
  не компилируется. Старшинство `onListenerError` —
  `2026-09-18[1]-solo-listenable-mixin-design.md`.
- Пол `sdk: ^3.6.0` не проверен никем: `lints` 6 и `flutter_lints` 6 требуют
  Dart 3.8, поэтому на 3.6 и 3.7 `pub get` в репозитории не разрешится. Нужен
  такой пол для работы — линты не поднимать, не подняв пол пакетов.
- Версии пакетов и экшенов выяснять командой — `npm view <пакет> version`,
  `gh api repos/<экшен>/git/matching-refs/tags/v`, — а не по памяти.
- Что в `async_job` держится и чего не трогать — раздел «Positive findings»
  в `2026-09-15[17]-async-job-project-review.md`.
- Push из-под песочницы инструмента не проходит: `curl` и `gh` до github
  доходят, а `git` получает «Failed to connect to github.com port 443», —
  отправлять с отключённой песочницей. Соединение бывает отваливается
  по таймауту в 75 секунд, помогает повтор.

**Не трогать.** `~/.claude/skills` опережает свой `origin` на двенадцать
коммитов, от `b74042a` до `8aa96bd`, — правки `codex`, `agy` и README; этой
сессии они не принадлежат, и отправлять тот репозиторий без поручения владельца
нельзя. `~/development/yandex/yx_camera` — чужой проект. В `kokjai`
неотправленного в `main` нет, а коммит `ea18852`, заливка его документов,
не лежит ни в одной ветке.

**Дерево и проверки.** Общий checkout `~/development/my/solo`, ветка `main`;
с 2026-09-25 по слову владельца работа идёт прямо в ней, перед каждым
коммитом — ветка и `git status`, чужое не коммитится. Других веток нет
ни локально, ни на `origin`; копий дерева, stash и `.git/wip` нет,
незакоммиченного нет, скретчпад сессии пуст. `origin/main` на момент записи —
`fd75c39`; коммиты вычистки handoff поверх него не отправлены. CI: `gate`
и `site` на `44a9c19` зелёные (`36253615225` и `36253615212`), `gate`
на `63016dd` зелёный (`36253732339`). Проверки на последней правке кода,
`d3916fb`, 2026-09-26: `packages/async_job` — формат, анализ
и `dart doc --dry-run` чистые, `dart test` 628; `packages/solo` — анализ чист,
803, пример 47; стенды `vs-bloc`, `accumulation` и Flutter собраны и прогнаны,
цитаты трасс сходятся; сайт собран, 42 страницы. `packages/flutter_solo`
на `63016dd` — 85 тестов, пример 4. После `d3916fb` менялись только записи,
`AGENTS.md` и этот файл; пять документных проверок из корня на них зелёные.

## На следующий релиз собрано у всех трёх

Ломающие правки есть в каждом пакете, поэтому все трое идут на `0.3.0`.
Развёрнуто — в разделах `## Unreleased` их `CHANGELOG.md`; коротко (счёт сверен
2026-09-19): у `async_job` шесть ломающих, громкие из них — переезд `JobBase`,
`JobContextBase` и `JobStatus` в `engine.dart` и протектед
`JobBase.inUncancellableSection`; у `solo` четырнадцать своих (`SoloBase` →
`Solo` вместе с переездом стрима в `SoloStream`, защищённые `job`, `add`,
`run`, `collect` и `accumulate`, `currentState`, `SoloCloseMode`,
`SoloTransition`, `errorHandler`, свои слушатели, `isFinished`, финальное
состояние закрытого, умолчание накопителя `join`, перенос вместо отмены
у `AccumulationPolicy.replace`, две про `droppable` и снятый
`SoloQueue.lastWhere`) плюс раздел унаследованных, `Solo.pending` и новая
библиотека `package:solo/listeners.dart`; у `flutter_solo` семь своих
ломающих — шестая, снятый реэкспорт `ValueListenable`, из волны B, седьмая,
`SoloSubscriptions.length` без отменённых, из волны D — плюс раздел
унаследованных, и новые `SoloSelection`, `SoloSelection.from`, `SoloSelector`,
`SoloBuilder` и `select`/`listen`.

Порядок связки задан зависимостями: `solo` уже пользуется новым
`inUncancellableSection`, поэтому в дереве и живут четыре
`pubspec_overrides.yaml`. Сперва `async_job`, затем `^0.3.0`
в `packages/solo/pubspec.yaml` и `solo`, затем `^0.3.0`
в `packages/flutter_solo/pubspec.yaml` и `flutter_solo`. Оверрайды снимаются
и добавляются в индекс — порядок
в `2026-09-11[1]-solo-release-readiness-report.md`.

Версии в трёх `pubspec.yaml` пока `0.2.0`, заголовки в `CHANGELOG.md` —
`## Unreleased`; и то и другое меняется при подготовке. Вместе с ними — строка
про стенд во вступлении `vs-bloc` (оригинал и перевод): там названа версия
исходников, на которых собраны фрагменты, и сейчас это `0.2.0`.

## Документация: вычитка страниц doc/

Карта вычитки — `2026-09-11[16]-docs-structure-design.md`. По ней вычитаны все
страницы до `packages/async_job/doc/observing.md`, и все, кроме неё, владелец
прочитал; её он читает сейчас. Остались `packages/async_job/doc/extending.md`
и README `solo` и `flutter_solo`. Разбор страницы — в её отчёте
`<дата>-<тема>-rakes-report.md`, чтение владельца — под «По чтению владельца»
в конце того же отчёта.

У страницы без стенда утверждения держит тест пакета, и правка страницы идёт
вместе с ним. В `async_job` это `cleanup_rakes_test.dart`,
`outcomes_rakes_test.dart`, `cancellation_rakes_test.dart`,
`observing_rakes_test.dart` и `children_rakes_test.dart`. В `solo` —
`state_rakes_test.dart`, `cancellation_rakes_test.dart`,
`resources_rakes_test.dart`, `errors_rakes_test.dart`
с `cancel_delay_recipe_test.dart` и `testing_rakes_test.dart`; `camera.md`
держит `camera_rakes_test.dart` пакета примера, а `children.md` —
`children_test.dart`, `each_test.dart`, `each_child_test.dart`
и `zone_test.dart`.

`packages/async_job/doc/observing.md` вычитана 2026-09-25 в `main`. Четыре
раздела из пяти открываются первой попыткой, раздел о наблюдателе — ответом.
Главная находка — абзац о времени отмены: наблюдатель считает от принятия
отмены, и придержанная `uncancellable` отмена показывает меньше, чем ждал
вызвавший `cancel`. Сторож сверяет и цитаты страницы. Отчёт —
`2026-09-25-observing-rakes-report.md`. Владелец читает страницу, вопросы
и правки по ним — под «По чтению владельца» в конце отчёта.

**Код `jobs.md` не собирается ничем.** Стенды есть только у `vs-bloc.md`
(`tool/doc_snippets.py`), `accumulation.md` (`tool/accumulation_snippets.py`),
README `flutter_solo` с его `doc/mixins.md` и быстрого старта README `solo`
(`tool/flutter_snippets.py`); у остальных страниц фрагменты не компилируются
ни в гейте, ни перед коммитом. Фрагменты `jobs.md` собраны отдельным проектом
вручную — компилируются все, — но на дереве такой проверки нет. Рецепт паузы
при этом сторожит `packages/solo/test/queue_pause_recipe_test.dart`, семь
сторожей.

**Стенда у `state.md` тоже нет, сторожа — тестом.** Каждую первую попытку
страницы и каждое утверждение, добавленное чтением владельца, держит
`packages/solo/test/state_rakes_test.dart`, и у каждого сторожа своя мутация
движка. Правка страницы идёт вместе с ним.

## Требования к README, которые нужно сохранить

- При описании API на русском называть `Job` прямо. Слово «задача» уместно для
  самой работы, но не должно подменять имя класса в объяснении его
  возможностей.
- `ctx.join(Database.open)` объясняется самостоятельно до сравнения с `wait`:
  вызывает операцию, ждёт её завершения и проверяет отмену. `wait`
  предлагается, когда нужно прекратить ожидание сразу при отмене.
- `discard` после `return` объясняется на примере ожидания дочерней задачи:
  тело вернулось, но весь `Job` ещё не завершился.
- В примере миграции `onCancel` передаёт сигнал токену, `join` ждёт фактической
  остановки, а `uncancellable` защищает шаг от самого сигнала.
- Внешние асинхронные операции в теле следует ожидать через контекст. Прямой
  `await` допустим, но сам не проверяет отмену. Внутри действия, переданного
  контексту, и при уборке обычный `await` уместен с учётом описанных в README
  границ.
- Упоминание `solo` следует из границ ядра: в `async_job` нет управления
  состоянием, очереди и правил планирования; `solo` добавляет их.
- Два примера проброса `Cancelled` показывают отдельную ветку `catch`
  и проверку типа внутри общего обработчика. Оба способа подходят и для
  `Exception`, и для `Object`; выбор типа не различает эти способы.
- Принятая отмена самого `Job` сохраняется после перехвата исключения. Код
  после `catch` может выполниться, следующая контрольная точка снова бросает
  отмену, а при завершении исход остаётся `Cancelled`. Перехваченный
  `Cancelled` от операции или дочерней задачи сам по себе родителя не отменяет:
  возможен `Done`.

Число блоков кода в README больше не годится как ориентир: материал разъехался
по страницам `doc/`. Форму держат `check_translations.py` (двадцать пар)
и `check_doc_shape.py`. Примеры `then`, его контракт и переводы обновлены
вместе с реализацией.

## Дальше, когда владелец выберет новую работу

Материал и замеры: `2026-09-09[9]-todo-review.md`. Готовый стенд может лежать
в `.artifacts/2026-09-09-wave2-review/a/stand`; восстановление —
`python3 tool/doc_snippets.py <каталог>`. При проверке внешней операции
дожидаться самой операции: снимок состояния сразу после отмены `Job`
не показывает её окончательный результат.

Пожелания владельца находятся в `docs/backlog.md`. После окончания текущей
реализации предложить выбрать оттуда работу; самостоятельно записи не менять.
Другие ранее отложенные примеры для `solo`: `stateAs`, `check`, retry/backoff.
Их брать только после выбора владельца.

Имя ядра решено 2026-09-19: держатель `job` ответил, и владелец остаётся
на `async_job`. Напоминаний больше не нужно, переписку не продолжать; разбор —
`2026-09-05[8]-job-name-request-report.md`, его шапка закрыта тем же днём.

## Ограничения и общение

- Не приписывать владельцу ответы на формы или выбор вариантов, которые он
  не давал. Собственную интерпретацию запроса называть своей.
- Владелец просит минимум служебного вывода: ему нужны сообщения по делу,
  а не журналы запусков. Заголовки инструментов формирует интерфейс; не обещать
  скрыть их, сокращать число вызовов и объём вывода.
- Текст рабочих документов переносить на 79 символах. Правило и порядок
  проверок сохранены в `AGENTS.md`.
- Английский README и русский перевод править вместе. Связность и смысл важнее
  косметического сокращения; подтверждённые условия не выкидывать.
- Не менять заодно согласованную семантику отмены.
- `ctx.run` принимает `Job<T>`, а не только `DeferredJob<T>`: библиотечные
  классы на основе `JobBase` тоже могут быть дочерними. Автоматически
  запускающийся обычный `Job` отвергается.
- Не расширять защищённые `owner` и `parent` без подтверждённой нужды
  потребителя. `isDisposing` не выносить в публичный `Job` без нового решения.
- Буфер ранних событий в `each` сохранять; прежние альтернативы теряли события
  при подписке. При ошибке тела дочерние задачи дорабатывают; при самоотмене
  тела отмена передаётся детям.
- Соседний `scopo` ведётся отдельно. Его прежний переход на опубликованное ядро
  0.1.0 выполнен; обновление на новый API не входит в эту работу. Архивирование
  `stream_future` тоже отдельная внешняя задача.
- `/Users/user/development/yandex/yx_camera` не трогать: это чужой проект.
- Не запускать push, публикацию, теги или релизные коммиты без отдельного
  поручения. Правила связки публикации полностью заданы в `AGENTS.md`.

## Архив

- Законченное до 2026-09-18 — `2026-09-18[6]-handoff-archive.md`.
- Законченное до 2026-09-18, второй вынос — `2026-09-18[7]-handoff-archive.md`.
- Законченное до 2026-09-19 — `2026-09-19-handoff-archive.md`.
- Законченное до 2026-09-19, третий вынос — `2026-09-19-handoff-archive-2.md`.
- Прежняя большая версия handoff — в git на `659512a`.
- Законченное до 2026-09-26 — `2026-09-26-handoff-archive.md`.
