# Handoff: текущее состояние проекта

Обновлено: 2026-09-23. Пятый круг независимого ревью личных скиллов `end`
и `handoff` пройден, 52 пункта внесены; две ошибки вчерашнего разбора про
`/clear` исправлены, а из `codex` и `agy` по слову владельца убран Fable. Ветка
`docs/resources` влита в `main` и отправлена, обе её страницы прочитаны
владельцем. Полное независимое ревью `solo` и `flutter_solo` проведено;
по слову владельца починены три его находки High и тринадцать Medium, среди них
хук `Solo.onClose`, стенд README `flutter_solo` и тесты его примера.
По остальному владелец принял решения, и все четыре волны правок сделаны: ревью
закрыто целиком. Следом по карте вычитки взята `packages/solo/doc/errors.md`:
вычитана, влита в `main` 2026-09-22, чтение владельца по ней закончено
2026-09-23.

**В работе.** Ничего не начато. Чтение владельцем `packages/solo/doc/errors.md`
и её русской версии `docs/ru/solo/errors.md` закончено 2026-09-23: восемнадцать
правок, каждая в обеих версиях, список — в конце
`2026-09-20-errors-rakes-report.md`. Дальше по карте вычитки идут
`packages/solo/doc/testing.md`, `camera.md`,
`packages/flutter_solo/doc/mixins.md`, четыре оставшихся страницы `async_job`
и README `solo` и `flutter_solo`.

Ветка `error-hook-split` ждёт владельца: 28 коммитов, сливать
`git merge --ff-only error-hook-split` из общего checkout.

Разделение хука ошибки влито владельцем в `main` 2026-09-22, коммит `eb94a2a`:
`Solo.onError` — оповещение, ответ за ошибку — новый `Solo.onUnanswered`,
маркер `_homeless` из ядра убран. Отчёт —
`2026-09-22-error-hook-split-report.md`, батарея зелёная. Сессия по-прежнему
заперта в worktree `.claude/worktrees/doc-errors` на ветке `error-hook-split`,
и по общему checkout она не ходит: следующие коммиты этой сессии тоже сливает
владелец.

**Ревью `solo` и `flutter_solo` проведено 2026-09-19** по поручению владельца;
разбор с вердиктами — `2026-09-19-solo-project-review.md`. Четыре ревьюера,
по копии дерева на каждого, дерево на `67b0a3e`: три на Opus — состояние
и закрытие, очередь и накопление, весь `flutter_solo`, полного ревью которого
не было ни разу, — и codex Astra на публичной поверхности трёх пакетов
и готовности к `0.3.0`. Находок 34 после сведения дубликатов: 3 High, 18
Medium, 13 Low. Оценки проекту: 8, 8, 7 и 7 из 10.

Три High: вечный `close(mode: drain)`, если снять последнюю ждущую группу;
`Policy.droppable`, отдающий отменённую или завершённую работу;
`SoloSubscriptions.cancel()`, который бросает из `dispose()` и уносит
размонтирование целого кадра. Все три и все восемнадцать Medium перепроверены
мной — своими зондами или перезапуском чужих в отдельной копии; где мой прогон
разошёлся с прогоном ревьюера, в записи сказано прямо.

**Три High починены 2026-09-19** по слову владельца («H1, H2, H3»), коммит
`f18efc6`; отчёт — `2026-09-19-high-findings-report.md`. Шесть сторожей,
и каждая половинка каждого условия проверена своей мутацией. H1:
`_onJobFinished` будит насос для любой закончившейся работы, пока идёт
дренаж, — очередь пустеет не только насосом. H2: `droppable` и защищённый
`lastJobWhere` не отдают текущую работу, которая отменена или уже закончилась;
открытый вопрос находки решён одним правилом на оба, а не двумя методами. H3:
`SoloSubscriptions.cancel()` не бросает вовсе, каждый отказ уходит
в `FlutterError.reportError`.

**Четыре кодовых Medium починены 2026-09-19** по слову владельца — M12, M7, M2
и M3; отчёт — `2026-09-19-medium-findings-report.md`. M12: `@protected`
повторён на `SoloListenable.onListenerError`. M7: таймеры накопления гасятся
в `_finishClose`, общем конце обоих путей закрытия. M2: упавший `publish`
не уносит изменения, стоящие за ним, — очередь дочерпывается, первая ошибка
выходит после; строка `doc/state.md` о том, куда уходит такая ошибка, была
неверна и переписана в обеих версиях. M3: законченная задача отпускает тело,
обработчики и `_parentJob`; правила остаются — вердикт ревью тут неточен,
утёкший контекст читает `keepWhile` после исхода, и два теста покраснели, когда
их отпустили.

**Ещё пять Medium починены 2026-09-19** по слову владельца («M15, M16, M11,
M10», следом «M6 - onClose»); отчёт — `2026-09-19-medium-findings-report-2.md`.
M15: вводный пример `doc/cancellation.md` отдаёт открытое `join` в `dispose`,
а не регистрирует уборку следующей строкой. M16: README `flutter_solo`
и инвариант 4 `docs/architecture.md` больше не говорят, что наблюдатель
перехватывает ошибку. M11 и M10: модель README `flutter_solo` получила
`canSave` и `save`, рецепт проекции в поле пересобирает её в `didUpdateWidget`,
а сам README собирает `tool/flutter_snippets.py` — двенадцать виджетных тестов
в задании `flutter` гейта; по дороге стенд поймал неверный комментарий о втором
нажатии в разделе Outcomes. M6: у `Solo` появился хук `onClose` — один раз,
после наблюдателя и последней задачи, — и рецепт `Camera` в `doc/state.md`
переехал в него из `async`-переопределения `close`.

**Ещё четыре Medium починены 2026-09-19** по слову владельца («ок»
на предложенные M13, M17, M8 и M4); отчёт —
`2026-09-19-medium-findings-report-3.md`. M13: пример `flutter_solo` больше
не застревает на `Loading` после провала — задача падает в `Empty`, экран даёт
`Cancel` и перезагрузку, у примера четыре своих виджетных теста в задании
`flutter` гейта. M17: оба верхних changelog открываются пунктом унаследованных
сломов со ссылками на нижние. M8: `SoloSelection` выбирает один раз на повод —
подписанная выборка держит последнюю выборку вместе со значением источника. M4:
dartdoc `Solo.pending` и `SoloStream.close`, `doc/errors.md` и `doc/state.md`
говорят, когда `close` ждёт при `pending == null`; прибора, который назвал бы
это ожидание, нет. Оставшиеся 5 Medium и 13 Low закрыты волнами решений
владельца, последняя из них — волна D.

Чтение `packages/async_job/doc/cleanup.md` владельцем закончено 2026-09-19:
пять правок, все внесены в обе версии, что именно — в конце
`2026-09-19-cleanup-rakes-report.md`. Из чтения вышли и два сторожа
инструментов: `tool/check_links.py` со своими тестами и шагом в гейте и починка
`tool/check_line_width.py`, которая расходилась с заливкой на ссылке без
`http`.

Правка личных скиллов по слову владельца 2026-09-19: из `codex` и `agy` убран
Fable. В `codex/SKILL.md` и в шапке `run.sh` строка о замене Astra больше
не называет модель — исполнителя на ревью называет владелец, сам замену
не подбирай, и идти к нему надо после пробы, а не вместо неё; в `agy/SKILL.md`
из замера 2026-09-15 убрана колонка стороннего ревьюера, мерой качества там
остались три ошибки, лежавшие в документах коммита. Коммиты `9dc5b08`,
`a8c58a0` (попутная жадная заливка постороннего абзаца, отдельным коммитом,
`verify.py` — «0 differ») и `e2ebf0e`. Упоминание Fable в `end/SKILL.md`
оставлено по слову владельца: оно внутри цитаты испорченного handoff'а.

Пятый круг ревью скиллов `end` и `handoff` закончен 2026-09-19: три ревьюера
на Opus дали 57 находок, сведённых в 52 пункта, все приняты и внесены
в `~/.claude/skills` коммитом `92ae5de`. Разбор с вердиктами —
`2026-09-19-end-handoff-skills-review-3.md`. Круг брали на формулировки
четвёртого, и они не устояли: четырнадцать пунктов из пятидесяти двух —
об одном рецепте, заведённом прошлым кругом, и он заменён целиком. Откат файла
к `HEAD` убран: редирект обнулял файл раньше, чем `git show` успевал упасть,
а возврат копии откатывал свой же коммит. Теперь чужие строки убираются
Edit'ами на время коммита, а сверка требует, чтобы дерево вернулось ровно как
было.

Две ошибки нашлись не в скилле, а в том, что я в него записал. Утверждение
о расписаниях стояло на цитате, которой в бандле 2.1.278 нет вовсе:
`durable`-задача `CronCreate` переживает и очистку, и перезапуск CLI. Цитата
«так портится верх» в истории `docs/handoff.md` тоже не существует — пересказ
стоял в кавычках. Обе пришли из отчётов субагентов и были приведены как
источник. Запись `2026-09-19-clear-and-live-work-report.md` получила раздел
поправок, и её шапка теперь говорит, что часть выводов неверна.

`handoff/check_move.py` поправлен по семи пунктам: счёт копий заменён
на баланс — архив не может нести больше копий блока, чем отдал файл, — забор
перестал резаться на комментариях внутри него, незакрытый забор называется
отдельно, а расхождение пустых строк больше не выдаётся за потерянный текст.
Сторожей 17, шесть краснеют на прежней версии; на трёх настоящих уборках solo
по-прежнему ноль проблем.

Четвёртый круг ревью скиллов `end` и `handoff` закончен 2026-09-19: три
ревьюера на Opus дали 38 находок, сведённых в 37 пунктов, все приняты и внесены
в `~/.claude/skills` коммитом `818b3c5`. Разбор с вердиктами —
`2026-09-19-end-handoff-skills-review-2.md`. Самое тяжёлое:
`git commit -- <путь>` берёт файл из дерева целиком, поэтому способ писать при
чужой правке, заведённый третьим кругом, уносил чужую работу в мой коммит;
теперь там рецепт с копией, проверенный пробой. Вместе с текстом поправлен
`handoff/check_move.py`: строки отчёта печатаются целиком, «скопировали,
а не перенесли» считается числом вхождений, забор в шапке записи больше
не режет тело. Сторожей стало 11, четверо краснеют на прежней версии скрипта.

Непроверенное утверждение `end` о фоновых задачах закрыто тем же днём: `/clear`
фоновые Bash и субагентов не убивает, но отчёт до новой сессии не доходит —
уведомление кладётся в новую беседу и глохнет там синтетическим ходом, что
видно в транскрипте владельца. Расписания (`CronCreate`, `ScheduleWakeup`,
`/loop`) очистку не переживают вовсе. Правило скилла теперь такое: всё, что
остаётся жить, пишет результат в файл, и блок называет этот файл. Разбор —
`2026-09-19-clear-and-live-work-report.md`.

Третий круг ревью скиллов `end` и `handoff` закончен 2026-09-19: три ревьюера
на Opus дали 35 находок, сведённых в 24 пункта, все приняты и внесены
в `~/.claude/skills` коммитом `d248bef`. Разбор с вердиктами —
`2026-09-19-end-handoff-skills-review.md`. Вместе с текстом скиллов поправлен
`handoff/check_move.py`: блок, оставшийся в файле состояния, теперь проблема,
печать не обрезается, пропавшие строки идут в порядке файла; сторожа —
`handoff/check_move_test.py`, три теста.

Копия `~/development/my/solo-children` снята, ветка `docs/children` удалена,
зонды и эталонные копии `lib/` ушли вместе с копией — на диске их больше нет.
Что они показывали, записано в `2026-09-18-children-rakes-report.md`.

**Ветка `docs/resources` влита в `main`.** Слияние 2026-09-19 принесло
восемнадцать коммитов вычитки `packages/solo/doc/resources.md`
и `packages/async_job/doc/cleanup.md`: первые попытки, сторожа, переводы и три
записи — `2026-09-19-resources-rakes-plan.md`,
`2026-09-19-resources-rakes-report.md` и `2026-09-19-cleanup-rakes-report.md`.
Владелец прочитал обе страницы: по `resources.md` внесены все тринадцать его
правок, по `cleanup.md` — пять. Сама ветка осталась и локально, и на `origin`
на `4f38faf`; снять её там можно только push'ем.

**Что дальше.** Бэклог владельца пуст; следующую работу выбирает он. Кандидаты:

- ревью `solo` и `flutter_solo` закрыто: все четыре волны решений владельца
  сделаны, отчёты — `2026-09-19-review-decisions-a-report.md`,
  `2026-09-19-review-decisions-b-report.md`,
  `2026-09-19-review-decisions-c-report.md`
  и `2026-09-20-review-decisions-d-report.md`. Из ревью не взято три
  предложения сверх находок, и почему — в конце последнего отчёта;
- порядок дальнейшей вычитки — раздел «Документация: вычитка страниц doc/»:
  `resources.md` и `cleanup.md` вычитаны и влиты, `errors.md` вычитана
  с первыми попытками, влита в `main` и прочитана владельцем, дальше по карте
  идут `testing.md`, `camera.md`, `packages/flutter_solo/doc/mixins.md`, четыре
  оставшихся страницы `async_job` и README `solo` и `flutter_solo`. Документных
  находок ревью для этой работы не осталось: M1 решён волной A, README
  `flutter_solo` собирается стендом, а страница о Flutter по решению L9
  переехала в `packages/flutter_solo/doc/mixins.md` и сведена с README.
- разделение хука ошибки сделано 2026-09-22 по плану
  `2026-09-22-error-hook-split-plan.md`; отчёт —
  `2026-09-22-error-hook-split-report.md`. Влито в `main` тем же днём.

**Открытые вопросы владельцу.** Нет. Имя нового хука взято из плана —
`onUnanswered`; если владелец предпочтёт `answerFor` или `onHomeless`,
переименование стоит одной правки.

Пятый круг ревью скиллов владелец взял в работу 2026-09-19, тем же ответом
велев убрать скретчпад прошлого круга. Два вопроса до него взяты в работу
тем же днём и сделаны, а четыре закрыты ещё раньше: ревью скиллов, заливка
и два отклонённых предложения, перешедших в решения ниже.

**Решения владельца, которые действуют.**

- Судьба оставшихся находок ревью решена 2026-09-19 ответами на вопросы: пять
  Medium, список «что лишнее» и четыре Low, которые ревью оставило владельцу.
  Полный список — раздел «Решения владельца» в конце
  `2026-09-19-solo-project-review.md`; волны A, B, C и D сделали их все. Против
  моей рекомендации одно: `job`, `add`, `run`, `collect` и `accumulate`
  защищённые с `0.3.0` — сделано волной C.
- Хвост «Cleanup order and late results» сведён 2026-09-19: порядок шагов
  описан и хранится в `packages/async_job/doc/cleanup.md`,
  а `packages/solo/doc/resources.md` держит таблицу — её требует правило
  «раздел начинается с кода или таблицы» — и говорит рядом, что первые три шага
  принадлежат движку, а контроллер добавляет последний. Дублирующая проза про
  второй проход из `solo` убрана. Ссылка между пакетами — абсолютная на GitHub,
  как в `flutter_solo/README.md`: сайт превращает её в свою страницу и держит
  локаль.
- «leaks» в английской `packages/solo/doc/resources.md` остаётся как есть,
  2026-09-19: замену, сделанную в русском переводе вместо метафоры «течёт»,
  на оригинал не переносить.
- Правка личных скиллов идёт порциями, 2026-09-19: одна проверенная правка —
  один коммит, а рецепт из команд встаёт в текст только после прогона
  на тяжёлом случае. Правило записано в `README.md` репозитория скиллов
  коммитом `866c1ea`; повод — `2026-09-19-end-handoff-skills-review-3.md`.
- Выпуск `0.3.0` отложен 2026-09-15 словом «пока рано»; собранное к нему —
  раздел «На следующий релиз собрано у всех трёх». Push — только отдельным
  поручением: прошлые разрешения были на свои push и на них одни.
- Перенос владения отменён окончательно 2026-09-15:
  `2026-09-14[17]-discard-follows-the-value-design.md` не делать. Освобождение
  регистрирует получатель — `ctx.run(opener, discard: …)`; разбор —
  `2026-09-15[8]-cleanup-registers-on-arrival-report.md`.
- Кортежная форма `ctx.runAll` отложена: добавляется потом без слома.
- Накопление через `externalSetState` отклонено:
  `2026-09-10[6]-state-accumulation-design.md` не реализовывать. Досылка при
  закрытии, надёжный буфер и автоматические повторы — отдельная работа,
  не начата.
- Имя дренажа по-русски, 2026-09-22: `SoloCloseMode.drain` в русской прозе
  зовётся «дренаж», и это единственное существительное для него — по нему
  читатель догадывается сам, а «слив» такого не даёт. «Досылка» и «слив»
  из документов убраны; где проза обходится без существительного, она называет
  сам вызов `close(mode: SoloCloseMode.drain)`, а состояние — `isDraining`.
- Бэктики в русской прозе, 2026-09-15: в них берут inline-код, имена классов
  и имена переменных; future в прозе — обычное слово, строчными и без бэктиков
  (`86edb87`).
- README объясняет API новому читателю, без ссылок на прежнее поведение
  и контекст диалога; в README `async_job` сохранять `removeDisposer` и пример
  ленивого лога из `479e934`.
- «The second attempt» в `accumulation.md` остаётся без входа отмены: попытка
  должна провалиться, иначе накопитель ниже не нужен (2026-09-15).
- Старые записи `docs/records/` не переименовывать, 2026-09-19: имена
  `YYYY-MM-DD[N]-…` остаются как есть, предложение отклонено.
- scopo не трогать, 2026-09-19. Находка о его `CHANGELOG.md` — раздел `0.14.0`
  говорит об удалении `ScopeInitState`, `ScopeProgress` и `ScopeReady`,
  а в дереве тега они есть — передана владельцу и закрыта этим решением.

**Грабли.**

- Старая запись, названная `tool/reflow.py` ради правки шапки, перезаливается
  целиком — шапку править руками и скрипту запись не называть. Склейка
  вложенных списков починена 2026-09-19 в обеих копиях скрипта, `verify.py`
  скилла `reflow` её теперь видит; сторожа — `tool/reflow_test.py`, шаг
  в задании `docs` гейта.
- Миксин `SoloListenable`: в позиции типа аргумент обязателен, хотя
  `strict-raw-types` его не требует; прямое создание —
  `class C<S extends Object> = Solo<S> with SoloListenable;`, форма с `{}`
  не компилируется. Старшинство `onListenerError` —
  `2026-09-18[1]-solo-listenable-mixin-design.md`.
- Пол `sdk: ^3.6.0` не проверен никем: `lints` 6 и `flutter_lints` 6 требуют
  Dart 3.8, поэтому на 3.6 и 3.7 `pub get` в репозитории не разрешится. Нужен
  такой пол для работы — линты не поднимать, не подняв пол пакетов.
- Версии пакетов и экшенов выяснять командой — `npm view <пакет> version`,
  `gh api repos/<экшен>/git/matching-refs/tags/v`, — а не по памяти.
- Что в `async_job` держится и чего не трогать — раздел «Positive findings»
  в `2026-09-15[17]-async-job-project-review.md`.

**Не трогать.** Копии `~/development/my/solo-resources` на диске больше нет:
соседняя сессия сняла её, закончив вычитку, а её ветка влита. Чужая правка
`agy/SKILL.md` в `~/.claude/skills` закоммичена соседней сессией (`fcf84a8`),
дерево того репозитория чисто. Своих копий и веток нет: копия
`~/development/my/solo-children` и ветка `docs/children` сняты 2026-09-19 после
слияния. На `origin` осталась ветка `docs/cancellation` на `44f46cb`, то есть
до правок по чтению; снять её можно только push'ем.

**Дерево и проверки.** `main`. Работа пятого круга — запись разбора, поправки
к отчёту про `/clear` и правки handoff — ушла на `origin` вместе с отправками
соседней сессии (ниже). `~/.claude/skills` отправлен по двум поручениям
владельца 2026-09-19: сперва `fcf84a8..866c1ea`, четыре коммита — четвёртый
круг ревью, пятый, правило о том, как править скиллы, и `e7bd996` соседней
сессии по `doc-reader`, — потом `866c1ea..e2ebf0e`, три коммита этой сессии про
Fable. Дерево того репозитория чисто, неотправленного в нём нет. Код пакетов
за сессию не менялся, наборы `dart test` не гонялись; прогнаны документные
проверки из корня — `reflow.py --check`, `check_line_width.py`,
`check_doc_shape.py`, `check_translations.py` и `check_links.py` — и сторожа
скиллов: `check_move_test.py` (17 тестов) и `reflow_test.py` (11), все зелёные;
шесть новых сторожей краснеют на прежней версии скрипта. Сам `check_move.py`
после правок перепроверен на трёх настоящих уборках solo (`c1e0101`, `fe275eb`
и `04c9664`) — ноль проблем во всех. Скретчпад сессии пуст: копии и лаборатории
трёх ревьюеров, мои стенды и выгрузка строк бандла Claude Code убраны
по поручению владельца 2026-09-19; скретчпад этой сессии тоже пуст. Из проб
в `/tmp` по-прежнему только чужое — каталоги от 2026-09-18, `c7a539-probes`
и `kokjai-t1-agy`, в котором 2026-09-19 к 18:45 шёл чужой прогон `agy`. Слияние
`docs/resources` сделано в этом дереве 2026-09-19: конфликт был только
в `docs/handoff.md`, где верх ветки описывал ту же работу, и снят в пользу
текущего состояния. Проверки на слитом дереве: `packages/solo` — `dart format`
без изменений, `dart analyze lib test` чисто, `dart test` 661;
`packages/async_job` — то же и 468, а после сторожа, пришедшего из чтения, 469;
из корня `check_line_width.py`, `check_translations.py`, `check_doc_shape.py`
и `reflow.py --check` зелёные. По двум поручениям владельца 2026-09-19
отправлено `0da7078..8737b7b` — 24 коммита, слияние со всей веткой — и следом
`8737b7b..a30dd17`, ещё 12: правки `cleanup.md` по чтению,
`tool/check_links.py` со сторожами и починка `check_line_width.py`.
`origin/main` — `a30dd17`, `gate` на нём зелёный (прогон `35443689022`), `site`
тоже (`35443689052`). По поручению владельца 2026-09-19 отправлено
`a30dd17..a1c74bb` — одиннадцать коммитов: восемь по чтению `cleanup.md`
и сведённому хвосту, `8b42d5c` соседней сессии и два с записями о ревью.
`origin/main` стал `a1c74bb`. Push из-под песочницы инструмента не проходит:
`curl` и `gh` до github доходят, а `git` получает «Failed to connect to
github.com port 443» — отправлять приходится с отключённой песочницей. Ревью
2026-09-19 сделано в скретчпаде сессии: четыре копии дерева на `67b0a3e` под
ревьюеров и пятая, моя, для перепроверки находок — в ней лежат мои зонды
и стенд замера. Копии одноразовые и уходят вместе со скретчпадом; рабочего
дерева ревьюеры не касались. После записи ревью прогнаны из корня
`reflow.py --check`, `check_line_width.py`, `check_links.py`,
`check_doc_shape.py` и `check_translations.py` — все зелёные; код пакетов
не менялся, наборы не гонялись, базовая линия ревью снята до его начала
и записана в разборе.

Правка трёх High сделана в рабочем дереве 2026-09-19. Проверки после неё:
`packages/solo` — `dart format` без изменений, `dart analyze` чисто,
`dart test` 665; `packages/flutter_solo` — то же и `flutter test` 95; из корня
все пять документных проверок зелёные. Прогнан и стенд документов: три пакета
собираются, все драйверы доходят до конца, `check_traces.py` сверяет тринадцать
процитированных трасс. Мутации гонялись прямо в дереве, копией файла
с восстановлением в `finally`, а не `git checkout`: незакоммиченного в дереве
было на всю правку. Волна четырёх Medium сделана тем же вечером, проверки после
неё: `dart test` в `solo` — 672, `flutter test` — 96, `dart doc --dry-run` без
предупреждений, пример `solo` — 9, стенд документов и тринадцать трасс, пять
документных проверок — всё зелёное. Волна пяти Medium сделана следом, проверки
после неё: `solo` — `dart format` без изменений, `dart analyze`
и `dart doc --dry-run` чисто, `dart test` 679, пример — 9; `flutter_solo` —
то же, `flutter test` 96, пример чисто; стенд Flutter (`flutter.md` и README
`flutter_solo`) — 20 тестов, четыре трассы на месте; стенды `vs-bloc.md`
и `accumulation.md` — тринадцать трасс; пять документных проверок и их сторожа
зелёные. Мутации снова копией файла; одна из них, вызов `onClose` на каждом
`close()`, повесила прогон синхронной рекурсией в `fakeAsync` — фоновая задача
остановлена, файл восстановлен из копии и сверен. Волна четырёх Medium сделана
следом, проверки после неё: `solo` — `dart format` без изменений,
`dart analyze` и `dart doc --dry-run` чисто, `dart test` 683, пример — 9;
`flutter_solo` — то же, `flutter test` 102; пример `flutter_solo` — анализ
чисто и 4 теста; стенд Flutter — 20 тестов, четыре трассы на месте; пять
документных проверок зелёные. Стенды `vs-bloc.md` и `accumulation.md`
не гонялись: код `solo` и `async_job` в этой волне не менялся. Мутации — копией
файла, в том числе одна в `async_job` ради сторожа M17; все файлы восстановлены
и сверены.

Волна A решений по ревью сделана 2026-09-19, проверки после неё: `async_job` —
`dart format` без изменений, `dart analyze` и `dart doc --dry-run` чисто,
`dart test` 472; `solo` — то же, 688, пример — 9; `flutter_solo` — то же,
`flutter test` 102, пример — 4; стенды `vs-bloc.md` и `accumulation.md` — три
пакета чисто, драйверы доходят, трассы на месте; стенд Flutter — 20; пять
документных проверок и сторож `reflow` зелёные. Тринадцать мутаций — копией
файла, все пойманы, файлы восстановлены и сверены.

Волна B сделана тем же вечером, проверки после неё: `flutter_solo` —
`dart format` без изменений, `flutter analyze` и `dart doc --dry-run` чисто,
`flutter test` 97; пример — анализ чисто и 4 теста; стенд Flutter — анализ
чисто, 19 тестов, четыре трассы на месте; пять документных проверок зелёные;
сайт собран, 43 страницы. Код `solo` и `async_job` не менялся, их наборы
и стенды `vs-bloc.md` и `accumulation.md` не гонялись. Девять мутаций — копией
файла, все пойманы, файлы восстановлены и сверены.

Волна C сделана следом, проверки после неё: `solo` — `dart format` без
изменений, `dart analyze` и `dart doc --dry-run` чисто, `dart test` 689,
пример — 9; `flutter_solo` — то же, `flutter test` 97, пример — 4; стенды
`vs-bloc.md` и `accumulation.md` — три пакета чисто, драйверы доходят, трассы
на месте; стенд Flutter — 19, четыре трассы на месте; пять документных проверок
зелёные; сайт собран, 43 страницы. Код `async_job` не менялся, его набор
не гонялся. Пять мутаций — копией файла, все пойманы, файл восстановлен
и сверен.

Волна D сделана 2026-09-20, проверки после неё: `solo` — `dart format` без
изменений, `dart analyze` и `dart doc --dry-run` чисто, `dart test` 694,
пример — 9; `flutter_solo` — то же, `flutter test` 83, пример — 4; стенды
`vs-bloc.md` и `accumulation.md` — три пакета чисто, драйверы доходят, 2 и 11
трасс; стенд Flutter — 19 тестов, 4 трассы; пять документных проверок и сторож
`reflow` зелёные; сайт собран, 42 страницы. Тестов в `flutter_solo` стало 83
вместо 97: шестнадцать ушли вместе с копией `Listeners`, три добавлены волной.
Код `async_job` не менялся — только его `doc/outcomes.md` и changelog, — его
набор не гонялся. Девять мутаций — копией файла, все пойманы, файлы
восстановлены и сверены; два сторожа волны мутации не имеют, и почему —
в отчёте. По поручению владельца 2026-09-20 отправлено `8c554b6..5cdbb5e` —
десять коммитов: три High, три волны Medium, решения владельца и четыре волны
по ним. `gate` и `site` на нём зелёные (прогоны `35478600408` и `35478600437`).
Следом отправлен и этот коммит состояния, `9f2620d`: `origin/main` — он,
неотправленного в `main` не осталось, `gate` зелёный (`35478755503`). Push
и с отключённой песочницей проходит не с первой попытки: соединение с github
отваливается по таймауту в 75 секунд, помогает повтор.

В `kokjai` мой коммит `ea18852` — заливка его документов — лежит среди
неотправленных: владелец отправку того репозитория не разрешал.

## На следующий релиз собрано у всех трёх

Ломающие правки есть в каждом пакете, поэтому все трое идут на `0.3.0`.
Развёрнуто — в разделах `## Unreleased` их `CHANGELOG.md`; коротко (счёт сверен
2026-09-19): у `async_job` шесть ломающих, громкие из них — переезд `JobBase`,
`JobContextBase` и `JobStatus` в `engine.dart` и протектед
`JobBase.inUncancellableSection`; у `solo` четырнадцать своих (`SoloBase` →
`Solo` вместе с переездом стрима в `SoloStream`, защищённые `job`, `add`,
`run`, `collect` и `accumulate`, `currentState`, `SoloCloseMode`,
`SoloTransition`, `errorHandler`, свои слушатели, `isFinished`, финальное
состояние закрытого, умолчание накопителя `join`, перенос вместо отмены
у `AccumulationPolicy.replace`, две про `droppable` и снятый
`SoloQueue.lastWhere`) плюс раздел унаследованных, `Solo.pending` и новая
библиотека `package:solo/listeners.dart`; у `flutter_solo` семь своих
ломающих — шестая, снятый реэкспорт `ValueListenable`, из волны B, седьмая,
`SoloSubscriptions.length` без отменённых, из волны D — плюс раздел
унаследованных, и новые `SoloSelection`, `SoloSelection.from`, `SoloSelector`,
`SoloBuilder` и `select`/`listen`.

Порядок связки задан зависимостями: `solo` уже пользуется новым
`inUncancellableSection`, поэтому в дереве и живут четыре
`pubspec_overrides.yaml`. Сперва `async_job`, затем `^0.3.0`
в `packages/solo/pubspec.yaml` и `solo`, затем `^0.3.0`
в `packages/flutter_solo/pubspec.yaml` и `flutter_solo`. Оверрайды снимаются
и добавляются в индекс — порядок
в `2026-09-11[1]-solo-release-readiness-report.md`.

Версии в трёх `pubspec.yaml` пока `0.2.0`, заголовки в `CHANGELOG.md` —
`## Unreleased`; и то и другое меняется при подготовке. Вместе с ними — строка
про стенд во вступлении `vs-bloc` (оригинал и перевод): там названа версия
исходников, на которых собраны фрагменты, и сейчас это `0.2.0`.

## Документация: вычитка страниц doc/

Начата сквозная вычитка страниц, которых она ещё не касалась: вычитаны
`packages/async_job/README.md`, `packages/solo/doc/vs-bloc.md`,
`packages/solo/doc/accumulation.md`, `packages/solo/doc/jobs.md`
и `packages/solo/doc/state.md` — прозой, граблями и чтением владельца.
`cancellation.md` — первые попытки и правки прозы, слияние `50077c5`; отчёт —
`2026-09-18[8]-cancellation-rakes-report.md`. Стенда у страницы нет, как
и у `state.md`: сторожа — `packages/solo/test/cancellation_rakes_test.dart`,
и правка страницы идёт вместе с ним. Разбор `jobs.md` и прозы `state.md` —
в `2026-09-18[6]-handoff-archive.md`, граблей и чтения —
в `2026-09-16[2]-state-rakes-report.md` и `2026-09-18[7]-handoff-archive.md`.
Порядок — карта из `2026-09-11[16]-docs-structure-design.md`: `jobs.md`,
`state.md`, `cancellation.md`, `resources.md`, `children.md`, `errors.md`,
`testing.md`, `camera.md`, `flutter.md` (теперь
`packages/flutter_solo/doc/mixins.md`), затем шесть страниц `async_job` и три
README.

`cleanup.md` пакета `async_job` вычитана на той же ветке, влита тем же слиянием
и прочитана владельцем; сторожей после чтения 23: 170 строк одного раздела
стали 263 строками двенадцати заголовков, семь блоков — одиннадцатью, три места
с граблями открываются первой попыткой. Сторожа —
`packages/async_job/test/cleanup_rakes_test.dart`, 23 теста и восемнадцать
мутаций; отчёт — `2026-09-19-cleanup-rakes-report.md`. В страницу дописана
оговорка, без которой её предупреждение не воспроизводится: окно, в котором
регистрация под `run` не делается вовсе, открывает ребёнок
с `cancellable: false`.

`resources.md` вычитана на ветке `docs/resources` и влита в `main` 2026-09-19;
владелец её прочитал, все тринадцать правок внесены: 115 строк стали 301,
четыре блока кода — одиннадцатью, а три раздела — четырьмя с первыми попытками
плюс прозаический о порядке уборки. Стенда у страницы нет, сторожа —
`packages/solo/test/resources_rakes_test.dart`, 20 тестов и четырнадцать
мутаций; отчёт — `2026-09-19-resources-rakes-report.md`. Заодно снято неверное
утверждение: `Cancelled` из уборки доходит и до обработчика, и до наблюдателя,
а не доходит он только до зоны.

`errors.md` вычитана 2026-09-20 в отдельном worktree по слову владельца, ветка
`worktree-doc-errors`, влита в `main` 2026-09-22. Страница шла по карте
следующей и пришла без единой первой попытки; из сторожей на ней был только
`cancel_delay_recipe_test.dart` на рецепт задержки. Двадцать два зонда по её
утверждениям дали пять расхождений, все взяты владельцем. Главное:
переопределённый `onError` без `super` заменяет маршрут «обработчик, иначе
зона» целиком и уносит бесприютные ошибки в никуда, а вводный пример страницы
написан именно так. Ещё: `Cancelled` от брошенной операции доходит до хуков
и не доходит только до зоны — та же правка, что уже сделана в `resources.md`,
до соседней страницы не дошла; `Solo.traceStateChanges` не был упомянут прозой
нигде во всём дереве; заметка про `clock` обещала, что зависимость заводить
не нужно; в списке хуков не хватало `onListenerError`.

Вторым заходом страница перестроена: шесть разделов из восьми открываются
первой попыткой, а хуки и наблюдатель, стоявшие во вступлении, стали двумя
разделами. 248 строк стали 426, блоков кода 16. Сторож —
`packages/solo/test/errors_rakes_test.dart`, 37 тестов и одиннадцать мутаций;
отчёт — `2026-09-20-errors-rakes-report.md`.

Чтение владельца началось 2026-09-22 и идёт по разделу `Reporting an error`.
По его вопросам снято «не чучело» из вступления (правило от 2026-09-14 —
писать, чем первая попытка является), раскрыто «хук — это ещё и сам маршрут»
и переписан комментарий про «отправить — не значит ответить». Заодно нашлось
расхождение с кодом в моём же абзаце: ненаблюдённый провал тела был назван
бесприютной ошибкой, а обработчика для него не спрашивают — зонд показал, что
до зоны он доходит сам, через исход. Страница поправлена, сторож на это
утверждение добавлен.

Из того же чтения вышло разделение хука: вопрос владельца «стоит ли делать
такую зависимость от `super.onError`» оказался про API, а не про текст.
Разделение сделано и влито, раздел `Reporting an error` переписан под два хука
и открывается ответом, а первой попытки в нём больше нет — чтение продолжается
с него.

Проход скиллом `doc-reader` по переписанной странице, 2026-09-22: скрипты
и проверки формы чисты, чтением нашлось три места, и два из них — расхождения
с кодом. Упавший слушатель до `onError` не доходит вовсе, он уходит
в `onListenerError`; последняя инстанция для ошибки переоценки — зона создания
задачи, а не контроллера. Третье — поле `_deviceFailures` в примере без единого
устройства. Разбор и зонд — в конце `2026-09-20-errors-rakes-report.md`.

Сам скилл поправлен по слову владельца 2026-09-22, коммит `ce86af9`
в `~/.claude/skills`: он требовал писать во вступлении, что первая попытка
«не чучело», вопреки решению от 2026-09-14. Теперь там сказано говорить, чем
она является, и сказано прямо, что первая попытка живёт, пока жива ловушка:
раздел, где ошибаться не на чем, открывается ответом.

`children.md` вычитана и влита слиянием `a8395a4` — той самой находкой ревью
она и была заказана: три сравнения `.wait`/`Future.wait`/`runAll` в одной
странице и разбор конверта раньше примера. Стенда у страницы нет, сторожа —
`packages/async_job/test/children_rakes_test.dart`; отчёт —
`2026-09-18-children-rakes-report.md`. Чтение владельца дало ещё десять правок,
девять из них в соседней `packages/solo/doc/children.md`: её сторожа стоят
в `children_test.dart`, `each_test.dart`, `each_child_test.dart`
и `zone_test.dart` своих пакетов, отдельного файла у них нет.

**Код `jobs.md` не собирается ничем.** Стенды есть только у `vs-bloc.md`
(`tool/doc_snippets.py`), `accumulation.md` (`tool/accumulation_snippets.py`),
README `flutter_solo` с его `doc/mixins.md` и быстрого старта README `solo`
(`tool/flutter_snippets.py`); у остальных страниц фрагменты не компилируются
ни в гейте, ни перед коммитом. Фрагменты `jobs.md` собраны отдельным проектом
вручную — компилируются все, — но на дереве такой проверки нет. Рецепт паузы
при этом сторожит `packages/solo/test/queue_pause_recipe_test.dart`, семь
сторожей.

**Стенда у `state.md` тоже нет, сторожа — тестом.** Каждую первую попытку
страницы и каждое утверждение, добавленное чтением владельца, держит
`packages/solo/test/state_rakes_test.dart`, и у каждого сторожа своя мутация
движка. Правка страницы идёт вместе с ним.

## Требования к README, которые нужно сохранить

- При описании API на русском называть `Job` прямо. Слово «задача» уместно для
  самой работы, но не должно подменять имя класса в объяснении его
  возможностей.
- `ctx.join(Database.open)` объясняется самостоятельно до сравнения с `wait`:
  вызывает операцию, ждёт её завершения и проверяет отмену. `wait`
  предлагается, когда нужно прекратить ожидание сразу при отмене.
- `discard` после `return` объясняется на примере ожидания дочерней задачи:
  тело вернулось, но весь `Job` ещё не завершился.
- В примере миграции `onCancel` передаёт сигнал токену, `join` ждёт фактической
  остановки, а `uncancellable` защищает шаг от самого сигнала.
- Внешние асинхронные операции в теле следует ожидать через контекст. Прямой
  `await` допустим, но сам не проверяет отмену. Внутри действия, переданного
  контексту, и при уборке обычный `await` уместен с учётом описанных в README
  границ.
- Упоминание `solo` следует из границ ядра: в `async_job` нет управления
  состоянием, очереди и правил планирования; `solo` добавляет их.
- Два примера проброса `Cancelled` показывают отдельную ветку `catch`
  и проверку типа внутри общего обработчика. Оба способа подходят и для
  `Exception`, и для `Object`; выбор типа не различает эти способы.
- Принятая отмена самого `Job` сохраняется после перехвата исключения. Код
  после `catch` может выполниться, следующая контрольная точка снова бросает
  отмену, а при завершении исход остаётся `Cancelled`. Перехваченный
  `Cancelled` от операции или дочерней задачи сам по себе родителя не отменяет:
  возможен `Done`.

Число блоков кода в README больше не годится как ориентир: материал разъехался
по страницам `doc/`. Форму держат `check_translations.py` (двадцать пар)
и `check_doc_shape.py`. Примеры `then`, его контракт и переводы обновлены
вместе с реализацией.

## Дальше, когда владелец выберет новую работу

Материал и замеры: `2026-09-09[9]-todo-review.md`. Готовый стенд может лежать
в `.artifacts/2026-09-09-wave2-review/a/stand`; восстановление —
`python3 tool/doc_snippets.py <каталог>`. При проверке внешней операции
дожидаться самой операции: снимок состояния сразу после отмены `Job`
не показывает её окончательный результат.

Пожелания владельца находятся в `docs/backlog.md`. После окончания текущей
реализации предложить выбрать оттуда работу; самостоятельно записи не менять.
Другие ранее отложенные примеры для `solo`: `stateAs`, `check`, retry/backoff.
Их брать только после выбора владельца.

Имя ядра решено 2026-09-19: держатель `job` ответил, и владелец остаётся
на `async_job`. Напоминаний больше не нужно, переписку не продолжать; разбор —
`2026-09-05[8]-job-name-request-report.md`, его шапка закрыта тем же днём.

## Ограничения и общение


- Не приписывать владельцу ответы на формы или выбор вариантов, которые он
  не давал. Собственную интерпретацию запроса называть своей.
- Владелец просит минимум служебного вывода: ему нужны сообщения по делу,
  а не журналы запусков. Заголовки инструментов формирует интерфейс; не обещать
  скрыть их, сокращать число вызовов и объём вывода.
- Текст рабочих документов переносить на 79 символах. Правило и порядок
  проверок сохранены в `AGENTS.md`.
- Английский README и русский перевод править вместе. Связность и смысл важнее
  косметического сокращения; подтверждённые условия не выкидывать.
- Не менять заодно согласованную семантику отмены.
- `ctx.run` принимает `Job<T>`, а не только `DeferredJob<T>`: библиотечные
  классы на основе `JobBase` тоже могут быть дочерними. Автоматически
  запускающийся обычный `Job` отвергается.
- Не расширять защищённые `owner` и `parent` без подтверждённой нужды
  потребителя. `isDisposing` не выносить в публичный `Job` без нового решения.
- Буфер ранних событий в `each` сохранять; прежние альтернативы теряли события
  при подписке. При ошибке тела дочерние задачи дорабатывают; при самоотмене
  тела отмена передаётся детям.
- Соседний `scopo` ведётся отдельно. Его прежний переход на опубликованное ядро
  0.1.0 выполнен; обновление на новый API не входит в эту работу. Архивирование
  `stream_future` тоже отдельная внешняя задача.
- `/Users/user/development/yandex/yx_camera` не трогать: это чужой проект.
- Не запускать push, публикацию, теги или релизные коммиты без отдельного
  поручения. Правила связки публикации полностью заданы в `AGENTS.md`.

## Архив

- Законченное до 2026-09-18 — `2026-09-18[6]-handoff-archive.md`.
- Законченное до 2026-09-18, второй вынос — `2026-09-18[7]-handoff-archive.md`.
- Законченное до 2026-09-19 — `2026-09-19-handoff-archive.md`.
- Законченное до 2026-09-19, третий вынос — `2026-09-19-handoff-archive-2.md`.
- Прежняя большая версия handoff — в git на `659512a`.
