# Local development only: `jobs` is resolved from the tree until it is on
# pub.dev. Kept out of `pubspec.yaml` so the published archive carries no
# dependency_overrides.
dependency_overrides:
  jobs:
    path: ../jobs
