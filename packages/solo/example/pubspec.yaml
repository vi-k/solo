name: solo_example
description: A fake camera controller built on solo.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  meta: ^1.15.0
  solo:
    path: ../

dev_dependencies:
  fake_async: ^1.3.1
  lints: ^5.1.1
  test: ^1.26.3
