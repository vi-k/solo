name: async_job
description: >-
  A cancellable Future: a job with an outcome, children, cooperative
  cancellation and a waiting family. Pure Dart.
version: 0.1.0
repository: https://github.com/vi-k/solo/tree/main/packages/async_job
issue_tracker: https://github.com/vi-k/solo/issues
topics:
  - async
  - cancellation
  - concurrency

environment:
  sdk: ^3.6.0

dependencies:
  meta: ^1.15.0

dev_dependencies:
  fake_async: ^1.3.1
  lints: ^5.1.1
  test: ^1.26.3
