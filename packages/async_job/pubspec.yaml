name: async_job
description: >-
  Cooperative cancellation, child tasks, resource cleanup and explicit
  outcomes for asynchronous Dart code. Pure Dart.
version: 0.2.0
repository: https://github.com/vi-k/solo/tree/main/packages/async_job
issue_tracker: https://github.com/vi-k/solo/issues
topics:
  - async
  - cancellation
  - concurrency

environment:
  sdk: ^3.6.0

dependencies:
  meta: ^1.15.0

dev_dependencies:
  fake_async: ^1.3.1
  lints: ^6.1.0
  test: ^1.26.3
