# Russian translation, kept in the repository but not published.
README.ru.md

# The example's local paths to packages that are not published yet.
# `example/` ships inside the package, and they lead outside the archive.
example/pubspec_overrides.yaml
