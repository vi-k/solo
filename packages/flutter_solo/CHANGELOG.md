## 0.2.0

- Floor raised to `solo: ^0.2.0`, whose job kernel lives in
  `package:jobs`. Nothing changes in `SoloListenable` itself.

## 0.1.0

- Initial release: SoloListenable.
- Re-exports `package:solo/solo.dart`, so importing `flutter_solo` alone is
  enough for `Solo`, `Job`, `JobContext`, `Outcome` and the rest.
